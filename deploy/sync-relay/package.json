{
  "name": "cherry-chat-sync-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal install manifest for the Cherry Chat personal sync relay Docker image (Linux x64/arm64). Versions pinned to the repository root manifest.",
  "license": "Apache-2.0",
  "dependencies": {
    "better-sqlite3": "12.11.1",
    "tsx": "4.20.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  }
}
