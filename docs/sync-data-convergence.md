# Sync Data Convergence — Approved ADR

> **Status**: Approved / Authoritative (target semantics).
> **Decision namespace**: `SYNC-DATA-*` (durable; this ADR owns these IDs).
> **Role**: This ADR owns target personal multi-device data-convergence semantics only: what complete synchronization means, what state may move, how divergent histories converge, and when a device may truthfully claim convergence. It does not own current implementation status, evidence, gaps, or next step — those live in [Personal Multi-Device Sync](./multi-device-sync.md). It does not own connection/registration/channel/pairing semantics — those live in [Sync Connection, Registration, and Hidden Multi-Channel Pairing ADR](./sync-connection-channel.md) (`SYNC-CC-*`). It does not own candidate/fallback analysis — that lives in [Sync Architecture Selection](./sync-architecture-selection.md) (conditional fallback reference only).
> **Non-implementation**: No protocol/code implementation is authorized by this ADR. Implementation requires a separate explicitly activated decision and step.
> **Development principle**: This ADR is governed by `adaptive-development` principles: the intended outcome remains the anchor, current state and gap determine the next step, and design, implementation, evidence, and validation evolve together.
> **Last updated**: 2026-09-10 — inventory 审阅 F-01..F-05 关闭（`SYNC-DATA-041`–`SYNC-DATA-045` / `SYNC-CC-021` 修订）：锁定 `sync-baseline-wire-v1`（wire 协议，仅 outer envelope）/ `chat-core-baseline-v1`（payload schema）/ `topic-message-stable-block-order-v1`（inventory）/ `parent-order-frame-v1`（order-frame）四版本角色与命名（不暗示 complete-product sync，三文档一致）；`wireVersion` 仅在 outer envelope，manifest 仅回显 `payloadSchema`/`inventoryVersion`/`orderFrameVersion` 三内容版本（F-01 矛盾已删）；**byte-level v1 锁定 JSON key spelling 与层次** — outer envelope / logical payload root / manifest / entity / tombstone / order-frame / clock 均固定 lowerCamelCase 集合（F-02，详见 §10A.1/`SYNC-DATA-040`/`SYNC-DATA-045`；路径/HTTP verb/TS 类型/DB 表/存储映射仍 deferred）；固定 `topic/message/messageBlock` 的 full-state 逻辑字段与可空/非空/禁止规则、per-row `sortOrder` 严禁上 wire、transient status 与 `tool/file/image/video/citation` 等 structured 块类型门控并补齐全量 JSON type/value domain（F-03，id/parent 非空 Unicode scalar、字符串字段 `string|null`、布尔字段 `boolean|null`、时间字段 I-JSON `string|null` 原样不解析/不归一化、所有键固定存在）；锁定 parent/frame 全集与 empty-frame 规则及 transient/永久 excluded 语义（F-04，每 live included parent 恰一 frame、零子项空 frame、`orderedChildIds` 恰为 inventory-included live children 经 `SYNC-DATA-035` 覆盖/追加后全集）；legacy tombstone `null operationId` 改为强删除屏障 `(T,null)` 高于同 `T` 任何 `(T,nonNull)` 并抑制同 `T` 全部 live 操作、两 `null` 等价、新 tombstone `operationId` 必须非空不可伪造且为 legacy compatibility order 不用于新 clocks（F-05 前半）；manifest counts/completeness 必须在 relay 与 client 从已验证 entities/tombstones/orderFrames + parent closure + membership clocks + frame coverage 唯一重算、声明不匹配 `400`/fail-closed（F-05 后半）；`jcs-sha256-v1` 输入仅 `payload` 对象（logicalPayload）不含 outer envelope/digest，outer digest 键为 `digest`（`digestScheme` 另存）且数组顺序规则保持（F-02/F-05）；`SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021` 未锁范围已更新为：inventory v1 baseline JSON spelling 已锁、未来操作名/端点/TS/DB/存储仍未锁、order-frame JSON keys 在 v1 已锁定；三文档一致传播、明确 living doc 当前代码尚未符合固定 wire keys/shape、仍 target-only/未授权实现。先前 F-01..F-04 / `SYNC-DATA-032` `jcs-sha256-v1` / 三段式边界等保持不变并纳入 F-05；2026-09-11 修订：新增 `SYNC-DATA-046` 锁定 relay baseline 资源传输绑定（`PUT`/`GET /sync/baseline`、exact envelope、单 current 行、status/body spelling，reference-relay 已实现）；2026-09-11 新增 `SYNC-DATA-047` 锁定 receiver bootstrap 合并事务与失败语义（200 严格验证 envelope exact keys/channel/digest、单 SQLite 事务 merge+cursor=N、事务前失败不改本地/cursor0、提交后 N+1 pull 失败保留 N；endpoint 仍由 `SYNC-DATA-046` 锁定，此处不重锁），其余 Locked 语义不变。

---

## 1. Context and problem

The implemented operation log converges covered topic, message, and message-block shapes between devices that share history, but the product question it does not answer is data convergence: what a newly paired device receives, what two devices with pre-existing independent data become, how a third device joins later, how long history survives retention, how divergence is detected and repaired, and when the UI may claim devices hold the same user data.

Without locked convergence semantics, each of those questions invites a local shortcut — raw database copy, one-directional overwrite, absence treated as deletion, cursor reset on missing history, or push/pull retry presented as reconciliation — that would silently lose user data or falsely claim synchronization. This ADR locks the target convergence semantics as the design authority for complete synchronization of the syncable personal-data domain.

## 2. Authority boundary

- **This ADR owns**: the definition of complete synchronization for the syncable personal-data domain; local-first authority placement for converged data; the hybrid baseline plus operation-log plus reconciliation shape; logical baseline properties, origin, and exclusions; relay-persisted per-channel current-effective baseline and offline-bootstrap availability; first-pairing and late-join convergence rules; identity, field-merge, same-field ordering, and tombstone semantics; parent-scoped order-frame semantics for user-visible message/block sequence including frame structure, authority boundary, concurrent LWW merge with deterministic retention append, atomic dense materialization, and baseline/incremental equivalence (`SYNC-DATA-033`–`SYNC-DATA-036`); baseline logical-payload order-frame coverage and structural-operation issuance rule (`SYNC-DATA-037`); wire inventory exclusion of per-row `sortOrder` with local DB projection authority preserved (`SYNC-DATA-038`); per-version strictly closed schema/inventory with fail-closed unknown fields/version (`SYNC-DATA-039`); non-locking scope for op/endpoint/TS/DB/JSON spelling (`SYNC-DATA-040`); baseline-watermark binding with the three-segment publish/retain/apply boundary and local/outbox preservation; aggregate completeness; the syncable-data inventory requirement; retention/compaction preconditions with single-effective-baseline replacement; baseline publish serialization with watermark-monotonic/coverage gating (`SYNC-DATA-023`) and in-flight bootstrap protection (`SYNC-DATA-024`); single-stage client-declared publish/fetch wire contract with publisher barrier/snapshot, envelope categories, relay validation boundary, and publish/fetch state machine plus status codes (`SYNC-DATA-025`–`SYNC-DATA-031`); public baseline digest contract `jcs-sha256-v1` with input scope, fail-closed canonicalization, relay recompute limit, and atomic binding (`SYNC-DATA-032`); manifest/digest reconciliation and repair; data-convergence status semantics; Disconnect/Unpair data consequences; version-incompatibility and failure semantics for convergence.
- **This ADR does not own**: current implementation status/evidence/gaps/next step ([multi-device-sync.md](./multi-device-sync.md)); connection/registration/channel/pairing and relay baseline envelope storage/distribution infrastructure semantics ([sync-connection-channel.md](./sync-connection-channel.md)); fallback candidate analysis ([sync-architecture-selection.md](./sync-architecture-selection.md)); application identity, updater/release freeze, platform scope ([cherry-chat-application-identity.md](./cherry-chat-application-identity.md)); SQLite chat authority and migration process ([sqlite-migration.md](./sqlite-migration.md)); context-window semantics ([context-window.md](./context-window.md)).
- **This ADR does not authorize**: protocol implementation, endpoint path strings, HTTP verb spellings, chunking/streaming, size thresholds, compression, retained physical history counts, attachment transport, E2EE design, schema migrations, IPC changes, or any code change beyond the locked wire JSON spelling in §10A.1/`SYNC-DATA-040`/`SYNC-DATA-045`. Parent-scoped order-frame operation names/endpoint/shared TS types beyond the wire DTO/DB tables mapping beyond the wire DTO per updated `SYNC-DATA-040`/`SYNC-DATA-045` remain deferred, but order-frame/ envelope / payload / manifest / clock / tombstone **wire JSON key spelling for inventory v1 is locked** (lowerCamelCase fixed sets per §10A.1). It creates no permissions/admin/merge/GC schedule beyond what is written here. Publish/fetch operation semantics and status codes are locked by `SYNC-DATA-025`–`SYNC-DATA-031` as a code-less contract; transport spellings and limits listed in `SYNC-DATA-031` stay deferred. `SYNC-DATA-033`–`SYNC-DATA-039` plus `SYNC-DATA-041`–`SYNC-DATA-044` plus §10A.1 lock wire logical semantics **and** wire JSON spelling for inventory v1; remaining deferred: future operation names/endpoints/TS types beyond wire DTO/DB mapping/storage. Current `local-sync-baseline-v1` remains internal/non-wire (F-02/F-05, not wire-compliant).

## 3. Decision table (`SYNC-DATA-*`)

| # | Decision | Status |
|---|---|---|
| **SYNC-DATA-001** | The user-observable product is complete synchronization of the explicitly versioned syncable personal-data domain: after convergence, each paired device holds the same logical user data for the claimed domain. Operation transport alone is not the product; moving operations without reaching the same logical state does not satisfy this ADR. | **Locked** |
| **SYNC-DATA-002** | Each device remains local-first with Main SQLite authoritative locally for its own copy. The relay remains authenticated hidden-channel store-and-forward/coordination infrastructure and never becomes chat authority. Convergence is decided by each device applying authenticated channel state under these semantics; the relay never decides what user data is true. | **Locked** |
| **SYNC-DATA-003** | Convergence architecture is hybrid: logical baseline/bootstrap plus incremental operation log plus manifest/digest reconciliation with targeted repair. The existing operation log remains the normal continuous convergence mechanism and is not replaced by physical database copy. Baselines bootstrap or repair state; they never replace the continuous log as the steady-state mechanism. | **Locked** |
| **SYNC-DATA-004** | Baselines are logical, allowlisted, schema-versioned, device-independent state — never a raw `chat.db` file copy. They carry only the versioned syncable-data inventory (§10) permits, and always exclude credentials, device-local paths, caches/FTS artifacts, and transient/UI-derived state. A baseline that cannot be validated against its declared schema version is rejected fail-closed. | **Locked** |
| **SYNC-DATA-005** | First pairing of devices with pre-existing independent data converges by bidirectional union/merge: each side's pre-pair user state survives unless removed by an explicit rule in this ADR. Absence on one side is unknown/missing, never deletion. Only explicit versioned tombstone/delete evidence removes state; a device must never interpret "the peer did not send it" as "the user deleted it." | **Locked** |
| **SYNC-DATA-006** | Stable entity IDs identify the same logical entity across devices. Independent fields of the same entity merge. Same-field collisions resolve by deterministic version ordering that is compatible with the current timestamp-then-operationId LWW for the current protocol version. Deletion/tombstone evidence and late-descendant containment follow §6: tombstones are explicit versioned state, and a late descendant of a tombstoned parent is contained, never resurrected through. | **Locked** |
| **SYNC-DATA-007** | Third-device and new-device bootstrap uses a baseline bound to a per-channel operation watermark N, then replays operations N+1 onward on the joining device. Concurrent writes during bootstrap must not fall into a snapshot/log gap: any operation above N that the baseline does not already contain must still be delivered through replay or repair. Gap-prone handoff (baseline at N, stream resumed above N+1 without repair) is not convergence. | **Locked** |
| **SYNC-DATA-008** | Baseline application must preserve and merge local pre-pair data (§6) and pending outbox intent on the receiving device. State transfer is not equivalent to acknowledging local operations: applying a baseline never silently drops unsent local intent, and never marks unacknowledged local operations as converged. | **Locked** |
| **SYNC-DATA-009** | Parent/child aggregates require completeness/ordering or atomic group semantics at apply time. A device must not durably present a partial user-visible shell: either the aggregate applies completely and in an order that preserves its references, or it applies as an atomic group, or the incomplete aggregate stays pending/invisible until its references arrive through replay or repair. | **Locked** |
| **SYNC-DATA-010** | A versioned syncable-data inventory is required before any full-sync claim. It classifies required user data, optional separately governed configuration, and device-local exclusions. The currently validated topic/message/partial message-block scope is not complete-product sync. Structured content, ordering/segments/branches, attachments and attachment bytes must receive explicit contract coverage before full-sync claims extend to them. | **Locked** |
| **SYNC-DATA-011** | Operation retention/compaction is allowed only when a valid recoverable baseline plus tombstone/deletion evidence covers the compacted prefix: any device bootstrapping from the retained state must still reach the same logical outcome for live and deleted entities. History-unavailable is an explicit state that routes to bootstrap/repair; it is never resolved by silent cursor reset or by treating missing prefix operations as converged. | **Locked** |
| **SYNC-DATA-012** | Reconciliation compares meaningful state manifests/digests and repairs missing/divergent entities through targeted baseline or operation redelivery. Rerunning push/pull alone is not reconciliation: without comparing what each side holds and repairing what differs, retry proves transport liveness only, not convergence. | **Locked** |
| **SYNC-DATA-013** | Data-convergence state is separate from service connection and pairing state. `Synced` requires, for the claimed domain: baseline completeness (no pending bootstrap/aggregate gaps), operation catch-up (no unapplied channel operations above the device cursor), no unresolved apply failures, and successful consistency verification (manifest/digest comparison per §8). Unsupported, partial, blocked, and repair-required states are explicit and truthful; a device that cannot verify convergence must say so. | **Locked** |
| **SYNC-DATA-014** | Disconnect and Unpair preserve local data on every affected device. Unpair stops future channel convergence but does not roll back already synchronized data: each device keeps what it holds, including what arrived through sync. Neither transition deletes, reverts, or migrates user data. | **Locked** |
| **SYNC-DATA-015** | Protocol/schema version incompatibility and convergence failures are fail-closed: on unknown or incompatible baseline/operation/manifest versions, on unvalidatable baselines, or on unresolvable apply failures, the device pins its cursor, retains pending and local state, surfaces a truthful blocked/repair-required state, and never fabricates convergence or discards user data to proceed. | **Locked** |
| **SYNC-DATA-016** | Convergence evidence and logs carry counts/statuses/digests only — never content, credentials, device-local paths, or raw sizes. This ADR claims no production readiness: capacity, durability beyond the living reference's bounded cases, deployment/upgrade/backup operations, and WAN exposure remain unclaimed here and governed where they live. | **Locked** |
| **SYNC-DATA-017** | Only the invariants above and in `SYNC-DATA-019`–`SYNC-DATA-045` are product decisions. Deferred implementation choices — endpoint path strings, HTTP verb spellings, chunking/streaming, size thresholds, compression, capacity thresholds, retained history physical counts, old-baseline physical reclamation mechanics, exact retention duration, attachment transport, SSE event names, lease/ack endpoints, timeouts, GC mechanics, frame operation names/shared TS types/DB tables/wire frame JSON spelling, and E2EE design — are not decided here and require their own explicitly activated decisions, except where locked: digest scheme/canonical bytes are locked per `SYNC-DATA-032`, parent-scoped order-frame wire logical semantics are locked per `SYNC-DATA-033`–`SYNC-DATA-039`, versioned logical wire inventory v1 semantics are locked per `SYNC-DATA-041`–`SYNC-DATA-044`, publish/fetch operation semantics and status codes are locked invariants under `SYNC-DATA-025`/`SYNC-DATA-028`–`SYNC-DATA-030`; transport spellings/limits and frame wire spelling/storage mapping are deferred under `SYNC-DATA-031`/`SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021`. An implementation choice must never silently narrow or contradict a locked invariant. | **Locked** |
| **SYNC-DATA-018** | This ADR authorizes target semantics only and does not itself authorize implementation. No baseline, bootstrap, reconciliation, retention, inventory, status, or migration work follows from citing it; each requires a separate explicitly activated bounded step. Citing this ADR as proof that convergence is implemented is false. | **Locked** |
| **SYNC-DATA-019** | Baseline origin and relay persistence: logical baselines are generated client-side from per-device Main SQLite authority as device-independent state; the relay durably persists, per channel, the current effective baseline blob plus schema/inventory-version, digest, and watermark metadata. The relay validates the protocol envelope and saves/distributes only — it never merges and never adjudicates business content. Chat authority remains per-device Main SQLite. | **Locked** |
| **SYNC-DATA-020** | Offline bootstrap availability: a joining device may fetch the channel's current effective baseline from the relay even when all prior devices are offline, then replay N+1 onward. Device-held state remains content truth and rebuild source; the relay baseline is an offline-bootstrap availability copy. Online-device temporary supply is not the main path; no presence tracking and no baseline source election are introduced. | **Locked** |
| **SYNC-DATA-021** | Watermark three-segment boundary: (1) publish — the publishing device's baseline content and the relay-continuously-persist-confirmed watermark N must derive from the same provable snapshot boundary; a locally observed cursor alone is not proof; (2) relay publish and retention — the relay atomically publishes baseline blob plus metadata and, while that baseline is effective, retains the continuous log from N+1 onward and never trims its required prefix; (3) receiver apply — the receiver merges the baseline in the same local transaction while preserving pre-pair data and pending outbox intent and persisting cursor=N; failure rolls back as a whole. | **Locked** |
| **SYNC-DATA-022** | Single current effective baseline: each channel has at most one current effective baseline; a new publish atomically replaces it as effective. Prior-version physical reclamation mechanics, endpoint path strings, HTTP verb spellings, chunking/streaming, capacity thresholds, and retained history physical counts remain deferred implementation choices under `SYNC-DATA-017`. Replacement itself is additionally gated by `SYNC-DATA-023` and protected by `SYNC-DATA-024`. | **Locked** |
| **SYNC-DATA-023** | Baseline publish serialization with watermark-monotonic/coverage gating: per channel, baseline publishes serialize so replacement is decided one candidate at a time. The relay accepts a candidate only when (a) its watermark N is relay-continuously-persist-confirmed from the same provable snapshot boundary (a locally observed cursor alone is not proof), (b) monotonic floor: in non-`empty` state the candidate watermark N must satisfy `N >= current effective baseline watermark`; any candidate below current is rejected with `409` and the current effective baseline stays unchanged (the `empty` channel has no current watermark so only (a) plus (c) apply), and (c) the candidate covers the compacted prefix plus the tombstone/delete evidence required for recoverability per `SYNC-DATA-011`. The recoverable floor/coverage gate in (c) is an additional condition and never substitutes for the monotonic floor in (b). A candidate failing any gate is rejected for publish and the current effective baseline stays unchanged. | **Locked** |
| **SYNC-DATA-024** | In-flight bootstrap protection: atomic replacement never removes the N+1 continuous replay/repair path of any successfully issued baseline before its bootstrap completes or explicitly fails. Until completion or explicit failure, the relay retains sufficient baseline/log/delete evidence — or provides an equivalent recoverable mechanism — to complete that bootstrap/repair under `SYNC-DATA-007`/`SYNC-DATA-021`. Only then may retention advance under `SYNC-DATA-011`/`SYNC-DATA-021`. Lease/ack endpoints, timeouts, retained physical old-baseline counts, and GC/reclamation mechanics remain deferred implementation choices under `SYNC-DATA-017` and are not decided here. | **Locked** |
| **SYNC-DATA-025** | Single-stage client-declared publish model for baseline release: the publisher declares watermark N plus digest plus channel binding in one publish operation. No relay-issued fence, token, or prepare handshake exists; the relay never issues a pre-publish grant and the publisher never waits for one. Serialization, watermark-monotonic/coverage gating, and in-flight protection still hold per `SYNC-DATA-023`–`SYNC-DATA-024`/`SYNC-DATA-028`–`SYNC-DATA-029`. | **Locked** |
| **SYNC-DATA-026** | Publisher pre-publish quiescence/barrier/snapshot contract: before publishing, the device reaches local mutation quiescence, drains and confirms every outbox operation entered before the barrier, continuously pulls to an empty page with no unresolved gap, then inside the barrier captures one SQLite snapshot proving logical baseline plus clocks plus tombstones plus channel binding plus cursor N plus outbox=0 plus digest. The barrier releases after publish success or failure. Current code has no barrier/attestation orchestration implementing this; it is a target-only contract. | **Locked** |
| **SYNC-DATA-027** | Publish envelope semantic required field categories with fixed JSON hierarchy & spelling (v1 locked, `F-01`/`F-02`): outer envelope strictly closed lowerCamelCase keys `{ wireVersion, channelId, watermark, digestScheme, digest, payload }` — `wireVersion` is `"sync-baseline-wire-v1"` and appears **only** in the outer envelope (never in manifest/digest/JCS input); `channelId` is the opaque channel binding derived implicitly from the authenticated member secret+membership via `SYNC-CC-*` (§5/`SYNC-CC-021`) and never trusts an arbitrary body `deviceId` (no `deviceId` key in envelope; body `channelId` if present must equal authenticated channel or fail-closed `400`/`403`); `watermark` is `N` (non-negative safe-int); `digestScheme` is `"jcs-sha256-v1"`; `digest` is the lowercase 64-hex `jcs-sha256-v1` over the JCS canonical bytes of **only** the `payload` object (see `SYNC-DATA-032``/`SYNC-DATA-044`, `F-05` digest key clarified); `payload` is the versioned logical payload carrying entities+clocks+tombstones+**order frames** (`SYNC-DATA-033`/`SYNC-DATA-037`/`SYNC-DATA-044`) plus manifest (`SYNC-DATA-044`). Order frames are versioned logical payload and, per `SYNC-DATA-032`/`SYNC-DATA-044`, enter the `jcs-sha256-v1` digest input deterministically. Three content versions `payloadSchema`/`inventoryVersion`/`orderFrameVersion` are echoed **only inside** `payload`/`payload.manifest`, not in the outer envelope (F-01). JSON hierarchy and lowerCamelCase key spelling for the v1 baseline inventory are **locked** per `SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021` + §10A.1 (path/HTTP verb/TS types/DB tables/storage mapping remain deferred, `SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021` updated). | **Locked** |
| **SYNC-DATA-028** | Relay validation boundary for a publish candidate: authentication plus membership; structure/version/digest checks including `SYNC-DATA-032` `jcs-sha256-v1` recompute over the received payload with scheme/version match; N must be a relay-continuously-persisted sequence position of that channel and must not exceed the current relay head, plus in non-`empty` state `N >= current effective baseline watermark` per the `SYNC-DATA-023` monotonic floor (below-current is `409`); serialized decision with the `SYNC-DATA-023` monotonic/coverage gate. A digest recompute match proves only byte-identity with the declaration, never payload semantic truth nor true correspondence to N. The relay never claims to verify payload semantic truth or that the payload truly corresponds to N; a wrong client declaration fails closed and repair stays publisher/client responsibility. | **Locked** |
| **SYNC-DATA-029** | Publish/fetch state machine plus status codes with `{error}` bodies: states are `empty` and `current baseline`; a successful publish atomically replaces `current`; a retry is idempotent success (`200`) only when the candidate matches the current effective baseline on channel binding, N, digest scheme/version, digest, and canonical logical payload; same-N digest/payload divergence, below-current watermark, or other watermark/overwrite conflict is `409`; a retry of a once-successful candidate already replaced by a later baseline is stale `409` with no permanent cross-replacement fingerprint retention required; structure/version/digest invalid is `400`; service-level auth follows existing `401` semantics and device-credential/membership refusal is `403`; oversize is `413`; store failure is `500`. Empty fetch is a legitimate empty state, not a routing error, and returns `404` with `{error:'baseline-not-found'}`. | **Locked** |
| **SYNC-DATA-030** | Fetch/replay handoff with hint-only notification: fetch returns the current envelope only to authenticated channel members; after fetch the client pulls with cursor=N to receive N+1 onward under `SYNC-DATA-007`/`SYNC-DATA-021`. SSE, when mentioned, is hint-only and never carries the blob; whether a new baseline event exists stays deferred. | **Locked** |
| **SYNC-DATA-031** | Minimal wire-contract deferred set plus retention path: operation semantics and status codes are locked per `SYNC-DATA-025`/`SYNC-DATA-028`–`SYNC-DATA-030`, digest scheme/canonical bytes are locked per `SYNC-DATA-032`, while endpoint path strings, HTTP verb spellings, chunking/streaming, size thresholds, compression, retained physical history counts, lease/ack/timeouts/GC, and SSE event names stay deferred. With no compaction implemented, full log retention is the minimal implementation path satisfying in-flight N+1 protection; it is not locked as a long-term never-compact product constraint. | **Locked** |
| **SYNC-DATA-032** | Public baseline digest contract `jcs-sha256-v1` with fixed input scope & outer digest key (v1 locked, `F-02`/`F-05`): digest scheme and version are locked as `jcs-sha256-v1` with no remaining algorithm/canonical-bytes choice. The JCS input is **only** the `payload` object inside the outer envelope (`SYNC-DATA-027`: value of outer key `payload`, i.e. the versioned logical payload) — syncable entities plus clocks plus tombstones plus **order frames** (per `SYNC-DATA-033`/`SYNC-DATA-037`, versioned logical payload) plus the business-integrity versioned manifest — and strictly excludes the outer envelope itself (including outer keys `wireVersion`/`channelId`/`watermark`/`digestScheme`/`digest`), authentication identity, transport metadata, and local diagnostic/provisional capture fields. Outer envelope carries `digestScheme` + outer key `digest` (lowercase 64-hex); the logical payload never carries digest. Order frames deterministically enter the digest input. Array order rules from `SYNC-DATA-044` continue and the payload object's JCS canonical bytes exclude the outer envelope. The logical payload must satisfy an I-JSON-compatible data model with the **fixed lowerCamelCase key spellings** locked per §10A.1; canonical bytes are the UTF-8 bytes of RFC 8785 JSON Canonicalization Scheme (JCS) applied to that payload; the digest is SHA-256 over those bytes rendered as lowercase 64-hex. Non-compliant inputs fail closed (`400`, never hashed around): non-finite numbers, out-of-safe-integer values not schema-encoded as string, illegal Unicode/surrogates, duplicate keys, or non-I-JSON types. The relay recomputes the JCS digest over the received `payload` and rejects mismatch (`400`); a match proves only byte-identity with the declaration, never payload business truth nor true correspondence to N — a wrong publisher declaration fails closed and repair stays publisher/client responsibility per `SYNC-DATA-028`. The relay atomically binds the stored payload with outer `wireVersion`/`digestScheme`/`digest`, channel binding (`channelId` via `SYNC-CC-*` auth), watermark N, and the three content versions inside `payload.manifest`. The current local `local_sync_baseline_candidate` canonicalizer/digest (`local-sync-baseline-v1`) remains internal/non-wire and is not this contract; future implementation requires an explicit wire projection to `jcs-sha256-v1` using the §10A.1 locked spelling and must not upload the local candidate as-is. This closes F-01 (digest input scope) and F-05 (outer `digest` key + input-only-`payload`). | **Locked** |

| **SYNC-DATA-033** | Parent-scoped order frame as the wire merge field for complete sync (target-only, code-less): user-visible `message`/`block` order belongs to the complete-synchronization domain and must not be permanently excluded from sync. Ordering is governed by parent-scoped order frames — one frame per `topic` (`topic → message` order) and one frame per `message` (`message → block` order) — rather than per-entity `sortOrder` as a wire merge field. Each inventory-covered parent carries its frame; parents outside the versioned inventory carry no frame. A frame consists of `parent identity` (`topicId` for message order, `messageId` for block order), `ordered child IDs` (`messageIds` or `blockIds` in user-visible order), `frame clock (timestamp, operationId)` as the total-order key, and `frame version` (per-inventory-version monotonic; receivers reject unknown frame version fail-closed per `SYNC-DATA-039`). Frame version is distinct from baseline schema/inventory version and from entity/field clocks; no per-row `sortOrder` field participates in wire merge per `SYNC-DATA-038`. | **Locked** |
| **SYNC-DATA-034** | Frame authority boundary (target-only, code-less): a frame decides only the relative order among currently alive members of the same parent; it never creates, deletes, reparents, or resets parent identity, and never overwrites entity content/fields. Membership and deletion remain governed by entity existence plus `op`/tombstone semantics (`SYNC-DATA-005`/`SYNC-DATA-006` plus §6). A frame that would imply a different parent, resurrect a tombstoned member, or invent an entity is out-of-authority and its out-of-authority members are not materialized (see `SYNC-DATA-035`). | **Locked** |
| **SYNC-DATA-035** | Concurrent frame merge with validity filtering, membership-clock-gated completeness, and deterministic retention — normative algorithm (target-only, code-less; closes F-02): concurrent frames for the same parent resolve by the existing `timestamp+operationId` total-order LWW (the same total order governing field clocks; no new ordering key introduced). Winner determination uses that total order only; payload order or receipt order does not decide. The winning frame's `ordered child IDs` are then filtered to the set of currently alive, same-parent, known children: duplicates are de-duplicated by first appearance (first occurrence retains semantic position; later duplicates ignored), deleted/tombstoned children, children whose parent identity differs per current membership, and unknown child IDs are not materialized and do not resurrect. Logical `parent-membership clock` governs coverage (normative): each child carries a logical `parent-membership clock (timestamp, operationId)` sharing the same total order as existing clocks; it is established at child creation and, only if a future explicitly governed `reparent` operation is accepted, updated by that reparent; ordinary content/field edits never change it. The winning frame's `frame clock` is the coverage cutoff. If any effective alive same-parent child has `membershipClock <= frameClock` yet is absent from the winner's ordered list, the frame is **incomplete** — receiver must reject/repair fail-closed per `SYNC-DATA-015`/`SYNC-DATA-039`, not silently append. Children with `membershipClock > frameClock` (concurrent or later arrivals relative to the winner's snapshot) that are alive and same-parent must be retained and appended deterministically by `membershipClock` ascending then `child ID` lexicographically. Any required membership clock missing or not comparable (not totally ordered with the frame clock) causes fail-closed (`400` on publish where applicable, otherwise blocked/repair-required per `SYNC-DATA-015`). Silent member loss to preserve winner-frame exactness is prohibited. `parent-membership clock` is a new logical contract state not yet implemented; it does not lock table/column/op/JSON spelling per `SYNC-DATA-040`, and current entity clocks must not substitute for it (see living doc). | **Locked** |
| **SYNC-DATA-036** | Atomic frame apply with persistent winning frame, fixed-point re-evaluation, deterministic dense materialization, baseline/incremental equivalence, and atomic transaction boundary (target-only, code-less; closes F-03/F-04): a receiver applies a parent's order frame atomically with the `SYNC-DATA-035` validity filtering, membership-clock-gated completeness check, and deterministic append (by `membershipClock` then `childId`), then materializes the resulting logical order into local dense `sortOrder 0..n-1` following the filtered winner plus deterministic append sequence. The receiver persistently stores per-parent winning-frame state. Whenever the winning frame for a parent changes, or any relevant child `existence` / `parent-membership clock or parent identity` / `tombstone` state changes, the receiver re-runs the same `SYNC-DATA-035` filtering / completeness / append / materialization algorithm until a deterministic fixed point is reached, guaranteeing arrival-order independence. Baseline application and incremental operation replay that deliver the same set of entity states plus the same winning frames reach the same logical order; dense `sortOrder` values are local projection/query authority only and carry no additional merge meaning beyond the frame-derived sequence. The SQLite governance in [sqlite-migration.md](./sqlite-migration.md) is not rewritten: local `sortOrder` remains authoritative for local queries/projection while wire truth is the frame per `SYNC-DATA-038`. **Transaction boundary (normative):** one visible commit must atomically cover all affected state for its scope: entity/tombstone/`parent-membership` merge, per-parent winning-frame state, the complete dense `sortOrder 0..n-1` for every affected parent (source and destination parents for a `reparent`; single parent for create/delete/frame-only), and the corresponding replay `applied`/`cursor` state. `reparent` is atomic across both sides. If the commit cannot complete all of the above, the change stays pending/invisible with a full rollback — no partial projection is committed. | **Locked** |
| **SYNC-DATA-037** | Baseline logical-payload order-frame coverage and structural-operation issuance rule (target-only, code-less): the baseline logical payload (`SYNC-DATA-004`/`SYNC-DATA-027`) must carry order frames for every parent within the versioned inventory. Incremental delivery of order is via frames (or an explicitly governed equivalent atomic group that yields the same frame semantics). Any future structural operation that actually changes user-visible `message`/`block` order or membership — including `reorder`, `insert-at`, `branch`/`clone`/`paste` or equivalent that reorders visible children or changes membership — must issue the corresponding order frame(s) (or the governed equivalent atomic group) before a device may claim complete convergence for its domain. A `select-answer` variant that only flips wire-excluded local overflow without changing order or membership (such as the current `foldSelected`-only `selectAnswerMessage`) changes no converged state and therefore issues no frame. The current implementation has frame issuance only for the `SYNC-DATA-048` covered paths including stable `pasteMessagesToTopic` (as well as stable `insertMessagesAfterAnchor` and the other listed ordinary paths); no complete-convergence claim extends over the remaining reordered state (`branch`/`clone`/`reset`/`resend` and order-changing `select-answer` variants plus excluded/transient transitions) until the frame path exists. | **Locked** |
| **SYNC-DATA-038** | Wire inventory exclusion of per-row `sortOrder` with local projection authority preserved (target-only, code-less): per-row `message.sortOrder` and `message_block.sortOrder` are excluded from the wire logical payload as entity fields and from the wire field-clock inventory; they are not transmitted as row properties and carry no per-field clock on the wire. Local SQLite `sortOrder` (dense `0..n-1` per parent) remains the local projection/query authority and is materialized from frames per `SYNC-DATA-036`; this decision does not rewrite [sqlite-migration.md](./sqlite-migration.md) storage or migration governance, only the wire sync semantics. Until frames are implemented, local `sortOrder` stays local-only and unsynced; no partial wire `sortOrder` merge is permitted. | **Locked** |
| **SYNC-DATA-039** | Per-version strictly closed schema/inventory (target-only, code-less): every versioned wire artifact — baseline logical payload, order frames, entity/field inventories, and envelope categories in `SYNC-DATA-027` — is strictly closed per version: it has a fixed key set, fixed `required` vs `nullable` rules, and fixed allowed-value constraints. Unknown fields, unknown frame or schema/inventory versions, or unknown envelope categories cause fail-closed rejection per `SYNC-DATA-015` (`400` on publish where applicable, otherwise blocked/repair-required). Silently ignoring unknown fields or versions for forward compatibility is prohibited; forward compatibility requires a new explicitly versioned inventory/schema revision. | **Locked** |
| **SYNC-DATA-040** | Locked vs deferred scope for order-frame / JSON spelling / storage mapping (target-only, code-less; `F-02`/`F-05` updated): this ADR locks wire logical semantics in `SYNC-DATA-033`–`SYNC-DATA-039` and the normative append/fixed-point/transaction boundary in `SYNC-DATA-035`–`SYNC-DATA-036`, **and for inventory v1 (`topic-message-stable-block-order-v1`) locks the byte-level JSON hierarchy & lowerCamelCase key spelling** for outer envelope (`wireVersion`, `channelId`, `watermark`, `digestScheme`, `digest`, `payload`), logical payload root (`payloadSchema`, `inventoryVersion`, `orderFrameVersion`, `scope`, `topics`, `messages`, `messageBlocks`, `tombstones`, `orderFrames`, `manifest`), manifest (`payloadSchema`, `inventoryVersion`, `orderFrameVersion`, `scope`, `liveCounts`, `tombstoneCounts`, `frameCounts`, `completeness`), entity/field/clock/tombstone/order-frame object keys (§10A.1), all strictly closed per `SYNC-DATA-039`/`SYNC-DATA-045`. Order-frame JSON keys are therefore locked in v1. It does **not** lock: future operation names, endpoint paths, HTTP verb spellings, `packages/shared` TypeScript types for non-wire layers, SQLite tables/columns for non-wire storage projections, or transport encoding; `parent-membership clock` storage mapping to SQLite columns remains implementation projection (wire JSON key `parentMembershipClock` is locked, column mapping deferred). No code, migration, or endpoint is authorized by this ADR. Any mapping to ops/types/tables/payload spelling for future inventories requires a future explicitly activated step conforming to the locked v1 spelling above. | **Locked** |

| **SYNC-DATA-041** | Version roles and naming for the first versioned logical wire inventory (wire v1, target-only, code-less; `F-01` corrected): the envelope wire protocol is `sync-baseline-wire-v1`; the logical-payload schema is `chat-core-baseline-v1`; the entity/field inventory is `topic-message-stable-block-order-v1`; the order-frame family is `parent-order-frame-v1`. Roles are distinct: `sync-baseline-wire-v1` identifies publish/fetch wire behavior plus envelope categories (§5/`SYNC-DATA-027`) and remains paired with the outer `jcs-sha256-v1` digest binding (`SYNC-DATA-032`); `chat-core-baseline-v1` identifies the versioned logical-payload structure (entity kinds, logical field sets per §10A, clock shapes, tombstone shape, order-frame logical shape, and manifest shape); `topic-message-stable-block-order-v1` identifies the exact allowlisted field inventory and excluded-status/type gate (§10A); `parent-order-frame-v1` identifies the frame logical composition (`SYNC-DATA-033`/`SYNC-DATA-041`–`SYNC-DATA-044`). **`wireVersion` travels only in the outer envelope (`SYNC-DATA-027` outer key `wireVersion`); the three content versions `payloadSchema`/`inventoryVersion`/`orderFrameVersion` travel only inside `payload` and are echoed inside `payload.manifest` (`SYNC-DATA-044`); any mismatch, unknown value, or missing value for the claimed `sync-baseline-wire-v1` or for the manifested triple fails closed per `SYNC-DATA-039`/`SYNC-DATA-015` (`400` on publish where applicable, otherwise blocked/repair-required). Prior phrasing implying all four echoed in both envelope and manifest is deleted as contradictory (F-01). Names are inventory-scoped and must not imply complete-product sync; the inventory payload is a subset (§10A/§11) and `SYNC-DATA-010`/`SYNC-DATA-041`–`SYNC-DATA-045` state explicitly that this wire v1 is not complete-product sync. A future excluded domain entering the wire requires a new inventory/schema/frame trio, not silent extension. Three-document naming is consistent via `SYNC-CC-021` propagation with the same envelope/manifest split. | **Locked** |

| **SYNC-DATA-042** | Wire inventory `topic-message-stable-block-order-v1` — included entities and fixed full-state logical field inventory with locked JSON spelling & type/value domain (wire v1, target-only, code-less; `F-02`/`F-03`): the `payload` object carries **only** (a) live `topic`, live `message`, and live **stable supported** `messageBlock` entities in full-state (see `payload.topics`/`payload.messages`/`payload.messageBlocks`), (b) one `entityClock` per live entity (`payload.*[].entityClock`), (c) per-field `fieldClocks` for every mutable inventory field (`payload.*[].fieldClocks` with fixed per-field keys, exactly one per mutable field, no extra), (d) one `parentMembershipClock` per `message` and per `messageBlock` (`payload.messages[].parentMembershipClock`, `payload.messageBlocks[].parentMembershipClock`), (e) explicit `tombstones` (`payload.tombstones`), (f) `topicMessage` and `messageBlock` order frames at `parent-order-frame-v1` (`payload.orderFrames`), and (g) the versioned manifest (`payload.manifest` per `SYNC-DATA-044`). Fixed full-state logical fields with **locked lowerCamelCase JSON key spelling** (§10A.1): `topic` — `id` + `name`/`assistantId`/`createdAt`/`updatedAt`/`deletedAt`/`pinned`/`prompt`/`isNameManuallyEdited`; `message` — `id`+`topicId` + `role`/`content`/`status`/`askId`/`model`/`modelId`/`assistantId`/`createdAt`/`updatedAt`; `messageBlock` — `id`+`messageId` + `type`/`content`/`status`/`createdAt`/`updatedAt`. **Type/value domain (F-03)**: `id`/`topicId`/`messageId` are non-empty valid Unicode scalar strings (only Unicode scalar values, no unpaired surrogates, no empty); `name`/`assistantId`/`createdAt`/`updatedAt`/`deletedAt`/`role`/`content`/`status`/`askId`/`model`/`modelId`/`type` are `valid Unicode scalar string|null` (when not null, only Unicode scalar values); `pinned`/`isNameManuallyEdited` are `boolean|null`; time fields `createdAt`/`updatedAt`/`deletedAt` are I-JSON `string|null` preserved verbatim — **never parsed as dates, never normalized, never used as LWW clocks** (LWW uses `entityClock`/`fieldClocks`/`parentMembershipClock`/`deletionClock`/`frameClock` only); `status`/`type` keep the existing gate (transient `streaming`/`pending`/`processing`/`searching` and `tool`/`file`/`image`/`video`/`citation`+overflow structured excluded, see `SYNC-DATA-043`); every mutable nullable field is a **fixed key** whose value is either gate-compliant value or `null`; `undefined` never appears, extra keys beyond the fixed set never appear, per-row `sortOrder` is strictly not a wire key and never carries a field clock; all keys have fixed existence. Identity/parent IDs never `null`; any `null`/`undefined`/empty identity, `undefined` value, extra key, wrong type (e.g. number for string field, string for boolean), unpaired surrogate, or illegal Unicode fails closed per `SYNC-DATA-039`. | **Locked** |

| **SYNC-DATA-043** | Status/type gate, clock shapes, and tombstone semantics for `topic-message-stable-block-order-v1` (wire v1, target-only, code-less; `F-03`/`F-05`): **Stable gate** — `message`/`messageBlock` transient statuses `streaming`/`pending`/`processing`/`searching` (case-sensitive) are never wire entities; such rows are excluded at capture and fail closed if presented on the wire. `messageBlock` types `tool`/`file`/`image`/`video`/`citation` (case-insensitive match after trimming; canonical lowercased value checked) and any block whose canonical content depends on structured overflow/attachment bytes (overflow presence of `content`/`file`/`fileId`/`file_id`/`response`/`knowledge`/`memories`/`toolId`/`arguments`/`url`/`l2AttachmentUnavailable` or equivalent structured attachment predicate per `isUnsupportedBlockForSync`) are never wire entities; such rows are excluded and fail closed if presented. Other legacy stable strings (e.g. `success`/`error`/`paused`/`sent` and any stable `status`/`type` value that passes the allowlist and is not in the excluded four/named type sets) remain wire-eligible; the gate does not over-enumerate an allowlist beyond the four transient statuses and the five excluded block types plus the overflow-structured predicate. **Clocks** — `timestamp` is a non-negative integer within JSON safe integer `[0, 9007199254740991]`; `operationId` is a non-empty string with `length 1..256`, containing no `:` (colon), and valid Unicode scalar string (no unpaired surrogates, no illegal code points). Entity clock (`entityClock: {timestamp, operationId}`) is required non-null for every live entity; field clock (`fieldClocks.<field>: {timestamp, operationId}`) is required exactly one per mutable inventory field per live entity (topic 8, message 9, block 5) and no extra field clock for `id`/`topicId`/`messageId`/`sortOrder` or absent fields; missing or duplicate field clock fails closed. `parentMembershipClock: {timestamp, operationId}` is required non-null for every live `message` and `messageBlock`; it shares the `(timestamp,operationId)` total order and is established at creation, never changed by ordinary content/field edits, and only updated by an explicitly governed `reparent` if a future decision authorizes it — `reparent` remains **not authorized / not supported** in this wire v1 and any wire payload implying it fails closed unless a later ADR explicitly governs it. Any missing parent-membership clock, duplicate, nan/infinite timestamp, out-of-safe-integer, illegal Unicode, or non-comparable clock fails closed. **Tombstone** — fixed JSON keys `{entityType, entityId, deletionClock, survivingEntityClock}` where `entityType in {topic,message,messageBlock}` (lowerCamelCase wire value `messageBlock`), `entityId` non-empty valid Unicode scalar string, `deletionClock: {timestamp, operationId}` where `operationId` is `string|null` (`null` only for legacy, see below), `survivingEntityClock: {timestamp, operationId}|null`. New deletions generated under this wire v1 **must** carry a fresh non-null `operationId` satisfying the bound above and must not forge `null`. Legacy deletions may carry `operationId: null`; they are explicitly `null`, never forged, and constitute a **strong deletion barrier**: at the same `timestamp` `(T,null)` is strictly **greater than** any `(T,nonNull)` and suppresses all equal-timestamp live operations; two `(T,null)` are equivalent; ordering otherwise is `(timestamp asc, null > any nonNull at equal timestamp)` with `null` cardinality only for this legacy compatibility total order and never used for new clocks. A tombstone covering a live entity resolves per `SYNC-DATA-006` plus §6 (deletion wins, late descendants contained); tombstone with unknown type/id shape, missing deletionClock, or extra keys fails closed. This `null→strong-barrier` mapping replaces the prior minimal tie-break and is normative for this wire v1; it is a legacy compatibility order not used for new clocks. | **Locked** |

| **SYNC-DATA-044** | Order-frame inventory rule, manifest fixed digest input with locked JSON spelling, and JCS array determinism for `topic-message-stable-block-order-v1` / `parent-order-frame-v1` (wire v1, target-only, code-less; `F-02`/`F-04`/`F-05`): **Order frames (locked lowerCamelCase keys: `frameVersion`, `kind`, `parentId`, `orderedChildIds`, `frameClock: {timestamp, operationId}` where `kind in {topicMessage, messageBlock}`, `frameVersion == "parent-order-frame-v1"`)** — per `SYNC-DATA-033`–`SYNC-DATA-037`, every inventory-within parent has exactly one winning frame; invariants are strictly closed. Each frame lives in `payload.orderFrames`. **Parent/frame full set (F-04)**: `payload` must contain exactly one `orderFrame` for every live included `topic` (`kind: topicMessage`, `parentId` = `topic.id`) and exactly one for every live included `message` (`kind: messageBlock`, `parentId` = `message.id`), including an **empty frame** (`orderedChildIds: []`) when that parent has zero inventory-included live children; a tombstoned parent or an inventory-excluded parent (transient/excluded-type block parent not in inventory) **must not** have a frame, and its frame if present fails closed. `orderedChildIds` must be exactly the inventory-included live children of that parent that passed `SYNC-DATA-035` coverage/append — i.e. the filtered winning `orderedChildIds` plus the deterministic `membershipClock > frameClock` append (by `membershipClock` asc then `childId` lexicographically); it is fail-closed if `orderedChildIds` omits a child that should be present or includes an extra/deleted/different-parent/unknown child. If a baseline would have a transient excluded entity causing an incomplete inventory-within children set, it **must not claim `completeness: "complete"` and must not be published** as `sync-baseline-wire-v1`; permanently excluded structured blocks (`tool`/`file`/`image`/`video`/`citation`+overflow) are **not members** of that inventory child set and are excluded from the frame's universe. A payload with zero or multiple frames for the same parent, unknown `frameVersion`/`kind`, extra frame keys, illegal Unicode in IDs, or `frameClock` outside clock bounds fails closed. `parentMembershipClocks` participate in completeness per `SYNC-DATA-035` (`membershipClock <= frameClock` absent is incomplete → `400`/blocked/repair-required; `membershipClock > frameClock` is deterministic `membershipClock` then `childId` append; missing/incomparable fail-closed; duplicate child ID — first occurrence retained, later ignored — but payload containing an explicit duplicate literal fails the strictly closed duplicate check → fail-closed before filtering). **Manifest (enters `jcs-sha256-v1` via `payload` per `SYNC-DATA-032`; locked lowerCamelCase keys `manifest: {payloadSchema, inventoryVersion, orderFrameVersion, scope, liveCounts: {topic, message, messageBlock}, tombstoneCounts: {topic, message, messageBlock}, frameCounts: {topicMessage, messageBlock}, completeness}`)** — fixed literal `scope: "chat-core-baseline-v1:topic-message-stable-block-order-v1"`; `completeness: "complete"` only for `sync-baseline-wire-v1`. The manifest is the only business-integrity summary entering the digest; it **excludes** `pendingOutboxCount`, `observedLocalChannelKey`/`observedLocalCursor`/`observationBinding`, `unversioned`/`excluded`/`orphan`/`aggregate` diagnostics, `reasons`/`completenessReasons` arrays, any free-form diagnostic string, and the digest itself. The digest additionally excludes outer `channelId`, `watermark`, `digestScheme`, `digest`, `wireVersion`, authentication identity, and transport metadata — those live only in the outer envelope (`SYNC-DATA-027`) and are not part of `jcs-sha256-v1` input. A manifest with unknown key, missing required count, or `completeness != "complete"` for this wire v1 fails closed for wire purposes. **Manifest recompute & validation (F-05)**: on relay publish validation and on client apply validation, `liveCounts`/`tombstoneCounts`/`frameCounts`/`completeness` **must be uniquely recomputed** from the validated `payload` (`topics`/`messages`/`messageBlocks`, `tombstones`, `orderFrames`) plus parent closure, membership clocks, and frame coverage (`SYNC-DATA-035`/`SYNC-DATA-044` full-set rule); declared values that do not match the recompute are `400` (publish) / fail-closed (apply) **even if the `jcs-sha256-v1` digest self-consistently covers the declared manifest** — self-consistency is never acceptance. **JCS array determinism** — RFC 8785 JCS provides object-key ordering; wire ID ordering remains UTF-8 byte lexicographic after Unicode scalar validation. Arrays in `payload` are ordered: (a) `topics` by `id` UTF-8 byte lexicographic, `messages` by `id`, `messageBlocks` by `id` (each array internally sorted); cross-type entity rank is implicit via separate arrays; (b) per-entity `fieldClocks` object keys are JCS-sorted field names (keys are lowerCamelCase field names: topic `name`/`assistantId`/`createdAt`/`updatedAt`/`deletedAt`/`pinned`/`prompt`/`isNameManuallyEdited`; message `role`/`content`/`status`/`askId`/`model`/`modelId`/`assistantId`/`createdAt`/`updatedAt`; block `type`/`content`/`status`/`createdAt`/`updatedAt`); legacy `tombstones` array by fixed type rank `topic < message < messageBlock` then `entityId` UTF-8 byte lexicographic; (c) `orderFrames` by `kind` rank `topicMessage < messageBlock` then `parentId` UTF-8 byte lexicographic; (d) `orderedChildIds` within each frame **preserve business order** (not re-sorted). Any duplicate `entity (type,id)` within its array, duplicate tombstone `(type,id)`, duplicate frame `(kind,parentId)`, duplicate field clock key, duplicate `childId` within a frame literal, unvalidated Unicode, or duplicate canonical object key fails closed. Readers validate wire byte ordering matches the locked order before applying; mismatched order fails closed (re-canonicalize-then-compare not permitted). | **Locked** |

| **SYNC-DATA-045** | Per-version strictly closed, excluded domains, and updated non-locking projection scope for wire v1 (wire v1, target-only, code-less; `F-02`/`F-03`/`F-05` updated): **Strictly closed per version** — every wire artifact at `sync-baseline-wire-v1`/`chat-core-baseline-v1`/`topic-message-stable-block-order-v1`/`parent-order-frame-v1` (`payload` object inside outer envelope per `SYNC-DATA-027`/`SYNC-DATA-032`, tombstones, clocks, order frames, envelope categories, manifest) is strictly closed: fixed lowerCamelCase key sets (§10A.1), fixed `required` vs `nullable` per `SYNC-DATA-042`–`SYNC-DATA-044` with the F-03 type/value domain, and fixed allowed-value constraints. Unknown fields, unknown `payloadSchema`/`inventoryVersion`/`orderFrameVersion`/`wireVersion`/`kind`/`entityType`, or unknown envelope category cause fail-closed per `SYNC-DATA-015`/`SYNC-DATA-039` (`400` on publish where applicable, otherwise blocked/repair-required). Silently ignoring unknown fields for forward compatibility is prohibited; any previously excluded domain entering the wire requires a new explicitly versioned `inventoryVersion`/`payloadSchema`/`orderFrameVersion`/`sync-baseline-wire` revision. **Explicit excluded product domains (never wire)** — `segments` / segment memberships / segment membership order, `branches` / tree operations (`branch`/`clone`/`paste`/`select-answer` and tree structure), `attachments` / file bytes / file references (`file`/`file_path`/`fileId`), `tool`/`citation`/rich structured block data that depends on overflow/attachment structured content, assistant settings / provider credentials / `contextWindowAnchor`, and any other row/table not enumerated in `SYNC-DATA-042`. **Explicit excluded local domains (never wire, never digest)** — local filesystem paths, FTS/cache/derived indexes, UI/transient state, outbox (`pendingOutboxCount`/payloads)/`applied`/`cursor` logs / per-channel cursor history, conflict logs, local diagnostics / `observedLocalChannelKey`/`observedLocalCursor`/`observationBinding` / `unversioned`/`excluded`/`orphan`/`aggregate` counters and reason arrays / `completenessReasons`. The current wire v1 carries only `completeness: "complete"` (per `SYNC-DATA-044`); `partial`/`unbound` remain internal/non-wire provisional states (see `local_sync_baseline_candidate`). This wire v1 is **not complete-product sync** — it is a bounded subset for chat-core history (topics + stable messages + stable supported blocks plus ordering); product domains above stay outside the claim and require their own governed decisions before any complete-product claim. **Updated non-locking scope (`SYNC-DATA-040`/`SYNC-CC-021` F-02/F-05, not the old `未锁 JSON spelling` blanket)**: for **inventory v1 baseline (`topic-message-stable-block-order-v1` at `sync-baseline-wire-v1`/`chat-core-baseline-v1`/`parent-order-frame-v1`) JSON hierarchy & lowerCamelCase key spelling is now **locked** (outer envelope keys, `payload` root keys, manifest keys, entity/tombstone/order-frame/clock keys per §10A.1; order-frame JSON keys are locked in v1); remaining **not locked** are: future operation names/endpoints, endpoint path strings / HTTP verb spellings, `packages/shared` TypeScript types beyond the wire DTO, SQLite tables/columns mapping beyond the wire projection, and storage/retention/GC mechanics. `parentMembershipClock` wire JSON key spelling is locked (`parentMembershipClock` + `timestamp`/`operationId`), only its column mapping deferred. **Current local projection is not wire-compliant** — `local_sync_baseline_candidate` (`local-sync-baseline-v1` / `topic-message-stable-block-v1`) remains internal/non-wire: it (a) still carries per-row `sortOrder` as an entity field, (b) carries diagnostic manifest keys (`pendingOutboxCount`, `observationBinding`, `unversioned`/`excluded`/`orphan`/`aggregate` counters, `reasons`), (c) carries provisional `observedLocalChannelKey`/`observedLocalCursor`, (d) uses a provisional `local-sync-baseline-v1` canonicalizer, (e) carries no `orderFrames`/`parentMembershipClocks` under the `jcs-sha256-v1` wire projection, and (f) uses pre-lock key spellings that do not match §10A.1 — it must be projected through an explicit `jcs-sha256-v1` wire validator/projection (with F-04 full-set + F-05 recompute + F-03 type/domain) before any relay publish and must never be uploaded as `sync-baseline-wire-v1`. No code, migration, column, type, or endpoint is authorized by `SYNC-DATA-041`–`SYNC-DATA-045`; a validated wire projection or barrier implementation requires its own explicitly activated, separately verified step that conforms to the locked inventory and locked spelling. | **Locked** |

| **SYNC-DATA-046** | Relay baseline resource transport binding for `sync-baseline-wire-v1` (reference-relay implemented 2026-09-11; narrows the `SYNC-DATA-031` deferred set for this resource only): publish is `PUT /sync/baseline`, fetch is `GET /sync/baseline`; request and success response bodies are directly the locked `SyncEnvelope` JSON (outer `{wireVersion, channelId, watermark, digestScheme, digest, payload}` per `SYNC-DATA-027`, `payload`-only `jcs-sha256-v1` outer `digest` per `SYNC-DATA-032`); each channel holds a single current row persisted by the relay (`sync_channel_baselines`, additive `cc-1` → `cc-2`), decided and written in one serialized per-channel transaction with restart durability. State machine and status codes follow `SYNC-DATA-023`/`SYNC-DATA-028`/`SYNC-DATA-029` unchanged: `empty` accepts the first relay-confirmed N (`N <= head`); non-`empty` requires `N >= current` (`409` below), full-match idempotent `200`, same-N divergence `409`, above-current replace under the coverage gate (which under full retention with no compaction is `N <= head`); `N` above head is `400`; envelope/digest invalid is `400`; auth is `401`/`403` per existing semantics with `403` on channel mismatch and on non-membership; `empty` fetch is `404 {error:'baseline-not-found'}`; store failure is `500`; all errors are exact `{error:string}` with locked spellings `invalid-envelope` / `digest-mismatch` / `watermark-above-head` / `baseline-conflict` / `channel-mismatch` / `pairing-required` / `baseline-not-found` / `unauthorized` / `store-unavailable`. No size threshold / `413` on this resource (still deferred). Publisher barrier (`SYNC-DATA-026`), receiver bootstrap/apply, remote frame ops (except the `topicMessage` + ordinary-`messageBlock` incremental `order_frame` dual pair now implemented under `SYNC-DATA-048`), compression, lease/ack, and GC stay deferred and unchanged; every other Locked row is unchanged. | **Locked** |


| **SYNC-DATA-047** | Receiver bootstrap merge transaction plus failure semantics (implemented 2026-09-11; endpoint/path/verb/body/status spelling stays locked by `SYNC-DATA-046`/`SYNC-CC-022` and is not relocked here): on `200` the receiver strictly validates the envelope with the shared `baselineWire` validator (exact keys/versions/closure/manifest-recompute/duplicate-key rejection on raw text) plus payload-only `jcs-sha256-v1` recompute (match proves byte-identity only), channel binding (envelope `channelId` must equal the persisted local channel), and watermark N (safe non-negative int); manifest/clock/frame violations fail closed. Merge reuses the single validated merge core (no copied LWW rules) mapping wire `messageBlock` to `message_block`, `parentMembershipClock` plus parentId, and `parent-order-frame-v1` frames; per-row `sortOrder` never goes on wire and is materialized locally as dense `0..n-1` only. One SQLite transaction atomically covers merge plus `sync_state.cursor=N` with pre-commit asserts (persisted cursor still 0, channel still matches envelope; channel/config change fails closed); `sync_outbox`/`sync_applied`/pre-pair rows are preserved untouched (outbox never cleared, empty never required). Pre-commit failure changes nothing (cursor stays 0); `N==0` is legal (commit stays 0, later cycles may repeat GET with idempotent apply, then pull from 0 gets seq1 — efficiency tradeoff, no bootstrapped flag); post-commit `N+1` pull failure retains N for next-cycle recovery. Sender barrier (`SYNC-DATA-026`) stays deferred and out of scope. Every other Locked row is unchanged. | **Locked** |

| **SYNC-DATA-048** | Incremental `topicMessage` + `messageBlock` parent-order-frame operation (implemented 2026-09-11; `messageBlock` second-pair extension implemented 2026-09-12 under user authorization with no new lock number — same `op:'order_frame'`, same strict five-key `parent-order-frame-v1` payload, same envelope id/time mirror with deviceId envelope-only; narrows the `SYNC-DATA-017`/`SYNC-DATA-031`/`SYNC-DATA-040` deferred set for this op only): reuses the existing `SyncOperation` endpoint/envelope with one precise `op: 'order_frame'`. This unit allows exactly two legal pairs — `entityType:'topic'`+`kind:'topicMessage'` and `entityType:'message'`+`kind:'messageBlock'` — each with `entityId === payload.parentId`; any cross combination fails closed. The payload is strictly closed and reuses the locked `parent-order-frame-v1` object exactly: `{frameVersion: 'parent-order-frame-v1', kind in {'topicMessage','messageBlock'}, parentId, orderedChildIds: string[], frameClock: {timestamp, operationId}}` with the exact two legal pairs stated above (`topic`+`topicMessage`, `message`+`messageBlock`); any cross combination fails closed. Envelope `id === frameClock.operationId` and `timestamp === frameClock.timestamp` (single clock — never a separately allocated second clock); `deviceId` stays envelope-only and never enters the payload. Unknown fields, unknown version/kind, duplicate children, and any mirror mismatch fail closed; the dedicated `order_frame` branch never enters the upsert field allowlist and delete ops never carry a frame payload. No new endpoint, no relay schema migration, no baseline `wireVersion` change; an old client meeting the new op fails closed on its existing validator (accepted forward contract). Issuance covers ordinary `topicMessage` paths with atomic membership/frame semantics — empty-topic establish, stable `appendMessage`, `deleteMessage`, `deleteMessages`, `deleteMessagesWithSegments` (with complete stored membership via the existing try-helper fail-safe), `reorderMessages` (with complete stored membership), stable batch `insertMessagesAfterAnchor` and stable `pasteMessagesToTopic` (true-new full-state upserts plus membership with one topicMessage plus per-parent messageBlock try-refresh; pure stable→stable patches advance nothing; paste missing-topic true creation emits the existing topic upsert with parent-first timestamps), and transient→stable promotion inclusion (`updateMessage`, `appendMessage` promotion, `updateMessageAndBlocks`) — plus ordinary `messageBlock` paths with a stable parent message and stable-supported blocks: stable block create/inclusion in `appendMessage`, true-new plus transient/unsupported→stable-supported promotion inclusion in `updateBlocks`/`updateSingleBlock`/`updateMessageAndBlocks`, stable-supported creates in `bulkAddBlocks`, ordinary deletes in `deleteBlocks` and in `updateMessageAndBlocks` block deletes, and transient→stable parent promotion issuing the complete `messageBlock` frame only when block membership is complete (promotion rescan supplements entity upsert/field evidence only, never membership — pre-existing/closure/promotion rows stay unversioned per 009/SYNC-DATA-035 with no backfill/guess; entity/message op precedes its frame; missing membership try-invalidates with 0 op while the user mutation succeeds and the candidate stays truthful partial; malformed/MAX_SAFE still rolls back): with complete stored membership, mint/persist the winning frame plus enqueue exactly one matching `order_frame` reusing that clock in the same SQLite transaction, with the stable entity op / membership preceding its frame so the receiver can verify coverage; with missing membership keep truthful invalidate/partial with 0 op while the user mutation still succeeds per existing `tryRefreshOrInvalidate` semantics. Errata (narrow, no other row changed): stable→transient is NOT a cross-device structure operation — transient status never rides the wire, so the frame has no member-exclusion authority there; all stable→transient paths (`updateMessage`, `updateMessageAndBlocks`, `appendMessage` overwrite) invalidate the local `topicMessage` frame in the same transaction with 0 frame op while the user mutation succeeds and the candidate stays truthful partial. Pure content edits (stable→stable, transient→transient) and brand-new transient appends mint no frame op. Remote apply never lets a frame exclude a member the receiver still holds alive/stable (fail-closed coverage gate); `SyncOrphanError` stays reserved for genuinely resolvable arrival gaps (unknown parent, listed member not yet arrived). Every issuance commits chat/order, membership (where applicable), the winning frame, and the outbox op atomically. Exclusion transitions (stable→transient/supported→unsupported) never mint: transient/unsupported never rides the wire and frames carry no exclusion authority — invalidate locally with 0 frame op, candidate truthful partial. Pure content edits (included→included) mint nothing. There is no block reorder API and none is introduced. Remaining compound/unsupported paths (`branch`/`clone`/`reset`/`resend` and any order-changing `select-answer` variants) keep existing invalidate/partial with zero frame ops and no membership extension, while the current `selectAnswerMessage` (`foldSelected` only, wire-excluded, no order/membership change) preserves frame/high-water and emits zero ops. Relay push persists and pull replays the op byte-identically with no schema change under the shared strict validator (legal accepted; illegal `400`; divergent id collision `409`; identical replay idempotent with no cursor growth); the `SyncClient` pull validator and the local enqueue validator enforce the same rule. Remote apply is handled before generic upsert/delete in one transaction: idempotent `sync_applied` (including equal-clock same-semantics idempotence with conflict-tolerant applied insert; equal-clock divergence fails closed with rollback and no cursor advance), live-topic parent gate (unknown parent without tombstone is a retryable orphan; tombstoned parent is consumed without materializing), member existence / non-tombstone / same-parent gating, `membershipClock` gating with `SYNC-DATA-035` suffix/fixed-point semantics (incomplete `<= frameClock` coverage fails closed), frame LWW winner selection with deterministic dense `sortOrder 0..n-1` materialization, and out-of-order arrival via the existing in-memory orphan buffer (no poison-ack, no new pending protocol). Apply never creates/deletes/reparents entities, never resurrects tombstones, and never overwrites content fields. An older losing frame is consumed without changing materialized order. Local implementation invariant (no wire change, no new lock): per-`(kind, parentId)` frame timestamp high-water (migration `012_sync_frame_high_water`, backfilled from current winners) — allocation takes max(high-water, existing frame, included membership clocks)+1 and advances the mark in the same transaction (any later failure rolls it back); invalidation deletes the winning row but never lowers the mark; every accepted winning-frame persist (local issuance, remote incremental apply, shared baseline merge core) advances it; older no-ops never lower it; `MAX_SAFE` allocation fails closed. This lock claims incremental `topicMessage` + ordinary-`messageBlock` order convergence only — never complete structure convergence (no full-structure claim without a complete membership/frame set) — and authorizes no other domain (remaining compound paths stay deferred). Every other Locked row is unchanged. | **Locked** |

## 4. Definitions

- **Syncable personal-data domain**: the explicitly versioned set of user data covered by the §10 inventory for a claimed sync scope. Only this domain is subject to complete-synchronization claims.
- **Complete synchronization**: after convergence, each paired device holds the same logical user data for the claimed domain, within the locked merge/tombstone/aggregate rules — not merely the same operation count or cursor.
- **Logical baseline**: an allowlisted, schema-versioned, device-independent capture of syncable state at a per-channel watermark, usable for bootstrap and repair. Generated client-side from Main SQLite authority. Never a raw database file.
- **Current effective baseline**: the single per-channel baseline blob plus schema/inventory-version, digest, and watermark metadata the relay durably persists and serves for bootstrap. A new publish replaces it atomically only when gated by `SYNC-DATA-023` and with in-flight bootstraps protected by `SYNC-DATA-024`; prior-version physical reclamation is deferred.
- **Availability copy vs content truth**: device-held Main SQLite state is content truth and rebuild source; the relay-persisted baseline is an offline-bootstrap availability copy that carries no adjudication power.
- **Operation watermark N**: the per-channel sequence position up to which a baseline claims to incorporate channel operations. Replay resumes at N+1 with gap repair per SYNC-DATA-007 and the three-segment boundary per SYNC-DATA-021.
- **Tombstone/delete evidence**: explicit versioned deletion state for an entity. The only convergence input that removes state; absence of an entity in a baseline or stream is never deletion evidence.
- **Parent-membership clock** (logical, `SYNC-DATA-035`–`SYNC-DATA-036`; wire JSON key `parentMembershipClock: {timestamp, operationId}` locked per §10A.1/`SYNC-DATA-040`/`SYNC-DATA-045`, column mapping deferred): per-child logical clock `(timestamp, operationId)` sharing the same total order as existing clocks, proving current parent membership. Established at child creation; only an explicitly governed accepted `reparent` updates it; ordinary content edits never change it. Winning `frame clock` (`frameClock: {timestamp, operationId}`) is the coverage cutoff (`membershipClock <= frameClock` must be listed or frame is incomplete; `membershipClock > frameClock` is deterministic append by `membershipClock` then `childId`; missing/incomparable is fail-closed). Wire JSON spelling `parentMembershipClock`/`frameClock`/`timestamp`/`operationId` is locked; table/column mapping remains deferred. Current entity clocks are not this clock.
- **Manifest/digest**: a comparable summary of meaningful held state (entity set and versions per the claimed domain), used to detect divergence and scope repair. Transport counters alone are not a manifest. Wire baseline integrity uses the locked public digest contract `jcs-sha256-v1` per `SYNC-DATA-032` (I-JSON-compatible `payload` object — `topics`/`messages`/`messageBlocks`/`tombstones`/`orderFrames`/`manifest` with fixed lowerCamelCase keys per §10A.1 — RFC 8785 JCS UTF-8 canonical bytes of **only** the outer `payload` object, SHA-256 lowercase 64-hex, fail-closed inputs, `payload`-only scope with outer `digest`/`digestScheme`/`wireVersion`/`channelId`/`watermark` excluded and atomically bound; `liveCounts`/`tombstoneCounts`/`frameCounts`/`completeness` must be recomputed from validated `payload` plus parent closure/membership/frame coverage per `SYNC-DATA-044` F-05, digest self-consistency never substitutes).
- **Targeted repair**: redelivery of the missing/divergent baseline partition or operation range identified by manifest comparison.
- **Data-convergence status**: the device's truthful claim about its converged state for a claimed domain (§8), independent of service connection and pairing membership.

## 5. Baseline and bootstrap (normative)

1. A baseline declares its schema version, its claimed inventory scope, and its per-channel watermark N. A receiver validates all three before applying; anything unvalidatable is rejected fail-closed per SYNC-DATA-015.
2. The baseline payload contains only allowlisted syncable fields (§10). Credentials, device-local paths, caches/FTS artifacts, and transient/UI-derived state are never baseline content, even when colocated with syncable rows in local storage.
3. Baseline origin is client-side and device-independent: a publishing device captures its baseline from its own Main SQLite authority; the relay never generates, merges, or adjudicates baseline business content (SYNC-DATA-019). The relay validates the protocol envelope and durably persists, per channel, the single current effective baseline blob plus schema/inventory-version, digest, and watermark metadata; a new publish replaces the effective baseline atomically only when it passes the SYNC-DATA-023 serialization/monotonic/coverage gate, and in-flight bootstraps stay protected per SYNC-DATA-024 (SYNC-DATA-019, SYNC-DATA-022–SYNC-DATA-024).
4. Bootstrap applies in watermark order: apply the baseline at N, then replay N+1 onward. Concurrent writes above N are part of the converged result; the handoff must prove no gap between what the baseline incorporated and where replay resumed (SYNC-DATA-007). The watermark handoff follows the three-segment boundary (SYNC-DATA-021): publish snapshot-boundary proof, relay atomic publish with N+1-onward retention, and receiver single-transaction merge with cursor=N or whole rollback.
5. A joining device may bootstrap from the relay-persisted current effective baseline even when all prior devices are offline (SYNC-DATA-020). Online-device temporary supply is not the main path; this ADR introduces no presence tracking and no baseline source election.
6. Applying a baseline merges with — never overwrites — local pre-pair data and pending outbox intent (SYNC-DATA-008, §6). A device with unsent local operations keeps them pending through bootstrap and still requires their acknowledgement; the baseline does not acknowledge them.
7. Parent/child aggregates apply under §7 completeness/ordering or atomic group semantics. Partial aggregates never become durable user-visible shells.
8. Baseline release uses the single-stage client-declared publish model only (SYNC-DATA-025): one publish operation carrying the `SYNC-DATA-027` envelope categories. There is no relay-issued fence, token, or prepare handshake to implement, request, or wait for.
9. The publisher proves the `SYNC-DATA-026` barrier before publishing: quiescence, drained and confirmed pre-barrier outbox, continuous pull to an empty page with no unresolved gap, then one in-barrier SQLite snapshot capturing logical baseline plus clocks plus tombstones plus channel binding plus cursor N plus outbox=0 plus digest. The barrier releases after publish success or failure. Barrier/attestation orchestration is not implemented in current code.
10. The relay applies the `SYNC-DATA-028` validation boundary and the `SYNC-DATA-029` state machine: `empty` versus `current baseline`, atomic replace on success, idempotent `200` retry only on full match with the current effective baseline (channel binding, N, digest scheme/version, digest, canonical logical payload), stale `409` for a replaced-candidate retry with no permanent cross-replacement fingerprint, `409` on same-N divergence or below-current watermark/overwrite conflict, `400` on structure/version/digest invalid, existing-`401` service auth plus `403` device-credential/membership refusal, `413` oversize, `500` store failure, all error bodies as `{error}`. Empty fetch returns `404` `{error:'baseline-not-found'}` as a legitimate empty state, not a routing error. Digest checks follow `SYNC-DATA-032` (`jcs-sha256-v1` recompute; match proves byte-identity only).
11. Fetch serves the current envelope only to authenticated channel members; the client then pulls with cursor=N for N+1 onward (SYNC-DATA-030). SSE stays hint-only without the blob; a dedicated baseline event stays deferred per SYNC-DATA-031.

## 6. Merge, ordering, and deletion (normative)

1. **Identity**: stable entity IDs identify the same logical entity on every device. Re-issuing identity at apply time (duplicating an entity because it arrived from another device) violates SYNC-DATA-006.
2. **Field merge**: updates carry intentional changed fields; independent fields of the same entity merge across devices. Merge behavior for the currently validated shapes stays compatible with the existing timestamp-then-operationId LWW for the current protocol version; a future versioned ordering decision may supersede the tie-break only through its own governed decision, never by silent drift. Per-row `message.sortOrder` / `message_block.sortOrder` are not wire fields per `SYNC-DATA-038`; field merge never touches member ordering.
3. **Deletion wins by evidence, not by absence**: only explicit versioned tombstone/delete evidence removes state. A baseline or stream that omits an entity states nothing about that entity. First-pairing union (SYNC-DATA-005) therefore keeps both sides' entities unless a tombstone says otherwise.
4. **Late descendants stay contained**: a child arriving after its parent's tombstone is suppressed and must not resurrect the parent. This extends the existing parent-tombstone containment to baseline/bootstrap delivery: a baseline that carries a tombstoned parent plus a descendant the tombstone already covers applies the tombstone outcome, not the descendant.
5. **Outbox preservation**: local pending intent participates in merge as unsent local state, not as already-converged state. Conflict ordering applies when that intent replays against converged state; bootstrap never pre-resolves it by dropping it.
6. **Parent-scoped order frames — the ordering contract** (`SYNC-DATA-033`–`SYNC-DATA-036`, normative): user-visible order for inventory-covered `message`/`block` members is governed solely by parent-scoped order frames as defined in `SYNC-DATA-033`. Each `topic` carries one `topic → message` frame, each `message` carries one `message → block` frame; each frame contains parent identity, ordered child IDs in user-visible order, `frame clock (timestamp, operationId)`, and frame version. Frames are strictly closed per version (`SYNC-DATA-039`): unknown fields or unknown frame version fail closed. See §6A for full boundary, merge, and materialization rules. Topic ordering is not introduced by this unit: if the current product has no per-topic visible-order field analogous to `message.sortOrder`/`block.sortOrder`, no topic order frame or field is introduced here; only `message`/`block` order is locked. Any future topic ordering requires its own governed decision.

## 6A. Parent-scoped order frame — boundary, merge, and materialization (normative)

Parent-scoped order frames (`SYNC-DATA-033`–`SYNC-DATA-036`) govern user-visible sequence without governing membership or content:

1. **Authority boundary** (`SYNC-DATA-034`): a frame decides only the relative order among currently alive members of the same parent. It never creates, deletes, reparents, or resets parent identity, and never overwrites entity content/fields. Membership and deletion remain governed by entity existence plus `op`/tombstone semantics (`SYNC-DATA-005`/`SYNC-DATA-006` plus §6 items 3–4). A frame that would imply a different parent, resurrect a tombstoned member, or invent an entity is out-of-authority and its out-of-authority members are not materialized.
2. **Concurrent merge — normative algorithm with logical parent-membership clock** (`SYNC-DATA-035`, closes F-02): concurrent frames for the same parent resolve by the existing `timestamp+operationId` total-order LWW — the same total order governing field clocks; no new ordering key is introduced. Winner determination uses that total order only; payload order or receipt order does not decide. The winning frame's `ordered child IDs` are then filtered: duplicates are de-duplicated by first appearance (first occurrence retains semantic position; later duplicates ignored), deleted/tombstoned children, children whose parent identity differs per current membership, and unknown child IDs are not materialized and do not resurrect. **Logical `parent-membership clock` (normative, new logical contract state, no implementation yet):** each child carries a logical `parent-membership clock (timestamp, operationId)` sharing the same total order as existing clocks; it is established at child creation and, only if a future explicitly governed `reparent` operation is accepted, updated by that reparent; ordinary content/field edits never change it. The winning frame's `frame clock` is the coverage cutoff. If any effective alive same-parent child has `membershipClock <= frameClock` yet is absent from the winner's ordered list, the frame is **incomplete** — receiver must reject/repair fail-closed per `SYNC-DATA-015`/`SYNC-DATA-039`, not silently append. Children with `membershipClock > frameClock` (concurrent or later arrivals relative to the winner's snapshot) that are alive and same-parent must be retained and appended deterministically by `membershipClock` ascending then `child ID` lexicographically. Any required membership clock missing or not comparable (not totally ordered with the frame clock) causes fail-closed (`400` on publish where applicable, otherwise blocked/repair-required per `SYNC-DATA-015`). Silent member loss to preserve winner-frame exactness is prohibited. Current entity clocks must not substitute for `parent-membership clock`; implementation may not reuse them as the membership clock without an explicit governed revision.
3. **Atomic apply with persistent winning frame, fixed-point re-evaluation, dense materialization, baseline/incremental equivalence, and atomic transaction boundary** (`SYNC-DATA-036`, closes F-03/F-04): a receiver persistently stores per-parent winning-frame state. It applies a parent's order frame atomically with the `SYNC-DATA-035` validity filtering, membership-clock-gated completeness check, and deterministic append (by `membershipClock` then `childId`), then materializes the resulting logical order into local dense `sortOrder 0..n-1` following the filtered winner plus deterministic append sequence. Whenever the winning frame for a parent changes, or any relevant child `existence` / `parent-membership clock or parent identity` / `tombstone` state changes, the receiver re-runs the same `SYNC-DATA-035` filtering / completeness / append / materialization algorithm until a deterministic fixed point is reached, guaranteeing arrival-order independence. Baseline application and incremental operation replay that deliver the same set of entity states plus the same winning frames reach the same logical order; dense `sortOrder` values are local projection/query authority only and carry no additional merge meaning beyond the frame-derived sequence. The SQLite governance in [sqlite-migration.md](./sqlite-migration.md) is not rewritten: local `sortOrder` remains authoritative for local queries/projection while wire truth is the frame per `SYNC-DATA-038`. Per-row `sortOrder` is excluded from the wire logical payload as an entity field and from the wire field-clock inventory; until frames are implemented, local `sortOrder` stays local-only and unsynced, and no partial wire `sortOrder` merge is permitted (`SYNC-DATA-038`). **Transaction boundary (normative, F-04):** one visible commit must atomically cover all affected state for its scope: entity/tombstone/`parent-membership` merge, per-parent winning-frame state, the complete dense `sortOrder 0..n-1` for every affected parent (source and destination parents for a `reparent`; single parent for create/delete/frame-only), and the corresponding replay `applied`/`cursor` state. `reparent` is atomic across both sides. If the commit cannot complete all of the above, the change stays pending/invisible with a full rollback — no partial projection is committed.
4. **Strictly closed per version** (`SYNC-DATA-039`): every versioned wire artifact including order frames has a fixed key set, fixed `required` vs `nullable` rules, and fixed allowed-value constraints. Unknown fields, unknown frame or schema/inventory versions, or unknown envelope categories cause fail-closed rejection (`SYNC-DATA-015`; `400` on publish where applicable, otherwise blocked/repair-required). Silently ignoring unknown fields or versions for forward compatibility is prohibited; forward compatibility requires a new explicitly versioned inventory/schema revision. `parent-membership clock` participates in this strictly closed inventory when versioned.
5. **Not locked here** (`SYNC-DATA-040`): specific operation names, endpoint paths, HTTP verb spellings, `packages/shared` TypeScript types, SQLite tables/columns, `parent-membership clock` storage mapping, and wire frame payload JSON spelling/key names (including digest canonicalization key spelling except as governed by RFC 8785 JCS) are not locked by this ADR. `parent-membership clock` is a new logical contract state with no implementation yet; its table/column/op/JSON spelling is not locked here. No code, migration, or endpoint is authorized here; any mapping to those artifacts requires a future explicitly activated step conforming to the locked semantics above.

## 7. Aggregate completeness (normative)

Aggregates with parent/child references (topics with messages, messages with blocks/segments/branches where the inventory covers them) converge as coherent units:

1. Either the full reference closure arrives (completeness with reference-preserving order), or the group applies atomically, or the incomplete aggregate remains pending/invisible until replay or repair completes it.
2. Ordering-only changes are not convergence inputs under the pre-`SYNC-DATA-033` baseline: before parent-scoped order frames, reordering without content change carries no merge meaning. After `SYNC-DATA-033`–`SYNC-DATA-037`, structural operations that change user-visible `message`/`block` order must issue order frames (or the governed equivalent atomic group) per `SYNC-DATA-037`; ordering changes without a frame do not converge and no complete-convergence claim extends over them.
3. Malformed or unvalidatable aggregate members fail closed per SYNC-DATA-015 without blocking unrelated aggregates beyond what cursor semantics require — and the blockage itself is surfaced as blocked/repair-required, never as convergence.
4. Baseline logical-payload order-frame coverage (`SYNC-DATA-037`, normative): the baseline logical payload (per `SYNC-DATA-004`/`SYNC-DATA-027`) must carry order frames for every parent within the versioned inventory. Incremental delivery of order is via frames (or an explicitly governed equivalent atomic group that yields the same frame semantics). Any future structural operation that actually changes user-visible `message`/`block` order or membership — including `reorder`, `insert-at`, `branch`/`clone`/`paste` or equivalent that reorders visible children or changes membership — must issue the corresponding order frame(s) (or the governed equivalent atomic group) before a device may claim complete convergence for its domain. A `select-answer` variant that only flips wire-excluded local overflow without changing order or membership (such as the current `foldSelected`-only `selectAnswerMessage`) changes no converged state and therefore issues no frame. The current implementation has no frame issuance for such structural operations; this gap is explicitly recorded and no complete-convergence claim extends over reordered state until the frame path exists. `sortOrder` is not a wire entity field (`SYNC-DATA-038`); only frames carry ordering on the wire.

## 8. Reconciliation and convergence status (normative)

1. Reconciliation starts from manifest/digest comparison over the claimed domain, scopes the divergence (missing entities, divergent versions, incomplete aggregates, uncovered prefix), and repairs through targeted redelivery. Push/pull retry without comparison and scoped repair is liveness, not reconciliation (SYNC-DATA-012).
2. History-unavailable (compacted prefix without a covering baseline, or a cursor pointing at unavailable operations) routes to bootstrap/repair under §5. Silent cursor reset — advancing past unavailable history and claiming convergence — is prohibited (SYNC-DATA-011).
3. Convergence status is per claimed domain and independent of `SYNC-CC-*` service/pairing status. At minimum it distinguishes: `Synced`, `Converging` (catch-up/repair in progress), `Partial` (claimed domain subset only), `Unsupported` (domain outside the versioned inventory), `Blocked` (version incompatibility or unresolvable failure), and `Repair required` (divergence or history-unavailable awaiting bootstrap/repair).
4. `Synced` holds only when baseline completeness, operation catch-up, zero unresolved apply failures, and successful consistency verification all hold for the claimed domain. Verification means a manifest/digest comparison confirming the same logical state, not equal cursor values alone.
5. Status transitions never delete or roll back user data to reach a cleaner state. Repair adds or corrects through governed inputs (baseline, replay, tombstones); it never removes local state except through explicit tombstone/delete evidence.

## 9. Retention and compaction (normative)

1. The compacted prefix must be covered by a valid recoverable baseline plus the tombstone/deletion evidence needed to preserve deletion outcomes for entities removed within the prefix. "Covered" is proven by recoverability: a device bootstrapping from retained state reaches the same logical outcome for live and deleted entities as replay from the uncompacted log would have produced. A baseline publish that cannot provide that coverage for the prefix it would compact is rejected per SYNC-DATA-023 and changes nothing effective.
2. Until that proof exists for a prefix, the prefix is retained. While a baseline is the channel's current effective baseline, its required prefix is never trimmed: the continuous log from N+1 onward is retained (SYNC-DATA-021). Replacement additionally preserves the N+1 replay/repair path of any successfully issued baseline until its bootstrap completes or explicitly fails, via retained baseline/log/delete evidence or an equivalent recoverable mechanism (SYNC-DATA-024). With no compaction implemented, full log retention is the minimal path satisfying that protection; it does not lock a long-term never-compact constraint (SYNC-DATA-031). Retention duration, storage location, endpoint path strings, HTTP verb spellings, chunking/streaming, size thresholds, compression, capacity thresholds, retained history physical counts, prior-version physical reclamation mechanics, lease/ack endpoints, timeouts, retained physical old-baseline counts, SSE event names, and reaping mechanics are deferred implementation choices (SYNC-DATA-017, SYNC-DATA-022, SYNC-DATA-024, SYNC-DATA-031), but the coverage precondition is not deferrable.
3. A device encountering history it cannot recover routes to bootstrap/repair and reports blocked/repair-required. It never advances past the gap silently.

## 10. Syncable-data inventory requirement (normative)

1. A versioned inventory must exist before any full-sync claim, classifying at minimum: (a) required user data that complete synchronization covers; (b) optional configuration governed separately with its own scope and default behavior; (c) device-local exclusions that never enter the channel (credentials, device-local paths, caches/FTS, transient/UI-derived state). For `SYNC-DATA-033`–`SYNC-DATA-039`, inventory versions additionally enumerate the set of parents whose order frames are required.
2. The currently validated topic/message/partial message-block scope is a subset of that inventory, not complete-product sync. Claims for the current scope stay bounded to the living reference's evidence; broader claims wait for explicit contract coverage. For ordering, per-row `message.sortOrder` / `message_block.sortOrder` are excluded from the wire field inventory per `SYNC-DATA-038`; ordering lives in parent-scoped frames per `SYNC-DATA-033`. Topic ordering is not introduced by this unit: if the current product has no per-topic visible-order field analogous to `message.sortOrder`/`block.sortOrder`, no topic order frame or field is introduced here; only `message`/`block` order is locked. Any future topic ordering requires its own governed decision.
3. Structured content, ordering/segments/branches, attachments and attachment bytes each require explicit contract coverage (shape, reference integrity with §7 aggregates, transport, and verification) before full-sync claims extend to them. Exclusion from a claim is explicit inventory scope, not silent omission. All per-version wire artifacts including order frames are strictly closed: fixed key set, fixed `required` vs `nullable` rules, fixed allowed-value constraints; unknown fields or unknown versions fail closed per `SYNC-DATA-039` (`SYNC-DATA-015`), never silently ignored.
4. Wire inventory exclusion of per-row `sortOrder` (`SYNC-DATA-038`, normative): per-row `message.sortOrder` and `message_block.sortOrder` are excluded from the wire logical payload as entity fields and from the wire field-clock inventory; they are not transmitted as row properties and carry no per-field clock on the wire. Local SQLite dense `sortOrder 0..n-1` remains local projection/query authority materialized from frames per `SYNC-DATA-036` and does not rewrite [sqlite-migration.md](./sqlite-migration.md) storage or migration governance.


## 10A. Versioned logical wire inventory — `topic-message-stable-block-order-v1` at `sync-baseline-wire-v1` / `chat-core-baseline-v1` / `parent-order-frame-v1` (normative, wire v1)

This section is the normative projection of `SYNC-DATA-041`–`SYNC-DATA-045` / `SYNC-CC-021` for the first versioned logical wire inventory. For inventory v1 (`topic-message-stable-block-order-v1`) it **locks byte-level JSON hierarchy & lowerCamelCase key spelling** for the baseline wire artifacts (outer envelope, `payload` root, manifest, entity/field/clock/tombstone/order-frame object keys) per `SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021` + §10A.1 (`F-02`); it also locks type/value domain, parent/frame full-set, tombstone strong barrier, and manifest recompute. Path/HTTP verb/TS types beyond the wire DTO/DB tables/storage mapping remain deferred per `SYNC-DATA-040`/`SYNC-DATA-045`/§10A.6.

### 10A.1 Version roles, naming, and locked JSON hierarchy & key spelling (`SYNC-DATA-041` + `F-01`/`F-02` locks)

- Wire protocol: `sync-baseline-wire-v1` — identifies publish/fetch wire behavior plus envelope categories (`SYNC-DATA-027`) and remains paired with the outer `jcs-sha256-v1` binding (`SYNC-DATA-032`/`SYNC-DATA-044`). **JSON location: only outer envelope key `wireVersion` (never inside `payload`/`payload.manifest`, never in JCS input).**
- Payload schema: `chat-core-baseline-v1` — identifies the versioned logical-payload structure (entity kinds, logical field sets per §10A.2, clock shapes, tombstone shape, order-frame logical shape, manifest shape). **JSON location: `payload.payloadSchema` and `payload.manifest.payloadSchema`.**
- Inventory: `topic-message-stable-block-order-v1` — identifies the exact allowlisted field inventory and excluded-status/type gate (§10A.2–§10A.3). **JSON location: `payload.inventoryVersion` and `payload.manifest.inventoryVersion`.**
- Order-frame family: `parent-order-frame-v1` — identifies the frame logical composition (`SYNC-DATA-033`/`SYNC-DATA-042`–`SYNC-DATA-044`). **JSON location: `payload.orderFrameVersion` and `payload.manifest.orderFrameVersion`.**

`wireVersion` travels only in the outer envelope; the three content versions travel only inside `payload` / `payload.manifest` (F-01). Names do not imply complete-product sync; this wire v1 is a bounded subset for chat-core history and is explicitly not complete-product sync per §10A.6/`SYNC-DATA-045`.

**Locked lowerCamelCase JSON key sets for inventory v1 (byte-level, strictly closed; unknown key fails closed per `SYNC-DATA-039`/`SYNC-DATA-045`):**

| layer | JSON object | fixed keys (lowerCamelCase, all required, fixed existence) |
|---|---|---|
| **outer envelope** | top-level publish/fetch JSON | `wireVersion` (`"sync-baseline-wire-v1"` only outer), `channelId` (opaque, auth-derived via `SYNC-CC-*`, never trust body `deviceId`; no `deviceId` key), `watermark` (safe-int `N`), `digestScheme` (`"jcs-sha256-v1"`), `digest` (64 lower-hex of `payload` JCS), `payload` (object — the only JCS input) |
| **logical payload root** | `payload` (value of outer `payload`) | `payloadSchema` (`"chat-core-baseline-v1"`), `inventoryVersion` (`"topic-message-stable-block-order-v1"`), `orderFrameVersion` (`"parent-order-frame-v1"`), `scope` (literal `"chat-core-baseline-v1:topic-message-stable-block-order-v1"`), `topics` (`Topic[]`), `messages` (`Message[]`), `messageBlocks` (`MessageBlock[]`), `tombstones` (`Tombstone[]`), `orderFrames` (`OrderFrame[]`), `manifest` (`Manifest`) |
| **manifest** | `payload.manifest` (enters JCS) | `payloadSchema`, `inventoryVersion`, `orderFrameVersion`, `scope` (echo literal), `liveCounts: {topic, message, messageBlock}`, `tombstoneCounts: {topic, message, messageBlock}`, `frameCounts: {topicMessage, messageBlock}`, `completeness: "complete"` |
| **topic entity** | `payload.topics[]` | `id`, `name`, `assistantId`, `createdAt`, `updatedAt`, `deletedAt`, `pinned`, `prompt`, `isNameManuallyEdited`, `entityClock`, `fieldClocks` — see type/domain in §10A.2/`SYNC-DATA-042` (all keys fixed existence) |
| **message entity** | `payload.messages[]` | `id`, `topicId`, `name`-set analogous: `id`, `topicId`, `role`, `content`, `status`, `askId`, `model`, `modelId`, `assistantId`, `createdAt`, `updatedAt`, `entityClock`, `fieldClocks`, `parentMembershipClock` |
| **messageBlock entity** | `payload.messageBlocks[]` | `id`, `messageId`, `type`, `content`, `status`, `createdAt`, `updatedAt`, `entityClock`, `fieldClocks`, `parentMembershipClock` |
| **clocks** | `*.entityClock`, `*.fieldClocks.*`, `*.parentMembershipClock`, `*.frameClock`, `*.deletionClock`, `*.survivingEntityClock` | `timestamp` (safe-int `[0,9007199254740991]`), `operationId` (`string` length `1..256` no `:` for live clocks; `string|null` for `deletionClock.operationId` where `null` is legacy strong barrier only, see `SYNC-DATA-043`) — valid Unicode scalar string when non-null |
| **fieldClocks container** | `*.fieldClocks` | object with fixed per-entity keys: topic `{name, assistantId, createdAt, updatedAt, deletedAt, pinned, prompt, isNameManuallyEdited}`; message `{role, content, status, askId, model, modelId, assistantId, createdAt, updatedAt}`; block `{type, content, status, createdAt, updatedAt}` — each value is `Clock` (`{timestamp, operationId}`) exactly one per mutable field, no extra, no missing |
| **tombstone** | `payload.tombstones[]` | `entityType` (`"topic"\|"message"\|"messageBlock"`), `entityId`, `deletionClock: {timestamp, operationId}` (`operationId` `string|null`, legacy `null` is strong barrier ` (T,null) > (T,nonNull)` suppressing all equal-`T` live ops, two `null` equivalent; new tombstone `operationId` must non-empty, non-forge), `survivingEntityClock: {timestamp, operationId}\|null` |
| **orderFrame** | `payload.orderFrames[]` | `frameVersion` (`"parent-order-frame-v1"`), `kind` (`"topicMessage"\|"messageBlock"`), `parentId` (topicId when `topicMessage`, messageId when `messageBlock`), `orderedChildIds: string[]` (business order, may be `[]` for zero children per F-04; exactly the inventory-included live children via `SYNC-DATA-035` coverage/append), `frameClock: {timestamp, operationId}` |

Channel binding logical key is singular `channelId` (outer envelope only, lowerCamelCase) — three-document consistent; no second channel binding key (`channelKey` etc.) travels; device identity is **not** trusted from any body `deviceId`. Path/HTTP verb/TS types beyond the wire DTO/DB tables/storage mapping remain deferred per `SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021`.


### 10A.2 Inventory and fixed full-state logical fields with locked JSON spelling & type/value domain (`SYNC-DATA-042`, `F-02`/`F-03`)

The `payload` object at this inventory carries **only**:

1. live `topic` in `payload.topics`, live `message` in `payload.messages`, live **stable supported** `messageBlock` in `payload.messageBlocks` — each in **full-state** (not patch), with locked lowerCamelCase keys per §10A.1;
2. one `entityClock: {timestamp, operationId}` per live entity (`payload.*[].entityClock`);
3. per-field `fieldClocks` with fixed per-field keys per §10A.1 (exactly one per mutable inventory field; no extra for `id`/`topicId`/`messageId`/`sortOrder`);
4. one `parentMembershipClock: {timestamp, operationId}` per `message` and per `messageBlock` (`payload.messages[].parentMembershipClock`, `payload.messageBlocks[].parentMembershipClock`);
5. explicit `tombstones` in `payload.tombstones` (`{entityType, entityId, deletionClock, survivingEntityClock}`);
6. `topicMessage` and `messageBlock` order frames at `parent-order-frame-v1` in `payload.orderFrames` (`{frameVersion, kind, parentId, orderedChildIds, frameClock}`);
7. the versioned manifest in `payload.manifest` (`SYNC-DATA-044`).

Fixed full-state logical fields with **locked lowerCamelCase JSON key spelling & type/value domain (F-03)**:

| entity (wire array / JSON key) | identity / parent (non-empty valid Unicode scalar `string`, immutable, never `null`) | mutable nullable fields (each **fixed key, fixed existence**, value `valid Unicode scalar string|null` or `boolean|null` per domain; never `undefined`, never extra, never `sortOrder`) |
|---|---|---|
| `topic` in `payload.topics` | `id` | `name: string|null` / `assistantId: string|null` / `createdAt: string|null` (I-JSON verbatim, not LWW) / `updatedAt: string|null` / `deletedAt: string|null` / `pinned: boolean|null` / `prompt: string|null` / `isNameManuallyEdited: boolean|null` — 8 `fieldClocks` keys matching these |
| `message` in `payload.messages` | `id` + `topicId` | `role: string|null` / `content: string|null` / `status: string|null` (transient gate in §10A.3) / `askId: string|null` / `model: string|null` / `modelId: string|null` / `assistantId: string|null` / `createdAt: string|null` / `updatedAt: string|null` — 9 `fieldClocks` keys matching these; plus `parentMembershipClock` |
| `messageBlock` in `payload.messageBlocks` | `id` + `messageId` | `type: string|null` (gate in §10A.3) / `content: string|null` / `status: string|null` / `createdAt: string|null` / `updatedAt: string|null` — 5 `fieldClocks` keys matching these; plus `parentMembershipClock` |

Domain notes (F-03): all `id`/`topicId`/`messageId`/`entityId`/`parentId`/`orderedChildIds[]` elements are non-empty valid Unicode scalar strings; all other string fields when non-null are valid Unicode scalar strings (`name`/`assistantId`/`createdAt`/`updatedAt`/`deletedAt`/`role`/`content`/`status`/`askId`/`model`/`modelId`/`type`); `pinned`/`isNameManuallyEdited` are `boolean|null`; time fields `createdAt`/`updatedAt`/`deletedAt` are **I-JSON `string|null` verbatim** — never date-parsed, never normalized, not LWW clocks; `status`/`type` keep the existing excluded gates (transient `streaming`/`pending`/`processing`/`searching` + `tool`/`file`/`image`/`video`/`citation`+overflow predicate). Every mutable nullable field is a fixed key with `null` or gate-compliant value; `undefined` never appears, extra keys beyond the fixed inventory never appear, and `sortOrder` is strictly not a wire key. Live `entityClock` + exactly-one-per-field `fieldClocks` + required `parentMembershipClock` (message/block) are fixed existence; any non-empty-valid-Unicode violation, wrong type, missing fixed key, `undefined`, extra key, or `sortOrder` on wire fails closed per `SYNC-DATA-039`.


### 10A.3 Status/type gate (`SYNC-DATA-043`)

- **Transient gate** — `message`/`message_block` statuses `streaming`/`pending`/`processing`/`searching` (case-sensitive) are never wire entities; such rows are excluded at capture and fail closed if presented on the wire.
- **Block-type gate** — `message_block` types `tool`/`file`/`image`/`video`/`citation` (case-insensitive trimmed match; canonical lowercased value checked) and any block whose canonical content depends on structured overflow/attachment bytes (overflow presence of `content`/`file`/`fileId`/`file_id`/`response`/`knowledge`/`memories`/`toolId`/`arguments`/`url`/`l2AttachmentUnavailable` or equivalent structured-attachment predicate per `isUnsupportedBlockForSync`) are never wire entities; such rows are excluded and fail closed if presented.
- Other legacy stable strings (e.g. `success`/`error`/`paused`/`sent` and any `status`/`type` not in the four excluded statuses nor the type/overflow excluded-block predicate) remain wire-eligible; the gate does not over-enumerate an allowlist beyond the four transient statuses and the five excluded block types plus the overflow-structured predicate.

### 10A.4 Clocks and tombstones with fixed keys & strong legacy barrier (`SYNC-DATA-043`, `F-03`/`F-05`)

- **Clock shapes (locked keys `timestamp`, `operationId`)** — `timestamp` is non-negative safe-int `[0,9007199254740991]`; `operationId` is non-empty valid Unicode scalar `string` `1..256` with no `:` when non-null, and `string|null` only for `tombstone.deletionClock.operationId` (legacy). Entity `entityClock: {timestamp, operationId}` required non-null per live entity; field `fieldClocks.*: {timestamp, operationId}` required exactly one per mutable inventory field; `parentMembershipClock: {timestamp, operationId}` required non-null per live `message`/`messageBlock`. Any missing/`null` (except legacy deletion `null`), duplicate, infinite/non-integer timestamp, out-of-safe-integer, illegal Unicode, non-comparable clock fails closed.
- **Entity clock** — required non-null for every live entity (`payload.*[].entityClock`).
- **Field clocks** — required exactly one per mutable inventory field per live entity (topic 8, message 9, block 5) with fixed lowerCamelCase subkeys per §10A.1; no extra field clock for `id`/`topicId`/`messageId`/`sortOrder` or absent fields; missing or duplicate field clock fails closed.
- **`parentMembershipClock`** — required non-null for every live `message` and `messageBlock` (`payload.messages[].parentMembershipClock`, `payload.messageBlocks[].parentMembershipClock`); shares the `(timestamp,operationId)` total order and is established at creation, never changed by ordinary content/field edits, and only updated by an explicitly governed `reparent` if a future decision authorizes it — `reparent` remains **not authorized / not supported** in this wire v1 and any wire payload implying it fails closed unless a later ADR explicitly governs it. Wire JSON key is `parentMembershipClock` (lowerCamelCase); column mapping deferred.
- **Tombstone (locked keys `entityType`, `entityId`, `deletionClock`, `survivingEntityClock`)** — `entityType in {topic,message,messageBlock}` (wire value `messageBlock` lowerCamelCase), `entityId` non-empty valid Unicode scalar string, `deletionClock: {timestamp, operationId}` where `operationId` is `string|null` (`null` only for legacy; new deletions must be `string` length `1..256` non-empty non-colon, never `null`, never forged), `survivingEntityClock: {timestamp, operationId}|null`. **Strong barrier (F-05)**: legacy `null` is not minimal — at equal `timestamp` `(T,null)` is **greater than** any `(T,nonNull)` and suppresses all equal-timestamp live operations; two `(T,null)` are equivalent. This is a legacy compatibility total order, never used for new clocks (new `operationId` must non-empty, non-forge). A tombstone covering a live entity resolves per `SYNC-DATA-006` plus §6 (deletion wins, late descendants contained); tombstone with unknown type/id shape, missing deletionClock, or extra keys fails closed.


### 10A.5 Order frames, manifest with recompute, and JCS determinism (`SYNC-DATA-044` + `F-02`/`F-04`/`F-05`)

- **Order frames (locked keys `frameVersion`, `kind`, `parentId`, `orderedChildIds`, `frameClock`)** — per `SYNC-DATA-033`–`SYNC-DATA-037`, every inventory-within parent has exactly one winning frame; invariants are strictly closed. Each frame in `payload.orderFrames` carries `frameVersion == "parent-order-frame-v1"`, `kind in {topicMessage, messageBlock}`, `parentId` (`topic.id` for `topicMessage`, `message.id` for `messageBlock`), `orderedChildIds: string[]` (business order, `[]` allowed for zero children per F-04, exactly the inventory-included live children after `SYNC-DATA-035` coverage/append), and `frameClock: {timestamp, operationId}`. Parent/frame full-set rule per `SYNC-DATA-044` (F-04): `payload` must contain exactly one `topicMessage` frame per live included `topic` and exactly one `messageBlock` frame per live included `message`, inclusive of empty frames; tombstoned or inventory-excluded parent must have no frame; `orderedChildIds` is fail-closed if not exactly the filtered winning list plus deterministic `membershipClock > frameClock` append (by `membershipClock` asc then `childId` lex); if a baseline would have a transient excluded entity making the inventory children set incomplete, it must not be published with `completeness: "complete"`; permanently excluded structured blocks are not part of the child set. A payload with zero or multiple frames for the same parent, unknown `frameVersion`/`kind`, extra frame keys, illegal Unicode, or `frameClock` out-of-bounds fails closed. `parentMembershipClock` participates in completeness per `SYNC-DATA-035`/`SYNC-DATA-043` (`membershipClock <= frameClock` absent is incomplete → `400`/blocked/repair-required; `membershipClock > frameClock` is deterministic `membershipClock` then `childId` append; missing/incomparable fail-closed; duplicate child ID — first occurrence retained after validation, but explicit duplicate literal in `orderedChildIds` fails strictly closed before filtering).
- **Manifest (locked keys in `payload.manifest`: `payloadSchema`, `inventoryVersion`, `orderFrameVersion`, `scope`, `liveCounts`, `tombstoneCounts`, `frameCounts`, `completeness`; enters `jcs-sha256-v1` per `SYNC-DATA-032`)** — `payloadSchema: "chat-core-baseline-v1"`, `inventoryVersion: "topic-message-stable-block-order-v1"`, `orderFrameVersion: "parent-order-frame-v1"`, `scope: "chat-core-baseline-v1:topic-message-stable-block-order-v1"` (fixed literal), `liveCounts: {topic: number, message: number, messageBlock: number}`, `tombstoneCounts: {topic: number, message: number, messageBlock: number}`, `frameCounts: {topicMessage: number, messageBlock: number}`, `completeness: "complete"` (only `"complete"` is wire-valid; `partial`/`unbound` are internal/non-wire). The manifest is the only business-integrity summary entering the digest; it **excludes** `pendingOutboxCount`, `observedLocalChannelKey`/`observedLocalCursor`/`observationBinding`, `unversioned`/`excluded`/`orphan`/`aggregate` diagnostics, `reasons`/`completenessReasons`, any diagnostic string, and the outer `digest`/`digestScheme`/`wireVersion`/`channelId`/`watermark`. The outer envelope's `digest`/`digestScheme` carry the `jcs-sha256-v1` hex (outer key `digest` clarified per F-05) and are never in the JCS input; only `payload` is input. A manifest with unknown key, missing required count, or `completeness != "complete"` fails closed for wire purposes. **Manifest recompute (F-05)**: on relay publish and on client apply, `liveCounts`/`tombstoneCounts`/`frameCounts`/`completeness` must be **uniquely recomputed** from the validated `payload` (`topics`/`messages`/`messageBlocks`, `tombstones`, `orderFrames`) plus parent closure, `parentMembershipClock` coverage, and frame coverage; declared manifest values that do not match the unique recompute are `400` (publish) / fail-closed (apply) even if the digest self-consistently covers the declared manifest.
- **JCS array determinism over locked `payload`** — RFC 8785 JCS provides object-key ordering; wire ID ordering remains UTF-8 byte lexicographic after Unicode scalar validation (F-03). Arrays in `payload` are ordered: (a) `topics` by `id` UTF-8 byte lex, `messages` by `id`, `messageBlocks` by `id` (each separate array sorted asc); (b) per-entity `fieldClocks` object keys are JCS-sorted (fixed lowerCamelCase field names); (c) `tombstones` by fixed type rank `topic < message < messageBlock` then `entityId` UTF-8 byte lex; (d) `orderFrames` by `kind` rank `topicMessage < messageBlock` then `parentId` UTF-8 byte lex; (e) `orderedChildIds` within each frame **preserve business order** (not re-sorted). Any duplicate `entity (type,id)` within its array, duplicate tombstone `(type,id)`, duplicate frame `(kind,parentId)`, duplicate field clock subkey, duplicate `orderedChildIds` literal, unvalidated Unicode, or duplicate canonical object key fails closed. Readers validate wire byte ordering matches the locked order before applying; mismatched order fails closed (re-canonicalize-then-compare not permitted).


### 10A.6 Per-version strictly closed, excluded domains, and updated non-locking scope (`SYNC-DATA-045`, `F-02`/`F-03`/`F-05`)

- **Strictly closed per version** — every wire artifact at `sync-baseline-wire-v1`/`chat-core-baseline-v1`/`topic-message-stable-block-order-v1`/`parent-order-frame-v1` (outer envelope keys `wireVersion`/`channelId`/`watermark`/`digestScheme`/`digest`/`payload`, `payload` root keys, `payload.manifest` keys, entity/tombstone/orderFrame/clock keys per §10A.1, `payloadSchema`/`inventoryVersion`/`orderFrameVersion`/`scope` literals) is strictly closed: fixed lowerCamelCase key set per §10A.1, fixed `required` vs `nullable` per §10A.2–§10A.5 with F-03 type/value domain, and fixed allowed-value constraints. Unknown fields, unknown `payloadSchema`/`inventoryVersion`/`orderFrameVersion`/`wireVersion`/`kind`/`entityType`, or unknown envelope key cause fail-closed per `SYNC-DATA-015`/`SYNC-DATA-039` (`400` on publish where applicable, otherwise blocked/repair-required). Silently ignoring unknown fields for forward compatibility is prohibited; any previously excluded domain entering the wire requires a new explicitly versioned `inventoryVersion`/`payloadSchema`/`orderFrameVersion`/`sync-baseline-wire` revision.
- **Explicit excluded product domains (never wire)** — `segments` / segment memberships / segment membership order, `branches` / tree operations (`branch`/`clone`/`paste`/`select-answer` and tree structure), `attachments` / file bytes / file references (`file`/`file_path`/`fileId`), `tool`/`citation`/rich structured block data that depends on overflow/attachment structured content, assistant settings / provider credentials / `contextWindowAnchor`, and any other row/table not enumerated in §10A.2.
- **Explicit excluded local domains (never wire, never digest)** — local filesystem paths, FTS/cache/derived indexes, UI/transient state, outbox (`pendingOutboxCount`/payloads)/`applied`/`cursor` logs / per-channel cursor history, conflict logs, local diagnostics / `observedLocalChannelKey`/`observedLocalCursor`/`observationBinding` / `unversioned`/`excluded`/`orphan`/`aggregate` counters and reason arrays / `completenessReasons`.
- The current wire v1 carries only `completeness: "complete"` (per `SYNC-DATA-044`); `partial`/`unbound` remain internal/non-wire provisional states.
- This wire v1 is **not complete-product sync** — it is a bounded subset for chat-core history (topics + stable messages + stable supported blocks plus ordering); product domains above stay outside the claim and require their own governed decisions before any complete-product claim.
- **Updated non-locking scope (`SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021`, F-02/F-05, not the old `未锁 JSON spelling` blanket)** — for **inventory v1 baseline (`topic-message-stable-block-order-v1` at `sync-baseline-wire-v1`/`chat-core-baseline-v1`/`parent-order-frame-v1`) JSON hierarchy & lowerCamelCase key spelling is now locked** (outer envelope keys, `payload` root keys, manifest keys, entity/tombstone/order-frame/clock keys per §10A.1; order-frame JSON keys are locked in v1). Remaining **not locked** are: future operation names/endpoints, endpoint path strings / HTTP verb spellings, `packages/shared` TypeScript types beyond the wire DTO, SQLite tables/columns mapping beyond the wire projection, and storage/retention/GC mechanics. `parentMembershipClock` wire JSON key spelling is locked (`parentMembershipClock` + `timestamp`/`operationId`), only its column mapping deferred. The relay baseline resource binding (`PUT`/`GET /sync/baseline`, body exact envelope, per-channel single current row, status/body spelling) is locked by `SYNC-DATA-046` for this resource only; the receiver bootstrap merge/transaction/failure semantics is locked by `SYNC-DATA-047`.
- **Current local projection is not wire-compliant** (`SYNC-DATA-045`) — `local_sync_baseline_candidate` (`local-sync-baseline-v1` / `topic-message-stable-block-v1`) remains internal/non-wire: it (a) still carries per-row `sortOrder` as an entity field, (b) carries diagnostic manifest keys (`pendingOutboxCount`, `observationBinding`, `unversioned`/`excluded`/`orphan`/`aggregate` counters, `reasons`), (c) carries provisional `observedLocalChannelKey`/`observedLocalCursor`, (d) uses a provisional `local-sync-baseline-v1` canonicalizer, (e) carries no `orderFrames`/`parentMembershipClocks` under the `jcs-sha256-v1` wire projection, and (f) uses pre-lock key spellings that do not match §10A.1 (e.g. `message_block` vs `messageBlock`, missing `parentMembershipClock`, `topics`/`tombStones` spelling). It must be projected through an explicit `jcs-sha256-v1` wire validator/projection (with F-03 type/domain + F-04 full-set + F-05 recompute) before any relay publish and must never be uploaded as `sync-baseline-wire-v1`. No code, migration, column, type, or endpoint is authorized by `SYNC-DATA-041`–`SYNC-DATA-045`; a validated wire projection or barrier implementation requires its own explicitly activated, separately verified step. **Living doc mark**: current implementation code does not yet emit/accept the fixed wire keys/shape; this ADR is target-only/no implementation authorization.


## 11. Disconnect, Unpair, and version consequences

- **Disconnect** (per `SYNC-CC-*` service attachment) changes reachability only. It preserves local data, pending outbox intent, cursors, and convergence status inputs; on reconnect, catch-up and reconciliation resume from pinned positions.
- **Unpair** (per `SYNC-CC-*` channel membership) stops future channel convergence for the unpaired device. It preserves all local data on every affected device, including data that arrived through sync, and performs no rollback, revert, or deletion of already synchronized state (SYNC-DATA-014).
- **Version incompatibility** (unknown baseline/operation/manifest/inventory version, unknown order-frame version, unknown wire fields, or a peer outside the compatible protocol version) fails closed per SYNC-DATA-015/`SYNC-DATA-039`: pin, retain, surface truthfully, and route to upgrade or repair. Downgrade-by-discard and compatibility-by-ignoring-unknown-fields are not permitted by this ADR; any such rule would require its own governed decision. For order frames, an unknown frame version or unknown field inside a strictly closed frame causes fail-closed rejection (`400` on publish where applicable, otherwise blocked/repair-required per `SYNC-DATA-039`), never silent ignore.

## 12. Non-goals and boundaries (not owned by this ADR)

- Connection/registration/channel/pairing behavior and relay baseline envelope storage/distribution infrastructure are owned by [sync-connection-channel.md](./sync-connection-channel.md) (`SYNC-CC-017`–`SYNC-CC-020`). This ADR assumes paired hidden-channel membership with per-channel sequencing plus relay-persisted current-effective baseline distribution exists; it defines what converges over them.
- Current implementation, evidence, gaps, and next step are owned by [multi-device-sync.md](./multi-device-sync.md).
- Endpoint path strings, HTTP verb spellings, chunking/streaming, size thresholds, compression, capacity thresholds, retained history physical counts, old-baseline physical reclamation mechanics, exact retention duration, attachment byte transport, lease/ack endpoints, timeouts, retained physical old-baseline counts, GC/reclamation mechanics, SSE event names, and E2EE are deferred implementation choices (SYNC-DATA-017, SYNC-DATA-022, SYNC-DATA-024, SYNC-DATA-031), not product invariants, except the relay baseline resource binding locked by `SYNC-DATA-046` (`PUT`/`GET /sync/baseline`, body exact envelope, per-channel single current row, status/body spelling) plus the receiver bootstrap merge/transaction locked by `SYNC-DATA-047`. Digest scheme/canonical bytes and **JCS input being only the outer `payload` object with outer `digest` key** are locked per `SYNC-DATA-032` and are not deferred. Publish/fetch operation semantics and status codes are locked per `SYNC-DATA-025`/`SYNC-DATA-028`–`SYNC-DATA-030`. Parent-scoped order-frame wire logical semantics **and inventory v1 wire JSON hierarchy & lowerCamelCase key spelling (outer envelope `wireVersion`/`channelId`/`watermark`/`digestScheme`/`digest`/`payload`, `payload` root, manifest, entity/tombstone/order-frame/clock keys per §10A.1)** are locked per `SYNC-DATA-033`–`SYNC-DATA-039` + `SYNC-DATA-040`/`SYNC-DATA-044`/`SYNC-DATA-045` (F-02/F-05); remaining deferred: future operation names, endpoint paths/verbs, `packages/shared` TypeScript types beyond wire DTO, SQLite tables/columns mapping beyond wire DTO. No presence tracking and no baseline source election are introduced (SYNC-DATA-020). No relay-issued fence/token/prepare handshake exists (SYNC-DATA-025). Topic ordering is not introduced by this unit: if the current product has no per-topic visible-order field analogous to `message.sortOrder`/`block.sortOrder`, no topic order frame is introduced here; only `message`/`block` order is locked.
- WAN exposure, TLS/certificate management, Docker image operations, backup/rotation operations, capacity/SLA, and production readiness are unchanged by this ADR.

## 13. Rejected alternatives (stable decisions)

- **Physical database copy as sync — rejected** (SYNC-DATA-003, SYNC-DATA-004). Copying `chat.db` across devices would transport device-local paths, credentials-adjacent material, caches/FTS artifacts, and schema-instance coupling, and would overwrite rather than merge. Baselines are logical and allowlisted; the continuous mechanism remains the operation log.
- **Absence as deletion — rejected** (SYNC-DATA-005). Treating a missing entity as a delete would turn every partial baseline, filtered scope, or in-flight aggregate into data loss. Only explicit versioned tombstones delete.
- **Snapshot/log gap handoff — rejected** (SYNC-DATA-007). Resuming the stream above the baseline watermark without proving coverage loses concurrent writes. The handoff must prove N+1-onward delivery or repair.
- **Baseline application as overwrite or implicit acknowledgement — rejected** (SYNC-DATA-008). Overwrite loses pre-pair data; implicit acknowledgement fabricates durability for unsent intent.
- **Relay merge/adjudication of baseline content — rejected** (SYNC-DATA-019). The relay validates the protocol envelope and saves/distributes; business truth is decided by per-device Main SQLite under these semantics.
- **Locally observed cursor as watermark proof — rejected** (SYNC-DATA-021). Baseline content and the relay-continuously-persist-confirmed watermark N must share one provable snapshot boundary.
- **Prefix trimming under an effective baseline — rejected** (SYNC-DATA-021, SYNC-DATA-022). While a baseline is effective, the continuous log from N+1 onward is retained.
- **Unserialized or coverage-less baseline replacement — rejected** (SYNC-DATA-023). Concurrent publishes without per-channel serialization, watermarks without relay-continuously-persist-confirmed proof, below-current watermarks violating the `N >= current` monotonic floor, and candidates that cannot cover the compacted prefix plus required tombstone/delete evidence are not convergence; the relay rejects such publishes with the effective baseline unchanged (below-current is `409`; coverage is an additional gate, never a substitute).
- **Severing an in-flight bootstrap's N+1 path — rejected** (SYNC-DATA-024). Replacing the effective baseline must not remove the continuous replay/repair path of a successfully issued baseline before its bootstrap completes or explicitly fails; retention or an equivalent recoverable mechanism preserves it. Full log retention is the minimal path when no compaction exists, not a locked never-compact promise (SYNC-DATA-031).
- **Per-entity `sortOrder` as wire merge field — rejected** (SYNC-DATA-033, SYNC-DATA-038). Per-row `message.sortOrder`/`message_block.sortOrder` as a wire entity field with per-field clock merge is not the convergence mechanism; ordering converges via parent-scoped order frames. Per-row `sortOrder` is excluded from wire entity field/field-clock inventory and carries no merge meaning; local dense `sortOrder 0..n-1` is materialization only.
- **Order frame as membership/content authority — rejected** (SYNC-DATA-034). A frame never creates, deletes, reparents, or resets parent identity, and never overwrites entity content/fields; membership and deletion remain governed by entity/tombstone/op semantics.
- **Silent member loss on concurrent order divergence — rejected** (SYNC-DATA-035). Concurrent frames resolve by `timestamp+operationId` LWW and validity filtering; any alive same-parent child absent from the winning frame — including concurrently inserted members — must be retained and deterministically appended (recommended: entity clock then child ID), or fail closed if deterministic inputs are missing; silently dropping members to preserve winner-frame exactness is not convergence.
- **Non-atomic or divergent baseline vs incremental order materialization — rejected** (SYNC-DATA-036). A parent's order frame applies atomically and materializes to dense `sortOrder 0..n-1`; baseline and incremental replay delivering the same entity states plus winning frames must reach the same logical order.
- **Ordering excluded from complete sync or baseline without frames — rejected** (SYNC-DATA-033, SYNC-DATA-037). User-visible `message`/`block` order belongs to the complete-synchronization domain and is not permanently excludable; the baseline logical payload must carry order frames for every inventory-within parent and structural reorder/insert-at/branch/clone/paste and order-changing select-answer operations must issue frames (or equivalent atomic group) before a complete-convergence claim.
- **Silently ignoring unknown wire fields/versions for compatibility — rejected** (SYNC-DATA-039). Every versioned wire artifact including order frames is strictly closed per version; unknown fields, unknown frame or schema/inventory versions, or unknown envelope categories cause fail-closed rejection, never silent ignore.
- **Locking frame wire spelling/storage mapping in this ADR — not selected** (SYNC-DATA-040). Operation names, endpoint paths/verbs, `packages/shared` types, SQLite tables/columns, and wire frame JSON spelling are not locked here and require a future explicitly activated step.
- **Relay-issued fence/token/prepare handshake — rejected** (SYNC-DATA-025). Baseline release needs no pre-publish grant; adding one would add coordination without changing the serialized gate outcome.
- **Online-device supply as the main bootstrap path and presence/source election — rejected** (SYNC-DATA-020). The main offline path is the relay-persisted availability copy; no presence tracking and no source election are introduced.
- **Partial aggregate shells — rejected** (SYNC-DATA-009). A durably visible parent without its children (or child without its parent) presents state the user never created.
- **Silent cursor reset on missing history — rejected** (SYNC-DATA-011). Advancing past unavailable operations manufactures convergence while abandoning data and deletion outcomes.
- **Push/pull retry as reconciliation — rejected** (SYNC-DATA-012). Retry without comparison cannot detect divergence it never looks at.
- **Connection/pairing status as convergence proof — rejected** (SYNC-DATA-013). A paired and connected device with unapplied operations, pending bootstrap gaps, or unverified state is not `Synced`.

## 14. Security and privacy

- Baselines, operations, manifests, and repair traffic carry only allowlisted syncable fields (§10). Credentials, device-local paths, and transient/UI-derived state are excluded at capture, not redacted at display.
- Secrets (registration credentials, tokens, keys) are never baseline, manifest, log, or evidence content. Evidence and logs carry counts/statuses/digests only, never content, credentials, paths, or raw sizes.
- Main SQLite remains the local chat authority (SYNC-DATA-002); the relay remains store-and-forward/coordination and never owns chat truth, including when it persists and serves baselines. Relay compromise or loss affects availability and coordination, never the definition of converged truth — recovery comes from device-held state under these semantics.
- Transport security, E2EE, and attachment-byte protection are deferred implementation choices and are not decided here.

## 15. Conformance requirements

An implementation conforms to this ADR only when its observed behavior satisfies, at minimum:

1. Paired devices with pre-existing independent data reach the bidirectional union outcome: both sides' entities survive absent explicit tombstones; absence alone deletes nothing.
2. A joining device bootstraps from a watermark-bound baseline plus N+1-onward replay with no gap loss under concurrent writes, while its local pre-pair data and pending outbox intent survive bootstrap unacknowledged. The publish snapshot boundary, relay atomic publish with N+1-onward retention, and receiver single-transaction merge with cursor=N or whole rollback hold per SYNC-DATA-021; publish additionally passes the SYNC-DATA-023 serialization/monotonic/coverage gate and in-flight bootstraps stay protected per SYNC-DATA-024.
3. Same-entity independent fields merge; same-field collisions resolve deterministically compatibly with the current timestamp-then-operationId LWW for the current protocol version; tombstoned parents contain late descendants without resurrection. Per-row `sortOrder` is not a wire field per `SYNC-DATA-038`; ordering converges via parent-scoped frames per `SYNC-DATA-033`–`SYNC-DATA-036`.
4. Parent-scoped order frames converge correctly per `SYNC-DATA-033`–`SYNC-DATA-036`: each inventory-covered parent carries a frame with parent identity + ordered child IDs + `frame clock (timestamp, operationId)` + version; frames are strictly closed per version (`SYNC-DATA-039`); a frame decides only relative order among same-parent alive members and never creates/deletes/reparents or overwrites content (`SYNC-DATA-034`); concurrent frames resolve by `timestamp+operationId` LWW with validity filtering (dedup by first appearance; deleted/different-parent/unknown children not materialized) and deterministic retention append for alive children absent from the winner (recommended: entity clock then child ID; missing total-order inputs fail closed) per `SYNC-DATA-035`; receivers apply frames atomically and materialize to local dense `sortOrder 0..n-1` with baseline/incremental equivalence per `SYNC-DATA-036`. Topic ordering is not introduced by this unit: only `message`/`block` order is governed; per-row `sortOrder` is excluded from wire inventory per `SYNC-DATA-038`.
5. Aggregates never rest as durable partial user-visible shells; incomplete aggregates stay pending/invisible until completed by replay or repair. After `SYNC-DATA-037`, structural `message`/`block` order changes without a corresponding frame (or governed equivalent atomic group) do not converge and no complete-convergence claim extends over them.
6. Baseline logical payload carries order frames for every inventory-within parent and structural reorder/insert-at/branch/clone/paste and order-changing select-answer operations issue frames per `SYNC-DATA-037`; per-row `sortOrder` is not a wire field per `SYNC-DATA-038`; baseline and incremental order results are equivalent per `SYNC-DATA-036`.
7. Retention/compaction keeps recoverability for live and deleted entities; history-unavailable routes to bootstrap/repair with a truthful blocked/repair-required state and never a silent cursor advance. Each channel holds at most one current effective baseline with gated atomic replacement per SYNC-DATA-023 and in-flight protection per SYNC-DATA-024; full log retention is the minimal protection path with no compaction, not a never-compact lock (SYNC-DATA-031); prior-version physical reclamation, lease/ack endpoints, timeouts, retained physical old-baseline counts, and GC mechanics stay deferred.
8. Reconciliation demonstrates manifest/digest comparison with scoped targeted repair; push/pull retry alone does not satisfy this item. Order divergence is detected via frame comparison under the same manifest/digest regime.
9. `Synced` is claimed per domain only with baseline completeness, operation catch-up, no unresolved apply failures, and successful consistency verification; all other states are reported truthfully. After `SYNC-DATA-037`, `Synced` additionally requires order-frame completeness for the claimed domain.
10. Disconnect and Unpair preserve local data; Unpair stops future convergence without rolling back already synchronized state.
11. Version incompatibility and unresolvable failures behave fail-closed: pinned cursor, retained state, truthful status, no fabricated convergence. For order frames, unknown fields, unknown frame version, or missing deterministic retention inputs cause fail-closed per `SYNC-DATA-039`/`SYNC-DATA-035`/`SYNC-DATA-015`.
12. Baselines are client-generated device-independent state; the relay persists and serves the per-channel current effective baseline plus schema/digest/watermark metadata with envelope validation only, and a joining device may bootstrap from it while all prior devices are offline, with no presence/source election. The baseline logical payload includes order frames per `SYNC-DATA-037` and per-row `sortOrder` remains excluded per `SYNC-DATA-038`.
13. Publish/fetch follows the single-stage client-declared wire contract: publisher barrier/snapshot per SYNC-DATA-026, envelope categories per SYNC-DATA-027 (now including order frames where inventory covers ordering per `SYNC-DATA-037`), relay validation per SYNC-DATA-028 (including `SYNC-DATA-023` monotonic floor `N >= current` in non-`empty` state and `SYNC-DATA-032` `jcs-sha256-v1` recompute over the frame-inclusive logical payload), state machine plus `200` (full current-match only; replaced-candidate retry is stale `409` without permanent fingerprint)/`400`/`401`/`403`/`404` (`{error:'baseline-not-found'}` on empty)/`409` (same-N divergence, below-current watermark, or overwrite conflict)/`413`/`500` with `{error}` bodies per SYNC-DATA-029, public digest `jcs-sha256-v1` with versioned-logical-payload-only scope, fail-closed canonicalization, recompute-proves-byte-identity-only, and atomic envelope binding per SYNC-DATA-032 (canonical bytes now deterministically include order frames), and member-only fetch with cursor=N then N+1 pull plus hint-only SSE per SYNC-DATA-030. No fence/token/prepare handshake exists per SYNC-DATA-025. Wire frame payload JSON spelling and operation names stay deferred per `SYNC-DATA-040`.

Conformance is a property of observed behavior against the statements above. This ADR does not prescribe test shape, evidence form, or implementation sequence.

## 16. Deferred items and consequences

- Endpoint path strings, HTTP verb spellings, chunking/streaming, size thresholds, compression, capacity thresholds, retained history physical counts, prior-version physical reclamation mechanics, exact retention duration, attachment transport, SSE event names, and E2EE: deferred (SYNC-DATA-017, SYNC-DATA-022, SYNC-DATA-031), except the relay baseline resource binding now locked by `SYNC-DATA-046` (`PUT`/`GET /sync/baseline`, body exact envelope, per-channel single current row, status/body spelling) which is implemented on the reference relay, plus the receiver bootstrap merge/transaction now locked by `SYNC-DATA-047` (client-side, implemented); all other transport/limit items stay deferred. Digest scheme/canonical bytes and **JCS input-only-`payload` + outer `digest` key + inventory v1 wire JSON spelling** are locked per `SYNC-DATA-032`/`SYNC-DATA-040`/`SYNC-DATA-044`/`SYNC-DATA-045`/§10A.1 and are not deferred. Parent-scoped order-frame wire logical semantics (frame composition, LWW + validity filtering + deterministic append, atomic dense materialization, baseline/incremental equivalence, baseline frame coverage, per-row `sortOrder` exclusion, per-version strictly closed) **plus inventory v1 wire JSON hierarchy & lowerCamelCase key spelling (outer envelope, payload root, manifest, entity/tombstone/order-frame/clock per §10A.1; order-frame keys locked in v1)** are locked per `SYNC-DATA-033`–`SYNC-DATA-039` + `SYNC-DATA-040` + `SYNC-DATA-044`/`SYNC-DATA-045` (F-02/F-05); remaining deferred: future operation names, endpoint paths/verbs, `packages/shared` types beyond wire DTO, SQLite tables/columns mapping beyond wire DTO. Lease/ack endpoints, timeouts, retained physical old-baseline counts, and GC/reclamation mechanics additionally stay deferred under SYNC-DATA-024 and SYNC-DATA-031. Consequence: no baseline, bootstrap, reconciliation, or retention work starts until its own explicitly activated step defines these within the locked invariants (and any existing code must first be migrated to the §10A.1 fixed spelling before claiming wire compliance). Operation semantics and status codes are already locked per SYNC-DATA-025/`SYNC-DATA-028`–`SYNC-DATA-030` and are not deferred.
- Syncable-data inventory content (exact entity/field lists and versions): required by SYNC-DATA-010 but its content lives in its own governed artifact/step. For ordering, inventory versions now additionally enumerate the parent set whose order frames are required and the wire field inventory explicitly excludes per-row `message.sortOrder`/`message_block.sortOrder` per `SYNC-DATA-038`; this unit locks only `message`/`block` order — topic ordering is not introduced (if the current product has no per-topic visible-order field analogous to `message.sortOrder`/`block.sortOrder`, no topic frame is introduced). Consequence: until the inventory and the per-domain contracts it demands exist, full-sync claims stay unmade and scope claims stay bounded to validated shapes.
- Conflict-recovery UX and row-level conflict visibility: deferred. Consequence: deterministic ordering resolves same-field collisions without a dedicated user recovery experience until a later decision designs one.
- Channel garbage collection, multi-group/merge, and credential recovery remain deferred under `SYNC-CC-*`; snapshot/backfill history they touch additionally requires this ADR's retention/repair preconditions.

## 17. ADR change policy

- **Status**: Approved and fixed. This ADR is the authoritative owner of target data-convergence semantics (`SYNC-DATA-*`). It stays unchanged while the design is unchanged.
- **Living state lives elsewhere**: current implementation, evidence, gaps, and next step belong exclusively to [multi-device-sync.md](./multi-device-sync.md), which references this ADR as target governance and must not duplicate its decision tables.
- **Design change requires a new decision**: this ADR changes only by explicit user-approved amendment, or is marked superseded by a named successor ADR that states what it replaces. The superseded ADR is retained for audit and no longer authoritative.
- **Evidence never silently modifies decisions**: validation runs, implementation findings, and new evidence are recorded in the living reference; they do not alter this ADR's decisions without an amendment or superseding ADR.
- [sync-architecture-selection.md](./sync-architecture-selection.md) stays a conditional fallback reference; it is unaffected by this ADR except for a routing pointer.
- **No implementation authorization flows from this ADR.** No baseline, bootstrap, reconciliation, retention, inventory, status, migration, IPC, or code change is authorized by citing it. Citing this ADR as proof that the target convergence model is implemented is false.

---

## References

- [Personal Multi-Device Sync](./multi-device-sync.md) — current reference (implementation/evidence/gaps/next step).
- [Sync Connection, Registration, and Hidden Multi-Channel Pairing ADR](./sync-connection-channel.md) — approved target for service connection/registration/channel/pairing semantics (`SYNC-CC-*`); pairing/channel scope assumed by this ADR.
- [Sync Architecture Selection](./sync-architecture-selection.md) — conditional fallback reference.
- [SQLite migration governance](./sqlite-migration.md) — SQLite chat authority, migration process.
- [Context window governance](./context-window.md) — context anchor semantics.
- [Application Identity ADR](./cherry-chat-application-identity.md) — identity, compatibility, updater/release freeze, platform scope.
- [Architecture Evolution Program](./architecture-evolution-program.md) — program intent (sync not tracked there).
