interface:
  display_name: "GitHub Workflow"
  short_description: "Create/update PRs with template compliance; review local branches, PRs, commits, and files with multi-agent adversarial review and auto-fix; inspect CI checks and logs"
  default_prompt: "Create a pull request for my current branch using the repository template workflow."
