# Sync Connection, Registration, and Hidden Multi-Channel Pairing — Approved ADR

> **Status**: Approved / Authoritative (target semantics).
> **Decision namespace**: `SYNC-CC-*` (durable; this ADR owns these IDs).
> **Role**: This ADR owns target relay service connection, device registration, channel, and pairing semantics only. It does not own current implementation status, evidence, gaps, or next step — those live in [Personal Multi-Device Sync](./multi-device-sync.md). It does not own candidate/fallback analysis — that lives in [Sync Architecture Selection](./sync-architecture-selection.md) (conditional fallback reference only).
> **Non-implementation**: No protocol/code implementation is authorized by this ADR. Implementation requires a separate explicitly activated decision and step.
> **Development principle**: This ADR is governed by `adaptive-development` principles: the intended outcome remains the anchor, current state and gap determine the next step, and design, implementation, evidence, and validation evolve together.
> **Last updated**: 2026-09-10 — inventory 审阅 F-01..F-05 传播修订（`SYNC-CC-021` ↔ `SYNC-DATA-041`–`SYNC-DATA-045`）：`sync-baseline-wire-v1` **仅 outer envelope**（`wireVersion`，F-01）/ `chat-core-baseline-v1` / `topic-message-stable-block-order-v1` / `parent-order-frame-v1` 四版本角色与命名锁（`SYNC-DATA-041`，不暗示 complete-product sync，三文档一致）；`payloadSchema`/`inventoryVersion`/`orderFrameVersion` 仅在 `payload`/`payload.manifest` 回显并绑定（F-01）；**byte-level v1 锁定 JSON hierarchy & lowerCamelCase key spelling** — outer envelope `{wireVersion, channelId, watermark, digestScheme, digest, payload}`、逻辑载荷根 `{payloadSchema, inventoryVersion, orderFrameVersion, scope, topics, messages, messageBlocks, tombstones, orderFrames, manifest}`、manifest / entity / tombstone / order-frame / clock 均固定（`SYNC-DATA-040`/`SYNC-DATA-045`/§10A.1，F-02）；全量全状态逻辑字段与可空/排除/`sortOrder` 严禁及 I-JSON `string|null`/`boolean|null` 类型域与全键固定存在（`SYNC-DATA-042`/`SYNC-DATA-043`，F-03）；`parentMembershipClock` 及 parent/frame 全集（含空 frame 与 transient/永久 excluded 语义，F-04）；tombstone legacy `null` 强删除屏障 `(T,null) > (T,nonNull)` 抑制同 `T` live 操作（F-05）；每 parent 恰一 winning frame、manifest 唯一重算与 `400`/fail-closed（F-05）；`jcs-sha256-v1` 输入仅 `payload` 对象不含 outer envelope/digest、outer `digest` 键明确、数组顺序保持（F-05/`SYNC-DATA-032`/`SYNC-DATA-044`）；`SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021` 未锁范围已更新为 inventory v1 JSON spelling 已锁、未来操作名/端点/TS/DB/存储仍未锁、order-frame keys 在 v1 已锁定；`SYNC-DATA-033`–`SYNC-DATA-039` 既有语义保持；`SYNC-CC-017`–`SYNC-CC-020` 传播上述逻辑载荷与 digest 范围，确立外层 envelope 仅承载 `wireVersion`/`channelId`/`watermark`/`digestScheme`/`digest`/`payload` 而 `payload.manifest` 进入 digest（target-only/code-less/未授权实现，当前代码尚未符合固定 wire keys/shape）；2026-09-11 修订：新增 `SYNC-CC-022` 锁定 relay baseline 资源绑定（`PUT`/`GET /sync/baseline`、exact envelope body、per-channel single current row cc-2、status/body spelling）；2026-09-11 新增 `SYNC-CC-023` 锁定 receiver-first baseline bootstrap fetch 触发与 GET200/404 行为及 N+1 接续（endpoint 仍由 `SYNC-CC-022` 锁定，此处不重锁），其余 Locked 语义不变。

---

## 1. Context and problem

The relay inherently supports multiple internal sync channels, but the current implementation behaves as a single global channel with an invite/founder/trust model. That model cannot express the target product behavior: a user connects each of their own devices to one relay service, pairs devices explicitly by device code, and syncs within a private channel per device group — while other users' channels on the same relay stay invisible and isolated.

Prior invite/founder/trust single-channel behavior is superseded rationale: it cannot express the target product behavior described below. This ADR locks the target connection/registration/channel/pairing semantics as the design authority for that behavior.

## 2. Authority boundary

- **This ADR owns**: target service connection/registration semantics, channel/pairing semantics, device-code semantics, request lifecycle, channel creation/join/unpair/dissolve rules, per-channel sequencing requirements, relay per-channel current-effective baseline envelope storage/distribution infrastructure with serialized publish gating and in-flight bootstrap retention (`SYNC-CC-019`) plus single-stage client-declared publish/fetch operation semantics and status codes (`SYNC-CC-020`) where the baseline logical payload now deterministically includes the wire v1 inventory `topic-message-stable-block-order-v1` / `parent-order-frame-v1` at `sync-baseline-wire-v1` / `chat-core-baseline-v1` (`SYNC-DATA-041`–`SYNC-DATA-045` / `SYNC-CC-021`) — fixed full-state logical fields with nullable/non-null/`sortOrder` exclusion (`SYNC-DATA-042`), transient status & `tool/file/image/video/citation` block-type gate plus clock shapes/`parent-membership`/`null-operationId` tie-break (`SYNC-DATA-043`), per-parent exactly-one winning frame plus membership-gated completeness (`SYNC-DATA-044`), fixed manifest entering `jcs-sha256-v1` with UTF-8 byte lexicographic determinism and per-version strictly closed (`SYNC-DATA-044`–`SYNC-DATA-045`), remaining not complete-product sync — with per-row `sortOrder` wire exclusion per `SYNC-DATA-038` and `jcs-sha256-v1` digest input includes those frames per `SYNC-DATA-032`/`SYNC-DATA-044` (wire `payload` root / manifest / entity / tombstone / order-frame / clock lowerCamelCase key spelling locked for inventory v1 per `SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021` + §10A.1, `F-02`; column mapping deferred), logical `parentMembershipClock` per `SYNC-DATA-035`/`SYNC-DATA-043` as versioned logical state (wire JSON key `parentMembershipClock` locked, column mapping deferred) and persistent per-parent winning frame with fixed-point re-evaluation plus atomic transaction boundary per `SYNC-DATA-036`, offline-bootstrap distribution without presence election, and the Disconnect-vs-Unpair contract.
- **This ADR does not own**: current implementation status/evidence/gaps/next step ([multi-device-sync.md](./multi-device-sync.md)); data-convergence semantics — baseline origin/content truth, publisher barrier/snapshot, envelope content categories (now `sync-baseline-wire-v1`/`chat-core-baseline-v1`/`topic-message-stable-block-order-v1`/`parent-order-frame-v1`, `SYNC-DATA-041`, `SYNC-CC-021`), watermark three-segment boundary, baseline publish serialization with watermark-monotonic/coverage gating and in-flight bootstrap protection, receiver merge including parent-scoped order-frame wire merge with logical `parent-membership clock` plus membership-clock-gated completeness and fixed-point re-evaluation plus atomic transaction boundary, baseline frame coverage, per-row `sortOrder` wire exclusion, strictly closed schema per `SYNC-DATA-033`–`SYNC-DATA-039`/`SYNC-DATA-041`–`SYNC-DATA-045` plus `SYNC-DATA-035`–`SYNC-DATA-036`/`SYNC-DATA-043`–`SYNC-DATA-044` normative append/fixed-point/transaction and `jcs-sha256-v1` digest input includes frames plus fixed inventory/manifest per `SYNC-DATA-032`/`SYNC-DATA-044` with JSON spelling not locked plus non-locking scope `SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021` (including `parent-membership clock` table/column/op/JSON not locked and no implementation), retention coverage, reconciliation, and convergence status ([sync-data-convergence.md](./sync-data-convergence.md), `SYNC-DATA-019`–`SYNC-DATA-045`); fallback candidate analysis ([sync-architecture-selection.md](./sync-architecture-selection.md)); application identity, updater/release freeze, platform scope ([cherry-chat-application-identity.md](./cherry-chat-application-identity.md)); SQLite chat authority and migration process ([sqlite-migration.md](./sqlite-migration.md)); context-window semantics ([context-window.md](./context-window.md)); initial snapshot/existing-data convergence content rules beyond the infrastructure duty stated here (separate sync-data problem owned by `SYNC-DATA-*` — see §12).
- **This ADR does not authorize**: protocol implementation, endpoint path strings, HTTP verb spellings, chunking/streaming, size thresholds, compression, retained physical history counts, lease/ack endpoints, timeouts, schema migrations, capacity thresholds, retained history version counts, retained physical old-baseline counts, old-baseline physical reclamation mechanics, GC mechanics, IPC changes, parent-scoped order-frame op names/endpoint/shared TS types/DB tables/wire frame payload JSON spelling per `SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021`, or any code change except the locked wire JSON spelling in §10A.1/`SYNC-DATA-040`/`SYNC-DATA-045`. Publish/fetch operation semantics and status codes are locked by `SYNC-CC-020` as a code-less contract; transport spellings and limits remain deferred, while inventory v1 **wire JSON spelling is locked** (outer envelope, `payload` root, manifest, entity/tombstone/order-frame/clock keys — lowerCamelCase fixed sets per §10A.1, F-02/F-05; `SYNC-DATA-041`–`SYNC-DATA-045` wire inventory is now byte-level per §10A.1, not logical-roles-only). It creates no permissions/admin/merge/GC/snapshot/WAN/TLS-management decisions beyond what is written here. Wire JSON spelling for inventory v1 baseline (`topic-message-stable-block-order-v1` at `sync-baseline-wire-v1`/`chat-core-baseline-v1`/`parent-order-frame-v1`) is locked per §10A.1 (outer `wireVersion`/`channelId`/`watermark`/`digestScheme`/`digest`/`payload`, `payload` root keys, manifest keys, entity/tombstone/order-frame/clock keys — lowerCamelCase fixed sets); remaining not locked: future operation names/endpoints/TS types beyond wire DTO/DB tables/storage (updated `SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021`, F-02/F-05).

## 3. Decision table (`SYNC-CC-*`)

| # | Decision | Status |
|---|---|---|
| **SYNC-CC-001** | Relay inherently supports multiple internal sync channels; current global single-channel behavior is a temporary implementation limitation, not a product constraint. Channels are hidden infrastructure: users never see channel IDs, lists, or management UX. | **Locked** |
| **SYNC-CC-002** | Each client belongs to at most one channel at a time; a channel supports 2+ devices. There is no channel merge: acceptance never merges two existing channels. | **Locked** |
| **SYNC-CC-003** | Relay service connection/registration and channel pairing are separate state machines with separate UI status. Service state is each client's observed relay attachment; pairing state is channel membership. Neither is derived from the other except where explicitly stated (Unpair requires connected relay, §8). | **Locked** |
| **SYNC-CC-004** | Service connection is explicit and user-initiated. First successful Connect registers the device and obtains a stable relay-scoped public device code plus a durable secret credential. Registered devices auto-reconnect after app/network recovery; relay unreachable/unavailable surfaces as Service disconnected. | **Locked** |
| **SYNC-CC-005** | Disconnect vs Unpair are distinct and never conflated. Disconnect stops service attachment/auto-reconnect but preserves registration and channel membership. Unpair requires a connected relay, atomically removes self membership, and preserves service connection and local chats. See §8. | **Locked** |
| **SYNC-CC-006** | Service connection is shown with an API Server-style red/green indicator. It reports each client's own observed relay state, not broadcast presence of other devices. Pairing state is shown separately. | **Locked** |
| **SYNC-CC-007** | Device code is a stable, relay-scoped, human-transcribable public identifier — not a secret and not an authorization credential. Pairing flow: requester enters the target's device code; the target explicitly accepts. No invite creation, no invite expiry, no founder/admin/revoke-other UX. | **Locked** |
| **SYNC-CC-008** | Pairing requests persist until accepted, rejected, requester-cancelled, or replaced by a new outgoing request; there is no automatic expiration. At most one outgoing request per device; retries and replacement are idempotent and fail-closed. | **Locked** |
| **SYNC-CC-009** | Channel formation/join rules: two unpaired devices accepted ⇒ relay atomically creates a channel and adds both. Unpaired requester to paired target ⇒ requester joins the target's channel. A paired requester cannot initiate another pairing; a paired target may accept additional devices. If the requester became paired before acceptance, acceptance fails. | **Locked** |
| **SYNC-CC-010** | User-visible pairing controls: unpaired ⇒ Request pairing; paired ⇒ Unpair. Pending states expose requester cancel and target accept/reject as necessary. No peer revoke and no permissions UI exist in the current model. | **Locked** |
| **SYNC-CC-011** | Unpair lifecycle: allowed only while the relay is connected; atomically removes self membership; never disconnects the service and never deletes local chats. If durable channel membership drops below two, the relay automatically dissolves the channel; the remaining member becomes unpaired on next relay observation. Offline/sleep/network loss/app or relay restart never changes membership. | **Locked** |
| **SYNC-CC-012** | Zombie channels/rows may remain and must never block users; garbage collection is deferred. | **Locked** |
| **SYNC-CC-013** | Reset without migration: current test-stage invite/founder/trusted state may be reset with no compatibility migration burden. Existing local chats remain local; old pairing state may be discarded for the new protocol. | **Locked** |
| **SYNC-CC-014** | Initial snapshot and existing-data convergence content rules are a separate sync-data problem owned by [sync-data-convergence.md](./sync-data-convergence.md) (`SYNC-DATA-*`) and are not owned by this ADR. This ADR governs connection/channel/pairing plus the relay baseline envelope storage/distribution infrastructure (`SYNC-CC-017`–`SYNC-CC-019`) only. | **Locked** |
| **SYNC-CC-015** | Infrastructure/authority boundary preserved: relay remains no-account/no-Web-admin store-and-forward infrastructure — including when it persists and serves baselines — and never merges or adjudicates business content; Main SQLite remains chat authority on each device. Docker bridge deployment and HTTP/HTTPS deployment choice remain external to channel logic. | **Locked** |
| **SYNC-CC-016** | Per-channel operation sequence and cursor are required. A global sparse filter over one shared sequence cannot preserve the current contiguous cursor contract; channel-scoped contiguous sequencing is an implementation requirement. This decides sequencing scope only, not snapshot semantics. | **Locked** |
| **SYNC-CC-017** | Relay per-channel current-effective baseline persistence as infrastructure: per channel the relay durably stores one current effective baseline blob plus schema/digest/watermark metadata alongside the per-channel operation log. The relay validates the protocol envelope and saves/distributes only; it never merges and never adjudicates business content. Baseline content truth and watermark/merge rules are owned by `SYNC-DATA-019`/`SYNC-DATA-021`–`SYNC-DATA-022`. | **Locked** |
| **SYNC-CC-018** | Offline bootstrap distribution without presence election: a joining device may fetch the channel's current effective baseline from the relay even when all prior devices are offline, then continue via per-channel cursor replay. Online-device temporary supply is not the main path; no presence tracking and no baseline source election are introduced. | **Locked** |
| **SYNC-CC-019** | Relay serialized baseline publish with coverage-gated replace and in-flight bootstrap retention as infrastructure: per channel, baseline publishes serialize so replacement is decided one candidate at a time. The relay rejects a candidate that fails the `SYNC-DATA-023` gate — watermark not relay-continuously-persist-confirmed, watermark below the current effective baseline watermark in non-`empty` state (`N >= current` monotonic floor, `409`; the recoverable floor/coverage gate is an additional condition and never substitutes for this floor), or candidate not covering the compacted prefix plus required tombstone/delete evidence — with the effective baseline unchanged. Atomic replacement never removes the N+1 continuous replay/repair path of any successfully issued baseline before its bootstrap completes or explicitly fails: until completion or explicit failure the relay retains sufficient baseline/log/delete evidence, or provides an equivalent recoverable mechanism, per `SYNC-DATA-024`. Baseline content/coverage truth is owned by `SYNC-DATA-023`–`SYNC-DATA-024`; lease/ack endpoints, timeouts, retained physical old-baseline counts, and GC/reclamation mechanics remain deferred and are not decided here. | **Locked** |
| **SYNC-CC-020** | Relay single-stage client-declared publish/fetch wire contract as infrastructure (code-less, target-only): one publish operation per candidate with no relay-issued fence, token, or prepare handshake, per `SYNC-DATA-025`. The relay enforces authentication plus membership, structure/version/digest checks including `SYNC-DATA-032` `jcs-sha256-v1` recompute with scheme/version match (match proves byte-identity only, never semantic truth nor correspondence to N), N relay-continuously-persist-confirmed for that channel and not above the current relay head plus the `SYNC-DATA-023` monotonic floor (`N >= current` in non-`empty` state, below-current is `409`), and the serialized `SYNC-DATA-023` gate; it never verifies payload semantic truth or correspondence to N. States are `empty` versus `current baseline` with atomic replace on success: idempotent `200` only on full match with the current effective baseline (channel binding, N, digest scheme/version, digest, canonical logical payload); same-N digest/payload divergence, below-current watermark, or other watermark/overwrite conflict is `409`; a replaced-candidate retry is stale `409` with no permanent cross-replacement fingerprint; structure/version/digest invalid is `400`; service-level auth follows existing `401` semantics and device-credential/membership refusal is `403`; oversize is `413`; store failure is `500`; error bodies are `{error}`. The relay atomically binds the stored payload with digest scheme/version, digest, channel binding, watermark N, and schema/inventory versions per `SYNC-DATA-032`. Empty fetch returns `404` `{error:'baseline-not-found'}` as a legitimate empty state, not a routing error. Fetch serves the current envelope only to authenticated members; SSE stays hint-only without the blob. Endpoint path strings, HTTP verb spellings, chunking/streaming, size thresholds, compression, retained physical history counts, lease/ack/timeouts/GC, and SSE event names stay deferred and are not decided here. | **Locked** |
| **SYNC-CC-021** | Wire inventory v1 envelope & binding with locked JSON hierarchy & spelling for `sync-baseline-wire-v1` (target-only, code-less; `F-01`/`F-02`/`F-05`): the outer envelope strictly closed lowerCamelCase keys `{ wireVersion: "sync-baseline-wire-v1" (only outer, never inside `payload`/`payload.manifest`/JCS input), channelId (opaque, auth-derived `SYNC-CC-*` channel binding; no `deviceId` key, never trust body `deviceId`; body `channelId` if present must equal authenticated channel or `400`/`403`), watermark: safe-int `N`, digestScheme: "jcs-sha256-v1", digest: 64 lower-hex of `payload` JCS, payload: object }` — `channelId`/`watermark`/`digest` live **exclusively** outer — never inside `payload` nor inside `jcs-sha256-v1` input (input is only `payload` object). The `payload` object (`payload` value) entering `jcs-sha256-v1` is exactly the `SYNC-DATA-042`–`SYNC-DATA-044` versioned inventory with locked lowerCamelCase keys per §10A.1: `payloadSchema`/`inventoryVersion`/`orderFrameVersion`/`scope` literal + `topics`/`messages`/`messageBlocks`/`tombstones`/`orderFrames`/`manifest` (strictly closed per `SYNC-DATA-039`/`SYNC-DATA-045`), where `payload.manifest` echoes the three content versions (`payloadSchema`/`inventoryVersion`/`orderFrameVersion`) and fixed `liveCounts`/`tombstoneCounts`/`frameCounts`/`completeness:"complete"`; the four-value `wireVersion`+triple echo contradiction is deleted (F-01): `wireVersion` only outer, triple only inside `payload.manifest`. Type/value domain per `SYNC-DATA-042`/`SYNC-DATA-043` (F-03): `id`/`topicId`/`messageId`/`parentId`/`entityId`/`orderedChildIds[]` non-empty valid Unicode scalar strings; `name`/`assistantId`/`createdAt`/`updatedAt`/`deletedAt`/`role`/`content`/`status`/`askId`/`model`/`modelId`/`type` are `string|null` valid Unicode scalar when non-null; `pinned`/`isNameManuallyEdited` `boolean|null`; time fields `string|null` verbatim I-JSON not parsed/normalized/not LWW; `status`/`type` gate unchanged; all keys fixed existence. `parentMembershipClock: {timestamp, operationId}` required per `message`/`messageBlock`. Tombstone `{entityType: "topic"|"message"|"messageBlock", entityId, deletionClock: {timestamp, operationId: string|null (legacy)}, survivingEntityClock}` with legacy `null` strong barrier `(T,null) > (T,nonNull)` suppressing all equal-T live ops, two `null` equivalent, new tombstone `operationId` non-empty non-forge (F-05). `orderFrames` with fixed keys `{frameVersion: "parent-order-frame-v1", kind: "topicMessage"|"messageBlock", parentId, orderedChildIds: string[], frameClock: {timestamp, operationId}}` per `SYNC-DATA-044` F-04: every live included `topic`/`message` exactly one winning frame including empty `[]` when zero children, tombstoned/excluded parent no frame, `orderedChildIds` exactly the inventory-included live children via `SYNC-DATA-035` coverage/append; transient excluded incomplete → must not claim `complete`/not publish. Manifest counts/`completeness` must be **uniquely recomputed** on relay publish and client apply from validated `payload` plus parent closure/membership clocks/frame coverage; declared mismatch is `400`/fail-closed even if digest self-consistent (F-05). Array determinism follows `SYNC-DATA-044` over the locked `payload` (§10A.1): `topics`/`messages`/`messageBlocks` each by `id` UTF-8 byte lex, `fieldClocks` keys JCS-sorted, `tombstones` by type rank then `entityId`, `orderFrames` by `kind` rank then `parentId`, `orderedChildIds` preserve business order; duplicate/invalid fails closed. Manifest fixed input per `SYNC-DATA-044` with literal `scope: "chat-core-baseline-v1:topic-message-stable-block-order-v1"`; pending outbox/observed cursor/channel/observationBinding/unversioned/excluded/orphan/aggregate diagnostics/reasons/digest are excluded from manifest/digest; outer `wireVersion`/`channelId`/`watermark`/`digestScheme`/`digest` are binding but not JCS input. Updated non-locking scope `SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021` (F-02/F-05): inventory v1 baseline JSON spelling (outer envelope/`payload` root/manifest/entity/tombstone/order-frame/clock) is now **locked** (order-frame JSON keys locked in v1); remaining not locked: future operation names/endpoints, path strings/HTTP verbs, TS types beyond wire DTO, SQLite tables/columns beyond wire projection, storage/GC. Current code not yet wire-compliant (pre-lock spelling) — target-only/no implementation authorization. | **Locked** |
| **SYNC-CC-022** | Relay baseline resource transport binding plus per-channel single-current persistence mapping (reference-relay implemented 2026-09-11): `PUT /sync/baseline` publishes one candidate per call with idempotent replace; `GET /sync/baseline` fetches the caller's channel current effective envelope. Request and success response bodies are directly the locked `SyncEnvelope` JSON (`sync-baseline-wire-v1` outer `{wireVersion, channelId, watermark, digestScheme, digest, payload}` with `payload`-only `jcs-sha256-v1` outer `digest`, per `SYNC-DATA-027`/`SYNC-DATA-032`/`SYNC-CC-021`). Persistence: one current row per channel in relay SQLite (`sync_channel_baselines`, schema `cc-2` purely additive over `cc-1` — devices/channels/memberships/requests/operations untouched), decided and written inside one synchronous per-channel transaction (serialized decision, no split-brain), surviving relay restart on the same DB. Auth follows the existing channel data plane: missing Bearer `401 {error:'unauthorized'}`; bad device credential `403`; unpaired `403 {error:'pairing-required'}`; envelope `channelId` not equal to the authenticated channel `403 {error:'channel-mismatch'}` (GET takes no channel parameter and never serves another channel). PUT decision per `SYNC-DATA-023`/`SYNC-DATA-028`/`SYNC-DATA-029` and `SYNC-CC-019`/`SYNC-CC-020`: strict envelope validation by the shared `baselineWire` parser/validator (exact keys/versions/closure/manifest-recompute/duplicate-key rejection on raw text) else `400 {error:'invalid-envelope'}`; `jcs-sha256-v1` recompute over the received `payload` else `400 {error:'digest-mismatch'}` (match proves byte-identity only, never semantic truth nor correspondence to N); `N` above the relay channel head `400 {error:'watermark-above-head'}`; `empty` state accepts the first relay-confirmed N; `N < current` is `409`; `N == current` with full match (channel binding, N, digest scheme, digest, canonical logical payload) is idempotent `200`, same-N divergence is `409`; `N > current` replaces when the coverage gate holds — with no compaction and the full op log retained the gate reduces to `N <= head` (no history-count/GC mechanics invented); every `409` carries `{error:'baseline-conflict'}` and a replaced-candidate retry is stale `409` with no permanent cross-replacement fingerprint; store failure is `500 {error:'store-unavailable'}`; GET on `empty` returns `404 {error:'baseline-not-found'}`. No size threshold / `413` on this resource (still deferred); publisher barrier, receiver bootstrap/apply, remote frame ops (except the `topicMessage` incremental `order_frame` now carried under `SYNC-CC-024`/`SYNC-DATA-048`), compression, lease/ack, and GC remain deferred and unchanged. This narrows the `SYNC-CC-020`/§17 deferred transport list for this resource only; every other deferred item and every other Locked row is unchanged. | **Locked** |



| **SYNC-CC-023** | Receiver-first baseline bootstrap fetch trigger plus GET 200/404 plus N+1 handoff (implemented 2026-09-11; endpoint/path/verb/body/status spelling stays locked by `SYNC-CC-022`/`SYNC-DATA-046` and is not relocked here): trigger is strictly registered/attached plus locally channel-bound plus strict persisted cursor==0, evaluated after auth/channel/cursor preflight and before any ordinary push/pull in the same `sync()` cycle; cursor>0 never GETs; a channel-switch reset to 0 may trigger on a later cycle. Transport is `GET /sync/baseline` with the existing device-header auth (plus Bearer when configured), abort/shutdown/config-generation managed like push/pull. `404` with the strict `{error:'baseline-not-found'}` single-key body is the legitimate empty state and continues with the existing push/pull unchanged; any other 404 body (HTML, empty, other error, extra keys) throws via the existing safe relay mapping and never masquerades as no-baseline. `200` serves the caller's channel current envelope only (never another channel); other non-2xx retain the relay `{error}` via the existing safe mapping. After a committed bootstrap at watermark N the client pulls from N (`N+1` onward) per `SYNC-DATA-007`/`SYNC-DATA-021`; `N==0` is legal (cursor stays 0, later cycles may repeat GET with idempotent apply, then pull from 0 gets seq1 — efficiency tradeoff, no bootstrapped flag); a post-commit pull failure retains cursor=N for next-cycle recovery and never rolls back N. Sender-side barrier (`SYNC-DATA-026`) is out of scope here. Every other Locked row is unchanged. | **Locked** |

| **SYNC-CC-024** | Incremental `topicMessage` order_frame transport and storage (implemented 2026-09-11; narrows the §17 deferred transport list for this op only): the `SYNC-DATA-048` `order_frame` op (`entityType: 'topic'`, `entityId === payload.parentId`, strictly closed `parent-order-frame-v1` payload with envelope `id`/`timestamp` mirroring `frameClock`, `deviceId` envelope-only) travels on the existing channel data plane with no new endpoint and no relay schema migration — `POST /sync/push` persists it into `sync_channel_operations` and `GET /sync/pull` replays it byte-identically alongside upsert/delete under the same per-channel contiguous seq/cursor contract. Relay ingress enforces the shared strict validator (legal accepted; illegal `400`; divergent id collision `409` with the current row unchanged; identical replay idempotent `200` with no cursor growth); the `SyncClient` pull validator enforces the same rule before any apply. Old clients meeting the new op fail closed on their existing validator (accepted forward contract). No baseline `wireVersion` change, no size/threshold change, no lease/ack, no GC change. Compound/unsupported paths and `messageBlock` frames stay deferred and never ride this op. Every other Locked row is unchanged. | **Locked** |

## 4. Definitions

- **Relay service connection (attachment)**: whether this client is currently attached to the configured relay service and auto-reconnecting when registered. Observed locally per client.
- **Registration**: durable relationship between one device and one relay, established on first successful Connect: a stable relay-scoped public device code plus a durable secret credential held by the client.
- **Device code**: stable, relay-scoped, human-transcribable public identifier for a registered device. Routable by transcription, not a secret, not an authorization credential.
- **Secret credential**: durable per-device authorization material obtained at registration; never displayed, never transcribed, never used as an identifier.
- **Channel**: hidden relay-side grouping of 2+ devices whose operations replicate together. Users never see channels directly; they see paired/unpaired plus pending states.
- **Membership**: durable relay-side record binding a registered device to a channel.
- **Pair request**: durable directed intent from a requester device to a target device code, resolved by target accept/reject, requester cancel, or replacement.
- **Service disconnected**: the client's observed state when the relay is unreachable/unavailable, regardless of registration or membership durability.

## 5. Service connection and registration state machine

States (per client, observed locally):

- `Unregistered` → user clicks Connect → connecting → on success: `Connected + Registered` (device code + secret stored durably).
- `Connected` ⇄ `Service disconnected`: transport/app/network loss moves to `Service disconnected`; registered devices auto-reconnect without user action. Registration and membership persist across these transitions.
- User clicks Disconnect from `Connected` or `Service disconnected` → `Disconnected (registered, membership preserved)`: attachment and auto-reconnect stop; registration (device code + secret) and channel membership are preserved. A later Connect re-attaches with the same registration.
- Registration is never silently re-created: re-registration (new device code/credential) is at most an explicit recovery path defined at implementation time, not a background fallback.

## 6. Channel pairing state machine

States (per client, membership as observed via relay):

- `Unpaired` → user initiates Request pairing (enters target device code) → `Outgoing pending` (at most one per device).
- Target observes `Incoming pending` → accepts or rejects.
- Resolution:
  - Both unpaired at acceptance ⇒ relay atomically creates a channel and adds both.
  - Requester unpaired, target paired at acceptance ⇒ requester joins the target's channel.
  - Requester already paired at acceptance time ⇒ acceptance fails; no merge, no partial join.
  - Paired requester cannot initiate a new request (client refuses before any relay call).
- `Paired` → user clicks Unpair (only while relay connected) → relay atomically removes self membership → `Unpaired`. Service stays connected; local chats untouched.
- Membership durability: offline/sleep/network loss/app restart/relay restart never create, move, or delete membership. Only Unpair (self-removal) and automatic dissolve after membership drops below two change it.

## 7. Combined UI status and action table

Service status and pairing status are always shown separately. Allowed actions:

| Service | Pairing | Primary action(s) | Notes |
|---|---|---|---|
| Unregistered / Disconnected | Unpaired, no pending | **Connect**; **Request pairing** disabled until connected | Pairing requires service attachment |
| Connected | Unpaired, no pending | **Request pairing**; **Disconnect** | Normal start state |
| Connected | Outgoing pending | **Cancel request**; **Disconnect** | Request persists until resolved |
| Connected | Incoming pending | **Accept** / **Reject**; **Disconnect** | Target decision |
| Connected | Paired | **Unpair**; **Disconnect** | Unpair keeps service connected |
| Service disconnected | Any pairing state | **Connect** (re-attach); **Disconnect** (stop auto-reconnect) | No Request/Accept/Reject/Unpair while disconnected |
| Disconnected (registered) | Any pairing state | **Connect** | Registration + membership preserved |

Control labels are fixed: service control is Connect/Disconnect; pairing controls are Request pairing/Unpair plus pending Cancel/Accept/Reject. No founder/admin/revoke-other/permissions controls exist.

## 8. Disconnect vs Unpair (normative)

- **Disconnect**: stops service attachment and auto-reconnect. Preserves registration (device code + secret) and channel membership. Does not delete local chats. Available in every service state. Reconnect restores sync without re-pairing.
- **Unpair**: requires a connected relay (`Connected`). Atomically removes self membership on the relay. Preserves service connection (client stays attached and addressed by its device code) and preserves all local chats. If durable membership drops below two, the relay automatically dissolves the channel; the remaining member becomes unpaired on its next relay observation (it is not pushed a presence event as authority).
- Implementations must not merge these: Disconnect must never remove membership; Unpair must never disconnect the service or wipe local data.

## 9. Protocol responsibilities (behavioral, not endpoint implementation)

The relay is responsible for, at minimum:

1. Durable device registration: issue a stable relay-scoped public device code plus a durable secret credential on first Connect; authenticate later attachment by the secret, never by the device code alone.
2. Durable pair-request lifecycle: one outgoing request per device; persistence until accepted/rejected/cancelled/replaced; idempotent retry/replacement; fail-closed on conflict (paired requester initiation refused; late acceptance after requester paired fails).
3. Atomic channel operations: create-channel-and-add-both, join-existing-channel, and remove-self-membership execute atomically; no partial membership and no merge path.
4. Automatic dissolve: when durable membership drops below two, dissolve the channel; the remaining member resolves to unpaired on next observation.
5. Per-channel operation sequencing and cursors (§10) scoped to channel members only; devices outside the channel never observe the channel's operations.
6. Truthful failure semantics: unauthenticated/unregistered/unpaired access fails closed with an explicit reason; unreachable relay surfaces as Service disconnected with pending work retained client-side (pending-work retention itself is existing sync-data behavior, not decided here).
7. Per-channel current-effective baseline envelope persistence and distribution (§10, `SYNC-CC-017`): one current effective baseline blob plus schema/digest/watermark metadata per channel, envelope validation with save/distribute only, serialized coverage-gated atomic replacement and in-flight bootstrap retention per `SYNC-CC-019` (`SYNC-DATA-023`–`SYNC-DATA-024`), single-stage publish/fetch operation semantics plus status codes per `SYNC-CC-020` (`SYNC-DATA-025`/`SYNC-DATA-028`–`SYNC-DATA-030`), and offline fetch availability per `SYNC-CC-018`. Baseline content truth, publisher barrier/snapshot, watermark proof, receiver merge, and retention coverage are owned by `SYNC-DATA-*` and are not decided here.

Endpoint path strings, HTTP verb spellings, chunking/streaming, size thresholds, compression, retained physical history counts, lease/ack endpoints, timeouts, and SSE event names are implementation decisions under a later activated step, except the baseline resource binding locked by `SYNC-CC-022` (`PUT`/`GET /sync/baseline`, body exact envelope, per-channel single current row, status/body spelling); this ADR otherwise constrains only the behaviors above plus the `SYNC-CC-020` operation semantics and status codes.

## 10. Internal data model requirements

Any implementation must durably represent, at minimum:

- **Devices**: stable device identity per registration: public device code (relay-scoped, stable, transcribable) bound to a secret credential (durable, never exposed as identifier).
- **Channels**: internal channel records; hidden from users.
- **Membership**: durable device→channel bindings; at most one channel per device.
- **Pair requests**: directed requester→target records with lifecycle `pending → accepted | rejected | cancelled | replaced`; at most one outgoing `pending` per requester; no expiry timestamp semantics.
- **Channel operations**: sync payload log partitioned by channel.
- **Per-channel sequence/cursor**: each channel has its own contiguous operation sequence and cursor. Rationale (SYNC-CC-016): the current sync contract relies on a contiguous cursor; multiplexing independent channels onto one global sequence with sparse per-device filtering breaks contiguity for each observer. Therefore channel-scoped contiguous sequencing is required. This is an implementation requirement on sequencing scope; it does not decide snapshot, retention, or GC policy.
  - **Channel baselines**: per channel, one current effective baseline blob plus outer `wireVersion`/`channelId`/`watermark`/`digestScheme`/`digest` + `payload` (SYNC-CC-017/021). The `payload` blob's logical payload deterministically includes parent-scoped order frames for every inventory-within parent per `SYNC-DATA-033`/`SYNC-DATA-037` and per `SYNC-DATA-032` digest which is JCS over **only** the `payload` object with outer `digest` key `digest` (locked spelling per §10A.1, input never includes outer envelope; outer `wireVersion` only outer, triple only inside `payload.manifest`, F-01/`SYNC-DATA-041`). Inventory v1 wire JSON hierarchy & lowerCamelCase key spelling is locked (§10A.1, F-02; outer `{wireVersion, channelId, watermark, digestScheme, digest, payload}`, `payload` root `{payloadSchema, inventoryVersion, orderFrameVersion, scope, topics, messages, messageBlocks, tombstones, orderFrames, manifest}`, manifest `{payloadSchema, inventoryVersion, orderFrameVersion, scope, liveCounts, tombstoneCounts, frameCounts, completeness}`, entity/tombstone/order-frame/clock keys as there — strictly closed; column mapping deferred), logical `parentMembershipClock: {timestamp, operationId}` per `SYNC-DATA-035` is versioned logical state (wire JSON key `parentMembershipClock` locked, column mapping deferred) and persistent per-parent winning frame with fixed-point re-evaluation plus atomic transaction boundary per `SYNC-DATA-036`, offline-bootstrap distribution without presence election. Type/value domain per `SYNC-DATA-042`/`SYNC-DATA-043` (F-03): `id`/`topicId`/`messageId` non-empty valid Unicode scalar strings; `name`/`assistantId`/`createdAt`/`updatedAt`/`deletedAt`/`role`/`content`/`status`/`askId`/`model`/`modelId`/`type` `string|null` (time strings verbatim I-JSON, not LWW); `pinned`/`isNameManuallyEdited` `boolean|null`; all keys fixed existence. Tombstone `{entityType, entityId, deletionClock: {timestamp, operationId: string|null (legacy strong barrier (T,null) > (T,nonNull))}, survivingEntityClock}` with new `operationId` non-empty non-forge (F-05). Order-frame `{frameVersion: "parent-order-frame-v1", kind: "topicMessage"|"messageBlock", parentId, orderedChildIds, frameClock}` with parent/frame full-set per `SYNC-DATA-044` (F-04): each live included `topic` exactly one `topicMessage` frame, each live included `message` exactly one `messageBlock` frame including `orderedChildIds: []` when zero children; tombstoned/excluded parent no frame; `orderedChildIds` exactly the inventory live children via `SYNC-DATA-035` coverage/append; transient excluded incomplete → must not publish `complete`. Manifest counts recomputed per validated `payload` plus closure/membership/frame coverage on relay publish & client apply; declared mismatch `400`/fail-closed even if digest self-consistent (F-05). Per-row `message.sortOrder`/`messageBlock.sortOrder` excluded from wire; JCS array determinism over locked `payload` per `SYNC-DATA-044` (§10A.1). Topic ordering not introduced (only `message`/`block` frames locked).


## 11. Lifecycle and error semantics

- Registration, membership, requests, channels, and per-channel sequences survive app restart and relay restart on the same relay DB/credential inputs.
- Offline/sleep/network loss/app or relay restart never changes registration, membership, or request outcome — except that a pending request may still be cancelled/replaced by its requester or accepted/rejected by its target when connectivity returns.
- Relay unreachable ⇒ client shows Service disconnected; local chats remain fully usable; pending sync work behavior follows the existing sync-data contract (unchanged by this ADR).
- Fail-closed matrix: unknown/expired credential ⇒ re-attach refused, surfaced truthfully (no silent re-registration); unpaired device ⇒ sync data path refused with explicit reason; paired-requester initiation ⇒ refused client-side; late acceptance after requester paired ⇒ fails with no membership change; Unpair while disconnected ⇒ refused (must Connect first).

## 12. Non-goals and boundaries (not owned by this ADR)

- Initial snapshot and existing-data convergence content rules (what historical state a newly paired device receives, publisher barrier/snapshot, envelope content categories, watermark proof with monotonic/coverage gating, in-flight bootstrap protection, receiver merge including parent-scoped order-frame merge with logical `parentMembershipClock` plus membership-clock-gated completeness and fixed-point re-evaluation plus atomic transaction boundary per `SYNC-DATA-035`/`SYNC-DATA-036` and atomic dense materialization, baseline frame coverage with parent/frame full-set (F-04, empty frames, `orderedChildIds` exactly inventory live children, transient incomplete not `complete`), per-row `sortOrder` wire exclusion, `jcs-sha256-v1` digest input is **only** the outer `payload` object with outer `digest` key `digest` per `SYNC-DATA-032` (input never includes outer envelope; outer `wireVersion` only outer, triple only inside `payload.manifest`, F-01), strictly closed schema with fixed lowerCamelCase key spelling for inventory v1 per §10A.1/`SYNC-DATA-040`/`SYNC-DATA-045` (F-02; order-frame keys locked in v1; `parentMembershipClock` wire key locked, column mapping deferred), retention coverage, reconciliation) are a separate sync-data problem owned by [sync-data-convergence.md](./sync-data-convergence.md) (`SYNC-DATA-019`–`SYNC-DATA-045`, including `SYNC-DATA-033`–`SYNC-DATA-039` frame semantics, `SYNC-DATA-035`–`SYNC-DATA-036` normative append/fixed-point/transaction plus `SYNC-DATA-041`–`SYNC-DATA-045`/§10A.1 wire JSON spelling lock and F-03 type/domain + F-05 tombstone strong barrier/recompute). This ADR assumes a working per-channel operation stream plus relay-persisted current-effective baseline distribution exists; it does not define backfill content, barrier orchestration, watermark proof, merge (including membership clock), retention coverage, or conflict-resolution behavior beyond the locked wire `payload` shape. Future operation names/endpoints/TS types beyond wire DTO/DB mapping remain deferred per updated `SYNC-DATA-040`; topic ordering is not introduced (only `message`/`block` frames are locked).
- No founder/admin/permission/revoke-other model is created. No channel merge path exists. No per-user multi-group management UX beyond one channel per client is created.
- WAN exposure, TLS/certificate management, Docker image operations, backup/rotation, capacity/SLA, E2EE, attachments, and compound-operation coverage are unchanged by this ADR and remain under their existing governance.

## 13. Rejected alternatives (stable decisions)

- **Global single channel as an inherent relay constraint — rejected** (SYNC-CC-001). The relay inherently supports multiple internal sync channels; treating one global channel as a product constraint would leak other users' devices into every user's sync scope. Channels are hidden infrastructure, never user-visible.
- **Invite/founder/admin/revoke-other model — rejected** (SYNC-CC-007, SYNC-CC-010). Pairing is a direct device-code request plus explicit target accept, with no invite creation or expiry, no founder privilege, no admin role, and no revoking other devices. Rationale: the pairing set is the user's own devices, so there is no other party to administer.
- **Presence-based membership and dissolve — rejected** (SYNC-CC-011). Offline, sleep, network loss, and app or relay restart never create, move, or delete membership. Dissolve follows only from durable membership dropping below two, observed on next relay contact — never from transient unreachability.
- **Online-device temporary supply as the main bootstrap path and presence/source election — rejected** (SYNC-CC-018). The main offline path is the relay-persisted current effective baseline; no presence tracking and no baseline source election are introduced.
- **Relay merge/adjudication of baseline business content — rejected** (SYNC-CC-015, SYNC-CC-017). The relay validates the envelope and saves/distributes; chat truth stays in per-device Main SQLite under `SYNC-DATA-*` semantics.
- **Unserialized or coverage-less baseline replacement and severing in-flight bootstraps — rejected** (SYNC-CC-019). Replacement without per-channel serialization, without the `SYNC-DATA-023` watermark-monotonic (`N >= current` in non-`empty` state, below-current is `409`)/coverage gate, or by removing an issued baseline's N+1 path before completion or explicit failure is not the infrastructure behavior. Full log retention is the minimal path with no compaction, not a never-compact lock. Per-row `sortOrder` wire merge is not the infrastructure mechanism; ordering converges via parent-scoped frames per `SYNC-DATA-033`–`SYNC-DATA-038`, and strictly closed unknown fields/versions fail closed per `SYNC-DATA-039`.
- **Relay-issued fence/token/prepare handshake — rejected** (SYNC-CC-020, SYNC-DATA-025). Publish is single-stage client-declared; no pre-publish grant exists.
- **Channel merge — not selected** (SYNC-CC-002, SYNC-CC-009). Acceptance never merges two existing channels: a paired requester cannot initiate, and late acceptance after the requester became paired fails with no membership change. Multi-group or merge behavior is deferred, not designed here.
- **Relay Web administration plane — rejected** (SYNC-CC-015). The relay remains no-account store-and-forward infrastructure with no user accounts, passwords, or web management plane; the client pairing UI is the only control plane.

## 14. Security and privacy

- The device code is public within the relay scope by design (transcribed by the user) and carries no authorization power. Authorization derives only from the durable secret credential plus channel membership.
- Secrets are never displayed, logged, or transcribed; evidence and logs carry counts/statuses only, never codes, credentials, paths, content, or raw sizes.
- The relay remains no-account/no-Web-admin infrastructure: no user accounts, no passwords, no web management plane are introduced by channel semantics.
- Main SQLite remains the chat authority; the relay remains store-and-forward and never owns chat truth, including when it persists and serves per-channel current-effective baselines.

## 15. Reset and no-migration decision (SYNC-CC-013)

Superseded invite/founder/trusted pairing state carries no compatibility burden: it may be reset. Existing local chats remain local and are never deleted or moved by the reset; only obsolete pairing state is discarded for the registration/channel protocol. Deployment documentation describes implementation; this ADR makes no claim about what any relay deployment runs — it defines the target only.

## 16. Conformance requirements

An implementation conforms to this ADR only when its observed behavior satisfies, at minimum:

1. Explicit Connect registers once (stable code + durable secret); reconnects are automatic; relay-down shows Service disconnected.
2. Disconnect preserves registration/membership; re-Connect resumes without re-pairing.
3. Device-code request → target accept pairs two unpaired devices into a hidden channel; unpaired→paired request joins the target channel; paired requester cannot initiate; late acceptance after requester paired fails with no merge.
4. Pending request lifecycle (accept/reject/cancel/replace, no expiry, single outgoing, idempotent retry) behaves per §6/§9.
5. Unpair requires connected relay, removes only self, keeps service connected and local chats intact; sub-two membership dissolves the channel with the survivor resolving to unpaired.
6. Per-channel contiguous sequence/cursor holds independently per channel; one channel's traffic is never observable from another channel.
 7. Per-channel current-effective baseline envelope holds: one effective baseline blob plus schema/digest/watermark metadata per channel with serialized monotonic (`N >= current` in non-`empty` state, below-current `409`; coverage never substitutes)/coverage-gated atomic replacement, envelope validation with save/distribute only (`jcs-sha256-v1` recompute proving byte-identity only over the **outer `payload` object** per `SYNC-DATA-032`/`SYNC-DATA-044` (only `payload` is input; outer `wireVersion`/`channelId`/`watermark`/`digestScheme`/`digest` excluded and bound; `F-01`/`F-05`) including order frames in digest (wire `payload` root / manifest / entity / tombstone / order-frame / clock lowerCamelCase key spelling locked for inventory v1 per `SYNC-DATA-040`/§10A.1 (outer `wireVersion`/`channelId`/`watermark`/`digestScheme`/`digest`/`payload`; order-frame keys locked in v1; `F-02`); logical `parentMembershipClock: {timestamp, operationId}` per `SYNC-DATA-035` is versioned logical state (wire JSON key `parentMembershipClock` locked, column mapping deferred) and no implementation yet), atomic envelope binding; per-row `sortOrder` excluded from wire field inventory per `SYNC-DATA-038` while baseline logical payload carries parent-scoped order frames per `SYNC-DATA-037`), membership-clock-gated completeness per `SYNC-DATA-035` with parent/frame full-set per `SYNC-DATA-044` (every live included `topic`/`message` exactly one frame including empty `[]`; `orderedChildIds` exactly inventory live children; F-04) (`membershipClock <= frameClock` not listed is incomplete → `400`/blocked/repair-required; `membershipClock > frameClock` deterministic append by `membershipClock` then `childId`; missing/incomparable fail-closed; duplicate first occurrence retained), per-parent persistent winning frame with fixed-point re-evaluation and atomic dense `0..n-1` materialization plus atomic transaction boundary per `SYNC-DATA-036` (entity/tombstone/`parent-membership`, winning-frame, all affected parents' complete dense sort, applied/cursor one commit; `reparent` both sides atomic; otherwise pending/invisible full rollback), in-flight bootstrap N+1 replay/repair retention per SYNC-CC-019, single-stage publish/fetch operation semantics plus idempotent-`200`-only-on-full-current-match / stale-`409`-without-permanent-fingerprint / `400`/`401`/`403`/`404` (`{error:'baseline-not-found'}` on empty)/`409` (same-N divergence, below-current watermark, or overwrite conflict; incomplete frame per `SYNC-DATA-035` is `400`/blocked/repair-required, plus `400` for strictly closed unknown field/version per `SYNC-DATA-039`)/`413`/`500` with `{error}` bodies and member-only fetch plus hint-only SSE per SYNC-CC-020, and offline fetch availability without presence/source election. Per-row `sortOrder` is local dense `0..n-1` projection derived from frames per `SYNC-DATA-036` and does not rewrite SQLite governance; topic ordering is not introduced (only `message`/`block` frames are locked). Frame wire spelling and `parent-membership clock` storage/JSON spelling remain deferred per `SYNC-DATA-040`.
8. Zombie rows never block Connect/pairing/sync user flows.

Conformance is a property of observed behavior against the statements above. This ADR does not prescribe test shape, evidence form, or implementation sequence.

## 17. Deferred items and consequences

- Channel garbage collection (reaping dissolved/empty/zombie rows): deferred. Consequence: orphan rows may accumulate; they must never block users. A later decision may define reaping without changing user-visible semantics.
- Multi-group per client, merge, delegation/admin, and recovery UX (device loss, credential rotation): deferred. Consequence: one channel per client, no merge, no admin — loss/recovery stays manual until a later decision.
- Snapshot/backfill content, publisher barrier/snapshot, envelope content categories, watermark proof with monotonic/coverage gating, in-flight bootstrap protection, receiver merge including parent-scoped order-frame merge (`SYNC-DATA-033`–`SYNC-DATA-036`) plus baseline frame coverage (`SYNC-DATA-037`), per-row `sortOrder` wire exclusion (`SYNC-DATA-038`), strictly closed schema (`SYNC-DATA-039`), retention coverage, and reconciliation: owned by `SYNC-DATA-019`–`SYNC-DATA-040` (frame wire spelling/storage mapping remains deferred per `SYNC-DATA-040`; topic ordering not introduced, only `message`/`block` frames are locked). Consequence: pairing a fresh device plus the relay baseline infrastructure does not by itself implement what history it receives; that behavior still requires its own explicitly activated step.
- Prior-version baseline physical reclamation mechanics, retained physical old-baseline counts, endpoint path strings, HTTP verb spellings, chunking/streaming, size thresholds, compression, capacity thresholds, retained history version counts, lease/ack endpoints, timeouts, SSE event names, and GC mechanics: deferred; future frame operation names/shared TS types beyond wire DTO/DB tables mapping beyond wire DTO remain deferred (frame logical semantics **and inventory v1 wire JSON spelling for outer envelope/`payload` root/manifest/entity/tombstone/order-frame/clock per §10A.1** locked per `SYNC-DATA-033`–`SYNC-DATA-039` + `SYNC-DATA-040`/`SYNC-DATA-044`/`SYNC-DATA-045` (F-02; order-frame JSON keys locked in v1); topic ordering not introduced, only `message`/`block` frames are locked). Consequence: only the single-current-effective-baseline replacement semantic with SYNC-CC-019 serialized gating and in-flight retention plus the SYNC-CC-020 operation semantics and status codes plus parent-scoped order-frame wire logical semantics **plus inventory v1 wire JSON spelling** per `SYNC-DATA-033`–`SYNC-DATA-045`/§10A.1 is locked; how old blobs are reclaimed, how completion/failure is signalled, which transport/JSON spellings beyond v1 and limits apply, and how many physical old baselines are kept stays undecided (and current code remains not wire-compliant with pre-lock spelling — target-only), except the baseline resource transport binding now locked by `SYNC-CC-022` (`PUT`+`GET /sync/baseline`, body exact envelope, per-channel single current row, status/body spelling) which is implemented on the reference relay, plus the receiver-first bootstrap fetch trigger/GET200/404/N+1 handoff now locked by `SYNC-CC-023` (client-side, implemented); all other transport/limit/GC items stay deferred.

## 18. ADR change policy

- **Status**: Approved and fixed. This ADR is the authoritative owner of target connection/registration/channel/pairing semantics (`SYNC-CC-*`). It stays unchanged while the design is unchanged.
- **Living state lives elsewhere**: current implementation, evidence, gaps, and next step belong exclusively to [multi-device-sync.md](./multi-device-sync.md), which references this ADR as target governance and must not duplicate its decision tables.
- **Design change requires a new decision**: this ADR changes only by explicit user-approved amendment, or is marked superseded by a named successor ADR that states what it replaces. The superseded ADR is retained for audit and no longer authoritative.
- **Evidence never silently modifies decisions**: validation runs, implementation findings, and new evidence are recorded in the living reference; they do not alter this ADR's decisions without an amendment or superseding ADR.
- [sync-architecture-selection.md](./sync-architecture-selection.md) stays a conditional fallback reference; it is unaffected by this ADR except for a routing pointer.
- **No implementation authorization flows from this ADR.** No protocol, endpoint, schema, migration, IPC, or code change is authorized by citing it. Citing this ADR as proof that the target model is implemented is false.

---

## References

- [Personal Multi-Device Sync](./multi-device-sync.md) — current reference (implementation/evidence/gaps/next step).
- [Sync Architecture Selection](./sync-architecture-selection.md) — conditional fallback reference.
- [SQLite migration governance](./sqlite-migration.md) — SQLite chat authority, migration process.
- [Context window governance](./context-window.md) — context anchor semantics.
- [Application Identity ADR](./cherry-chat-application-identity.md) — identity, compatibility, updater/release freeze, platform scope.
- [Architecture Evolution Program](./architecture-evolution-program.md) — program intent (sync not tracked there).
