# Performance Workstreams — Current Actionable State

> **Position**: This document carries **current actionable state only** — open product problems, evidence, bounded cost models/assumptions, explicit unknowns, next experiment/analysis targets, acceptance framework. Not an ADR; no new governance authority (PERF-LOCK-001).
> **Mutable**: Updated as work progresses; session流水, checksums, date details and duplicate run narratives are owned by Git history (DOC-002). Evidence-only work is diagnostic and does not default to progress unless bound to an explicitly activated decision/outcome with all four evidence-task fields (named decision/outcome, claim, minimum sufficient method, stopping condition); explicit activation alone is insufficient (see `performance-program.md` §4B).
> **Authoritative sources**: Methodology entry: [`performance-program.md`](./performance-program.md) (proportionality/routing canonical); persistent measurement contract: [`performance-measurement.md`](./performance-measurement.md) (formal quantitative only); architecture evolution program: [`architecture-evolution-program.md`](./architecture-evolution-program.md).
> **Measurement complete ≠ product problem closed** (DOC-004): "Done" measurement slices below do not close their corresponding product problems; product problems stay `Open` until §4 closure conditions are met. Phase closure requires accepted outcomes + boundary-matched regression + governance/delivery validation + explicitly accepted residual risk — not harness completeness.

## 1. Priority Rubric

Priority is evaluated by the amplification-first methodology (`performance-program.md` §4): **Priority = user impact × (duration, frequency, scale curve, blocked thread/process)** (PERF-LOCK-004). Open workstreams below are ordered by user visibility; each workstream推进 requires Main/user explicit authorization (`Active` unique per level; closure does not auto-activate; status and authorization rules in `performance-program.md` §6/§8). Evidence cost scales with risk, irreversibility, uncertainty, and expected reuse; every evidence task binds to an explicitly activated decision/outcome, claim, minimum sufficient method, and stopping condition (all four required; explicit activation alone is insufficient).

## 2. Product Problems & Registry Workstreams

Product problem status and measurement slice status are **separate**: problems stay `Open` until closure conditions are met; their historical measurement assets serve as **evidence identity** only, not closure. §2.1–2.3 are current `Open` user-visible product problems; §2.4 is a `Planned` direction draft (not active Open workstream); status semantics in `performance-program.md` §6. B-01..B-05 are **implemented renderer-local retention enforcement (max 8 inactive evictable topics, 32 MiB logical budget, 30-minute TTL, deterministic LRU/lexical tie-break, oversized fail-closed and evictable after unpin), not empirically optimal/baselines/SLA, calibration optional/non-blocking, measurement-only directional L3 executed 2026-08-31 (small profile, 25 metrics, 7/7 gates, artifact `b0105-calibration-20260831-235508.418.json`, `pnpm bench:b0105-calibration`, synthetic isolated, does not change implemented defaults; future invocations inactive by default)**.

### 2.1 PERF-TOPIC-SWITCH — Topic Switch (Open · Independent Post-Refactor Reference/Reassessment, Non-Blocking for Architecture Phase 3)

- **Product problem status**: `Open` (remains Open after Architecture Phase 3 structurally Closed; architecture closure does not close PERF problems per ARCH-011).
- **Architecture relationship (reclassified per ARCH-009..011)**: Former architecture acceptance surface reclassified as independent post-refactor reference/reassessment. No absolute threshold gates architecture; reproducible material regression under controlled same-state comparison would challenge correctness per ARCH-010 (none established).
- **Historical measurement assets**: legacy PERF-101 (cache-miss + cache-hit repeat-switch `perf101-cache-hit-repeat-switch.spec.ts`) — evidence identity, not closure. Directional L3 only: no fixed-W/N cross-profile growth claim beyond directional; L1 is correctness/completeness pass; values are L3 directional, not thresholds.
- **Reference provenance (ARCH-012 preserved, directional L3 only, not baselines)**: Historical clean baselines preserved per ARCH-012 (directional L3 only, not baselines; artifacts gitignored). Exact p50/p95 values and run provenance in Git history; not reproduced as thresholds.
- **Bounded cost model**: (1) Topic switch cold mount — full-topic Main load + IPC (`loadTopicMessagesThunk` whole-topic) + full-topic recomputation (`createLatestMessageWindow`/`computeContextInfo`). (2) Markdown visible window scaling — strong W growth, no N amplification at fixed W.
- **Explicit unknowns**: Large topic 300+ with controlled content complexity; content complexity axis; user frequency; O(N) vs O(W) attribution; Markdown parse CPU contribution; virtualization feasibility; cross-run noise bound.
- **Next target (candidate, not authorized)**: N300/W10 with controlled content complexity, or renderer window-side structural attribution (requires direct parse-CPU evidence and explicit approval). **Not authorized — do not execute**.
- **Acceptance framework**: Closure requires accepted user-visible click→first-render + integrated implementation when needed + boundary-matched regression evidence. Do not treat L3 values as thresholds.

### 2.2 PERF-STREAMING — Streaming Output Batch Arrival (Open)

- **Product problem status**: `Open`. Primary symptom is single-stream visible output arriving in batched chunks (not smooth per-character); concurrent multi-stream amplification deferred until directly linked.
- **Historical measurement assets**: legacy PERF-102 — evidence identity, not closure. Directional L3 only, attribution incomplete, cannot attribute to IPC/SQLite without cross-boundary instrumentation.
- **Current evidence (L3 directional, non-threshold)**: Candidate interval shows directional reduction in per-update volume with stable frame/long-task; DOM/Redux relationship directional, needs wider confirmation. No formal baseline.
- **Persistence attribution (PERF-STREAM-ATTR-001 directional)**: Directional single-block vs batch write pattern; Renderer IPC and Main tx remain directional, not dominant cost carrier for single-stream batch arrival (directional, not exclusion; detailed counts/timings in Git history).
- **Bounded cost model**: (1) Streaming cadence throttle `MARKDOWN_PARSE_CADENCE_MS` (150→50ms candidate directional support, not confirmed root cause). (2) Full-content re-processing — long-task/frame stable across cadence change.
- **Explicit unknowns**: User-visible smoothness acceptance for 50ms; wider device/load validation; concurrent N>1 50ms behavior; chars/update granularity perception; DOM/Redux ratio consistency.
- **Next target (candidate, not authorized)**: (Priority) User-visible smoothness confirmation for 50ms candidate (requires explicitly activated decision/outcome + four fields; not authorized — do not execute); (Conditional) wider device validation (candidate, not authorized); (Deferred) concurrent multi-stream 50ms behavior only if linked (candidate, not authorized). **Not authorized — do not execute**.
- **Acceptance framework**: Closure requires accepted single-stream smoothness + integrated implementation when needed + boundary-matched regression evidence. Do not treat L3 values as thresholds.
- **Measurement slices**: PERF-STREAM-ATTR-001/002/003 completed as measurement-only (default inert/skip, L1 correctness pass, timing L3 non-threshold). Attribution remains directional; explicit unknowns unchanged.

### 2.3 PERF-ECHO — Message Echo (Open · Independent Post-Refactor Reference/Reassessment, Non-Blocking)

- **Product problem status**: `Open` (remains Open after Architecture Phase 3 Closed; ARCH-011). Echo delay is user-visible interaction path.
- **Historical measurement assets**: legacy PERF-103 (baseline + batch-seeded high-turn `perf103-high-turn-echo-measurement.spec.ts`) — evidence identity, not closure. `reduxToDom` directional portion of `firstRender`; interval-overlapping long task directional.
- **Reference provenance (ARCH-012 preserved, directional L3 only)**: Historical clean baselines as in §2.1; directional L3 only, not thresholds. Exact p50/p95 values and run provenance in Git history; not thresholds.
- **Directional results (L3, directional, non-threshold)**: Standard and high-turn profiles directionally stable within-run with significant cross-run variance. Not thresholds/baselines; detailed ranges in Git history.
- **Bounded cost model**: Echo full-topic pre/post localization to renderer full-topic lifecycle; shared projection + window deduplication + anchor fast path consistent but not individually attributed.
- **Explicit unknowns**: Stage attribution within single long task; individual contribution of anchor/completed-block/Phase 2B slices; 300 prior turns; wider device/load; cross-run noise; user acceptance; matching-boundary regression protection not yet recorded.
- **Next target (candidate, not authorized)**: Stage-level attribution within Redux→DOM interval, or new controlled viewport convergence experiment (must prove elimination of actual redundant work).
- **Acceptance framework**: Closure requires accepted echo delay + integrated implementation when needed + boundary-matched regression evidence; candidate must prove elimination of actual redundant work.

### 2.4 PERF-DB-HEALTH — SQLite Data Health and Optimization (Planned · Architecture-Program Sequencing)

- **Workflow status**: `Planned` (direction draft, scope not approved). This document records direction draft and risk/measurement slices only, does not activate any experiment/measurement/implementation; `Active` unique per level, activation requires Main/user explicit authorization via explicitly activated decision/outcome (`performance-program.md` §6/§8). Evidence-only work does not default to progress unless bound to an explicitly activated decision/outcome with all four evidence-task fields (§4B); explicit activation alone is insufficient.
- **Architecture relationship**: Reclassified per Architecture Evolution Program §6–§7. Independent diagnostics (M1/M2/M3/M4/M5/M7/M8) are read-only w.r.t. production/user state (M8 synthetic owned mkdtemp writes permitted; M4 read-only post-seed numeric aggregation; M5 synthetic rows + synthetic booleans) and do not require ADR, but each diagnostic requires explicit Main/user activation before execution. M6 is architecture-phase-dependent/governance-bound per `architecture-evolution-program.md` §7.2 (read-only required-table inventory on synthetic isolated schema-v1, synthetic isolated small-profile directional L3 — evidence completed 2026-08-31, 4 schema-absent + 5 open-design categories, 5 required +1 excluded tables, 11 excluded domains, 7/7 gates, artifact `chatdb-m6-sync-metadata-gap-small-20260831-232423.448.json`, Node 24.11.1/pnpm 10.27.0/ABI 137, commit 9a929453695bec9c5b68b23dc4035589998319e9 clean, directional-only, analysis-only; no schema/migration/sync design/sync-schema claim, future invocations inactive by default, requires governance/ADR for any production schema/migration/sync design) and does not authorize S6.5/Phase 8. M4 (1k/10k/50k via `pnpm bench:m4-fts-dup`, directional only, measurement-only) and M5 (bounded synthetic directional L3 — small-profile evidence completed 2026-08-31, 100 references, 4 scenarios, 5/5 gates, artifact `chatdb-m5-file-dual-state-20260831-232513.325.json`, Node 24.11.1/pnpm 10.27.0/ABI 137, commit 9a929453695bec9c5b68b23dc4035589998319e9 clean, synthetic isolated, directional-only) are measurement-only; future runs inactive by default and require explicitly activated decision/outcome + four fields to count as valid/progress; no S6.5 authorization, no threshold/baseline/phase-closure, no runtime-consistency (M5) claim (M6 sync-design/schema claim separately excluded as architecture-phase-dependent). Full-topic/windowed fetch/cache joins belong to Phase 5 contract. PERF-DB-HEALTH remains `Planned`. B-01..B-05 are implemented renderer-local retention enforcement (max 8 inactive evictable topics, 32 MiB logical budget, 30-minute TTL, deterministic LRU/lexical tie-break, oversized fail-closed and evictable after unpin), not empirically optimal/baselines/SLA, calibration optional/non-blocking and executed as measurement-only directional L3 on 2026-08-31 (small profile, 25 finite numeric metrics, 7/7 correctness gates, artifact `b0105-calibration-20260831-235508.418.json`, 2026-08-31T15:55:06.202Z, `pnpm bench:b0105-calibration`, synthetic isolated, does not change implemented defaults; future invocations inactive by default); M6 fingerprint 4 schema-absent + 5 open-design categories, 5 required tables + 1 excluded file_references, 11 excluded sync domains — per `architecture-evolution-program.md` §7.2.
- **Persistent facts (recorded, non-threshold, non-root-cause)**: FTS block update write amplification fixed by migration 004 (remaining cost is normalized + FTS logical duplication); short <3 codepoint LIKE full-table scan fallback; index/query opportunity hypothesis; dense sort_order O(N) shift (tail append O(1) via LOCK-002, middle/batch O(N)); file dual-state hypothesis; absent sync metadata (ADR-level if introduced).
- **Bounded cost model**: Amplification axes DB volume / topic size / FTS corpus, landing in Main-process SQLite write/query paths; items above are recorded/untested carriers, no root-cause attribution before controlled microbenchmark.
- **Explicit unknowns**: Real S2/S3 scale cost per carrier; middle-insert O(N) user frequency; FTS duplication volume proportion; short-term LIKE real-corpus impact; file dual-state drift; sync metadata方案 not locked. M4 1k/10k/50k remain synthetic logical evidence only.
- **Diagnostic registry status (diagnostic · Planned, not threshold sources)**: All slices are diagnostic/planning artifacts requiring explicitly activated decision/outcome + four fields to count as valid/progress — current status: M1, M2/M3/M4(1k/10k/50k)/M5/M6/M7/M8, and B-01..B-05 calibration executed as measurement-only directional L3 (harnesses inactive by default for future runs; artifacts gitignored, schema v1; M1 9 combinations N=100/500/1000 × M=1/10/50, 45 finite numeric metrics, 3/3 correctness gates, artifact `chatdb-sort-order-shift-20260831-235421.921.json`, 2026-08-31T15:54:21.031Z, `pnpm bench:sort-order-shift`; B-01..B-05 small profile, 25 finite numeric metrics, 7/7 correctness gates, artifact `b0105-calibration-20260831-235508.418.json`, 2026-08-31T15:55:06.202Z, `pnpm bench:b0105-calibration`, synthetic logical-boundary/TTL/LRU/fit-step/oversized classifications; M8 3 scenarios ×5 measured samples, 21/21 scenario gates (7 gate kinds ×3), 15 finite numeric metrics, artifact `chatdb-m8-l3-archive-health-20260901-000004.064.json`, 2026-08-31T16:00:03.394Z, `pnpm bench:m8-l3-archive`; M5 small-profile 100-reference/4-scenario 5/5 gates `chatdb-m5-file-dual-state-20260831-232513.325.json` and M6 small-profile 7/7 gates `chatdb-m6-sync-metadata-gap-small-20260831-232423.448.json`, both 2026-08-31, Node 24.11.1/pnpm 10.27.0/ABI 137, commits 59b9eefecacb5fe93754b3604d0ce802b22d34d6 (M1/B0105/M8) and 9a929453695bec9c5b68b23dc4035589998319e9 (M5/M6) clean, synthetic isolated, numeric-only, privacy-safe, directional-only, analysis-only; detailed per-run values not reproduced here; provenance in canonical text and gitignored artifact metadata); B-01..B-05 calibration is optional/non-blocking, does not change implemented defaults, analysis-only, not adopted optima, future invocations remain inactive by default and require explicitly activated decision/outcome + four fields; M1 is independent DB-health diagnostic; M8 is synthetic archive/restore health only and does not prove real relaunch/recovery or user-data consistency. No diagnostic authorizes S6.4/S6.5 production changes, thresholds, baselines, SLA/capacity policy, phase closure, schema/index/migration/IPC/lifecycle changes, or policy adoption; real-corpus/physical-size and production S6.5 remain ADR-gated. Exact per-run metrics belong only in `performance-measurement.md` §6 summary; no threshold/baseline/Phase closure, no runtime-consistency (M5) or sync-design/schema (M6) claim, no S6.5/Phase 8 authorization; future invocations remain inactive by default and require explicitly activated decision/outcome + four fields.
- **Acceptance framework**: Closure requires accepted user-visible/data-health result + integrated implementation when needed + boundary-matched regression evidence (`performance-program.md` §8); any schema/migration/search semantics change via ADR (PERF-LOCK-008).

### 2.5 PERF-RENDER-FLOW — Renderer Visible Subtree Fanout/Lifecycle (Paused · Superseded by Architecture Program)

- **Workflow status**: `Paused`. Candidate queue exhausted, stop condition satisfied. Resume requires Main/user explicit approval. No probes during pause.
- **Superseded by Architecture Program**: Tactical queue (A: topic-key remount, B: MessageGroup identity, C: selector/effect fanout) superseded by Architecture Evolution Program Phase 2/3. Root structural causes — full-topic load, O(N) recomputation, eager mounting, unstable projection identities — addressed via lifecycle refactoring, not isolated renderer patches.
- **A/B/C conclusions retained**: A (topic-key) evaluated but not implemented as isolated candidate (LOCK-005), later implemented via architecture S3.1; B fully reverted; C fully reverted — requires broader ownership change. Architecture program delivers stable render/state/action graph.
- **Resume rule**: Resume only on Main/user explicit approval for a new bounded candidate not overlapping Architecture phases and not crossing §9 governance boundaries (PERF-LOCK-008).

## 3. Completed Production Optimization Summary

| Optimization Group | Outcome Summary | Production Implementation | Regression Evidence |
|---|---|---|---|
| Edit mode enter/exit | No longer remounts visible subtree (stable host + resetToken) | Integrated | renderer tests + fresh production-build E2E (edit gate) |
| Multi-message middle insert (paste) | Per-message serial IPC → batch insert (single dense normalization + single commit) | Integrated | fresh production-build E2E (paste gate) |
| Multi-model answer label switch | Two foldSelected writes → single atomic select-answer-message | Integrated | new contract/aggregation/IPC/Redux tests + fresh E2E |
| Echo/completion block renderer lifecycle | Active-resolvable anchor fast path; completed Markdown bypasses smooth-stream | Integrated | anchorService/Markdown/streaming tests + fresh E2E |
| Stable host / topic transition (S3.1) | Stable Messages host, useTopicTransition layout-phase reset, removed key remount | Implemented (Phase 3 Closed; reference measurements directional L3 per ARCH-012, no controlled comparison claim; provenance in Git) | focused renderer + fresh production-build E2E |

> Production baseline assets: PERF-002 committed-state machine-readable reference baselines (schema v1, gitignored, L3 non-threshold, sole threshold cold-open <500ms per measurement contract §7). PERF-001 schema v1 contract; PERF-004 FTS 1k/10k fast deterministic curve.

## 4. Closure Conditions and Advancement Rules

1. **Product problems stay Open** until: accepted user-visible result + integrated implementation when needed + boundary-matched regression evidence (`performance-program.md` §8) + explicitly accepted residual risk. Harness completeness never closes.
2. **Measurement complete does not close product problems** (DOC-004): legacy measurement assets are evidence identity; their Done does not close corresponding Open problems.
3. **Three tracks non-substitutable** (`performance-program.md` §4B): decision evidence, implementation regression evidence, aggregate delivery validation.
4. **Do not treat L3 values as thresholds/root causes** (PERF-LOCK-003): all values are L3 directional/reference, not thresholds, not root-cause attribution.
5. **Evidence-task contract**: every evidence task binds to an explicitly activated decision/outcome, claim, minimum sufficient method, and stopping condition (all four required; explicit activation alone is insufficient); evidence-only work without all four does not count as progress and cannot close a phase/workflow.
6. **Formal artifacts reserved**: schema v1/bench artifacts and lane/provenance/privacy rigor only for formal quantitative claims or reusable measurement contracts.
7. **Advancement requires explicit authorization**: any new experiment/analysis/implementation requires Main/user explicit authorization; not authorized = not executed; closure does not auto-activate.
8. **Architecture handoff**: when performance evidence exposes structural debt changing ownership/lifecycle/data contracts, it enters Architecture Evolution Program instead of expanding a performance patch loop. PERF-TOPIC-SWITCH/ECHO remain Open non-blocking for Architecture Phase 3 (ARCH-011); controlled-regression rule ARCH-010 applies before phase exit; provenance preserved per ARCH-012.
