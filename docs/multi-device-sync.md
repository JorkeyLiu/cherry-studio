# Personal Multi-Device Sync — Current Reference

> **Status**: Current reference for personal multi-device sync. Limited validation; not production-ready.
> **Role**: This document owns sync goal, current approach, current status/limits, evidence, gaps, and next decision.
> **Fallback reference (conditional only)**: [Sync Architecture Selection](./sync-architecture-selection.md) — candidate analysis reusable only on a concrete current-path blocker with clear technical advantage.
> **Development principle**: This sync effort is initiated and evolved under `adaptive-development` principles: the intended outcome remains the anchor, current state and gap determine the next step, and design, implementation, evidence, and validation evolve together.
> **Target connection/channel governance**: [Sync Connection, Registration, and Hidden Multi-Channel Pairing ADR](./sync-connection-channel.md) (`SYNC-CC-*`, approved 2026-09-09; `SYNC-CC-017`–`SYNC-CC-021` baseline persistence/offline-bootstrap plus serialized publish-gate/in-flight-retention plus single-stage publish/fetch wire contract with `N >= current` monotonic floor, full-current-match idempotency, and `jcs-sha256-v1` outer `digest` over **only** `payload` (`SYNC-DATA-032`/`SYNC-DATA-044`, F-01/F-05) including versioned logical wire inventory v1 (`sync-baseline-wire-v1` only outer `wireVersion`/`topic-message-stable-block-order-v1`/`parent-order-frame-v1`, `payloadSchema`/`inventoryVersion`/`orderFrameVersion` only inside `payload.manifest` — F-01 — `SYNC-DATA-041`/`SYNC-CC-021`) — inventory: `topic`/`message`/stable supported `messageBlock` full-state with locked lowerCamelCase keys per §10A.1 (F-02/F-03) + entity/field/`parentMembershipClock` clocks + explicit tombstones (legacy `null` → strong barrier `(T,null) > (T,nonNull)` F-05) + `topicMessage` & `messageBlock` `parent-order-frame-v1` frames with exactly-one winning frame per parent and parent/frame full-set including empty `[]`; type/value domain locked (F-03) and `SYNC-DATA-033`–`SYNC-DATA-040` F-01..F-04 plus F-05 closed — outer `wireVersion` only outer, triple only manifest, byte-level JSON spelling locked (`SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021` F-02/F-05).
> **Target data-convergence governance**: [Sync Data Convergence ADR](./sync-data-convergence.md) (`SYNC-DATA-*`, approved 2026-09-10; `SYNC-DATA-019`–`SYNC-DATA-045` baseline persistence/offline-bootstrap/watermark-boundary plus publish-gate/in-flight-protection plus single-stage publish/fetch wire contract plus public digest `jcs-sha256-v1` **over only `payload` with outer `digest` key** plus parent-scoped order frame F-01..F-05 closed plus **versioned logical wire inventory v1** `topic-message-stable-block-order-v1` at `chat-core-baseline-v1`/`sync-baseline-wire-v1`/`parent-order-frame-v1` (`SYNC-DATA-041`–`SYNC-DATA-045` / `SYNC-CC-021`): fixed logical-wire inventory — `topic` (`id`+`name`/`assistantId`/`createdAt`/`updatedAt`/`deletedAt`/`pinned`/`prompt`/`isNameManuallyEdited`), `message` (`id`+`topicId`+`role`/`content`/`status`/`askId`/`model`/`modelId`/`assistantId`/`createdAt`/`updatedAt`), `messageBlock` (`id`+`messageId`+`type`/`content`/`status`/`createdAt`/`updatedAt`) full-state with locked lowerCamelCase keys per §10A.1, every mutable field as fixed key (`string|null` or `boolean|null` verbatim I-JSON for time fields, not LWW; `id`/parent non-empty valid Unicode scalar; all keys fixed existence — F-03), per-row `sortOrder` excluded, transient `streaming`/`pending`/`processing`/`searching` and `tool`/`file`/`image`/`video`/`citation`+overflow excluded, clocks `{timestamp, operationId}` safe-int/colon-free≤256 with per-live-entity `entityClock` + per-mutable-field `fieldClocks` + required `parentMembershipClock` (F-03), tombstones `{entityType, entityId, deletionClock: {timestamp, operationId: string|null legacy strong barrier}, survivingEntityClock}` (F-05, `(T,null) > (T,nonNull)`), `topicMessage`/`messageBlock` `parent-order-frame-v1` frames `{frameVersion, kind, parentId, orderedChildIds, frameClock}` with exactly-one winning frame per parent including empty + parent/frame full-set (F-04) plus membership-gated completeness, fixed manifest `payload.manifest` (`payloadSchema`/`inventoryVersion`/`orderFrameVersion`/`scope` + `liveCounts`/`tombstoneCounts`/`frameCounts` + `completeness:'complete'` uniquely recomputed — `400`/fail-closed even if digest self-consistent, F-05) and `jcs-sha256-v1` input only `payload` with outer `digest` key `digest` (F-05), JCS array determinism over locked `payload` (§10A.1), per-version strictly closed — **inventory v1 byte-level JSON spelling now locked** (`SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021` F-02/F-05; path/HTTP verb/TS/DB still deferred).
> **Implementation-vs-target**: the SYNC-CC connection/registration/channel/pairing foundation is implemented with limited validation on the current exact worktree (see §7 Current validation). The `sync-baseline-wire-v1` pure protocol validator `packages/shared/sync/baselineWire.ts` (`SYNC-DATA-041`–`SYNC-DATA-045` / `SYNC-CC-021` F-01..F-05) is implemented as a pure, transport-free protocol module with limited validation (see §3/§7) — strict wire types/constants, validation and `jcs-sha256-v1` `payload`-only digest; since 2026-09-11 it backs the relay baseline resource (`PUT`/`GET /sync/baseline`, `SYNC-CC-022`/`SYNC-DATA-046`) for strict envelope validation, payload-only digest recompute, and canonical idempotency comparison — receiver bootstrap/apply is now wired (cursor-0 fetch + single-transaction merge + cursor=N + N+1 replay) and the publisher barrier is now wired as the verification-only explicit entry (`SyncService.publishBaseline` with single client-declared `PUT`, no auto-publish from `sync()`); no other network protocol is yet online. Main SQLite additive migration `009_sync_membership_clock` (`sync_membership_clock` — `child_entity_type`/`child_entity_id` (`message`/`message_block`), immutable `parent_id`, creation `{timestamp, operationId}`) now truthfully persists `parentMembershipClock` for supported local creation paths and accepted remote true creates atomically in the aggregate transaction; ordinary edits preserve it, deletion retains it, same-parent accepted reappearance after delete preserves the original tuple, different-parent retained metadata/reparent fails closed and rolls back. Pre-existing rows, closure snapshots, transient→stable promotions, and currently unsupported/skipped compound/branch/clone/paste/reset paths remain without membership (no backfill/guessing; `entityClock`/`fieldClock`/`createdAt`/current time never substitutes) and are therefore truthful unversioned for wire completeness. Deterministic Main-only internal `local_sync_baseline_candidate` (`local-sync-baseline-v1`/`topic-message-stable-block-order-v1`) is now frame-aware (`topic-message-stable-block-order-v1`, `parent-order-frame-v1`) and captures persistent `sync_parent_order_frame` frames in the same SQLite snapshot: it emits normalized effective full-set order (filtered winning list + deterministic `membershipClock>frameClock` suffix) with original `frameClock` unchanged, including empty `[]` frames and soft-deleted live topics; hard/tombstoned parents have no frames and retained orphan/excluded frames are omitted. Payload no longer carries or field-merges per-row `sortOrder`; legacy `sortOrder` clocks are ignored for this local baseline path. Missing/incomplete/unversioned remains exact partial diagnostics (`missing-order-frame`/`incomplete-order-frame`/`unversioned-membership` etc.), never synthesized, and digest deterministically covers frames/membership/manifest. Local validated `applyLocalSyncBaselineCandidate` atomically merges entities/tombstones/membership/winning frames, uses `frameClock` LWW with equal-clock semantic effective comparison, re-evaluates fixed point after all relevant changes, preserves versioned local-only children per cutoff/suffix rules, blocks on unversioned local children, and materializes dense `sortOrder 0..n-1` only as local projection; only winning tombstone suppresses with legacy `(T,null)` strong barrier and UTF-8 tie ordering, full rollback preserves protected local sync state (outbox/applied/cursor/channel). Normalized representation boundary: source DB retains raw winning frame; candidate carries effective normalized sequence with unchanged clock; apply persists selected normalized snapshot and re-evaluates against merged live state. Colon-containing IDs are supported in internal keys with strict local Unicode-scalar / `operationId 1..256`/no-colon validation, without changing shared/remote `syncTombstoneCodec` behavior. Additive migration `011_sync_parent_order_frame_parent_id_unbounded` rebuilds `sync_parent_order_frame` to remove the erroneous 256 cap from ordinary parent IDs while preserving rows/other constraints; ordinary IDs are non-empty valid Unicode scalar with no upper bound, `operationIds` remain `1..256`/no-colon, `ParentMembershipClock` `parentId` mandatory/exact. Local persistence/candidate/apply remain `local-sync-baseline-v1`/`topic-message-stable-block-order-v1` + `parent-order-frame-v1`, plus the landed pure local payload-only wire projection (`projectLocalBaselineToWirePayload`, see §3); still no relay envelope binding/JCS publish to relay, no relay baseline persistence/publish/fetch/barrier, no receiver wire bootstrap, and remote incremental `SyncOperation` remains frame-less unchanged (see §4). Relay-persisted per-channel current-effective baselines, offline bootstrap, and the watermark three-segment boundary (`SYNC-CC-017`–`SYNC-CC-021`, `SYNC-DATA-019`–`SYNC-DATA-045`) remain approved target only and are not implemented — including the `SYNC-DATA-023` serialized watermark-monotonic (`N >= current`, below-current `409`)/coverage publish gate, the `SYNC-DATA-024`/`SYNC-CC-019` in-flight bootstrap N+1 protection, the `SYNC-DATA-025`–`SYNC-DATA-031`/`SYNC-CC-020` single-stage publish/fetch wire contract with publisher barrier/snapshot, envelope categories outer `{wireVersion, channelId, watermark, digestScheme, digest, payload}` (channelId auth-derived, never trust body `deviceId`; `wireVersion` only outer, triple only inside `payload.manifest` — F-01) with locked lowerCamelCase JSON spelling for inventory v1 per §10A.1 (F-02), validation boundary with manifest recompute `400`/fail-closed even if digest self-consistent (F-05) and JCS input only `payload` with outer `digest` key (F-05), and publish/fetch state machine plus status codes (idempotent `200` only on full current-match; replaced-candidate retry is stale `409`), the `SYNC-DATA-032`/`SYNC-CC-021` public digest `jcs-sha256-v1` contract wired only inside the pure validator (fail-closed JCS, `payload`-only scope, recompute-proves-byte-identity-only, atomic envelope binding), and the `SYNC-DATA-033`–`SYNC-DATA-040` F-01..F-04 closed plus `SYNC-DATA-041`–`SYNC-DATA-045` wire inventory v1 with fixed full-state fields plus F-03 type/value domain, stable gate, clocks, per-live-entity `entityClock` + `fieldClocks` + required `parentMembershipClock`, tombstone strong barrier `(T,null) > (T,nonNull)` (F-05), per-parent exactly-one winning frame + membership-gated completeness + parent/frame full-set including empty frames and `orderedChildIds` exactly inventory live children (F-04), manifest recompute (F-05), plus per-version strictly closed.
> **Last updated**: 2026-09-12 — incremental `messageBlock` order_frame second-pair extension (`SYNC-DATA-048`/`SYNC-CC-024`, user-authorized implementation, same wire contract, no new lock; limited validation, not production-ready) on top of the 2026-09-11 publisher barrier publish (`SYNC-DATA-025`/`026`/`027`/`032`, `SYNC-CC-019`/`020`/`021`/`022`, verification-only explicit entry; relay baseline resource and receiver bootstrap landed earlier, see §3/§7): frame-aware local baseline `local_sync_baseline_candidate`/`applyLocalSyncBaselineCandidate` (`topic-message-stable-block-order-v1`/`parent-order-frame-v1`) plus additive `011_sync_parent_order_frame_parent_id_unbounded` — candidate captures persistent frames in same SQLite snapshot with normalized effective order (including empty `[]` and soft-deleted live topics) under original `frameClock`, no `sortOrder` carry/merge (legacy clocks ignored), exact partial diagnostics never synthesized, digest covers frames/membership/manifest; apply atomically merges entities/tombstones/membership/winning frames with `frameClock` LWW + equal-clock semantic effective comparison, fixed-point re-evaluation, `suffix`/`cutoff` preservation for versioned local-only children, unversioned block, and dense `0..n-1` materialization with legacy `(T,null)` barrier and full rollback; migration 011 rebuilds `sync_parent_order_frame` to remove 256 cap on ordinary parent IDs (operationIds stay 1..256/no-colon) preserving rows; colon IDs supported internally with strict local validation without changing shared/remote codec; remote `SyncOperation` still frame-less. Pure local transport-free wire `payload` projection (`src/main/services/sync/syncBaselineWireProjection.ts`): complete frame-aware `local-sync-baseline-v1` candidate → locked `sync-baseline-wire-v1` `payload` only (no envelope), stripping all local diagnostics (`pendingOutboxCount`/`observationBinding`/`observedLocalChannelKey`/`observedLocalCursor`/`unversioned`/`excluded`/`orphan`/`aggregate`+`reasons`), full entity/tombstone/order-frame/manifest mapping with wire UTF-8 byte-lex order, self-validated by the shared strict `baselineWire` validator/canonicalizer plus payload-only `jcs-sha256-v1` digest/verify; rejects partial/unbound/incomplete candidates fail-closed; no relay/network/IPC/UI. Evidence: see §7 final validation — `pnpm build:check` exit 0 in 732s on the 2026-09-11 final exact code state (lint/typecheck/i18n/format/openapi passed; full Vitest 12,544 passed + 90 skipped, 0 failed) plus receiver bootstrap focused 25/25 (`syncBaselineFetch` 11/11, `syncBaselineWireApply` 5/5, `syncBootstrap` 9/9) and `baselineResource` 15/15; limited validation, not production-ready; no new E2E runs, prior E2E evidence retained. Sixth unit (2026-09-11): relay baseline resource `PUT`/`GET /sync/baseline` (`SYNC-CC-022`/`SYNC-DATA-046`, `cc-2` persistence, 15 focused tests; see §3/§7) — publisher barrier, remote frame ops, and size/compression/lease/GC stay deferred (receiver bootstrap landed, see §3/§7).

## 1. Goal

Syncthing-like local-first personal multi-device sync with no Cherry Chat account.

- Each device keeps a complete local copy and remains usable offline. Sync is an explicit opt-in coordination layer, not a cloud-primary model.
- Devices are paired through explicit device-code request/accept into hidden per-channel membership. Pairing and device authentication are distinct from an account system.
- At any time the user connects to one user-selected compatible sync service: a personal-hosted relay they deploy themselves or that is deployed online. The service is a pluggable infrastructure choice, not a Cherry Chat account system.
- Target behavior is automatic online convergence plus cursor-based recovery after short disconnection, with no silent loss.

## 2. Current approach

The selected path is application operation-log plus thin personal-hosted HTTP relay.

- Chat authority stays in Main-process SQLite (`Data/chat.db` via `ChatDbAggregateService`). The renderer never holds a SQLite connection; all chat access goes through typed IPC.
- Supported stable mutations enqueue sync intent as an operation log that preserves business intent (create, edit, delete) rather than raw row diffs. Only stable persisted checkpoints are sync candidates; transient streaming state is not a sync event.
- A thin HTTP relay carries operations between paired devices. The relay stores and forwards operations; it never owns chat authority. The relay baseline resource (`PUT`/`GET /sync/baseline` per-channel single current envelope with strict validation, payload-only digest recompute, serialized gating, and restart persistence — `SYNC-CC-022`/`SYNC-DATA-046`) is implemented on the reference relay; publisher barrier/snapshot is now implemented client-side as an explicit verification-only entry (`SyncService.publishBaseline` with single client-declared `PUT`, no auto-publish from `sync()`; see §3) while in-flight lease/ack signaling stays approved target only and is not current behavior, and receiver bootstrap/apply is now implemented client-side (`SYNC-CC-023`/`SYNC-DATA-047`) — the single-stage client-declared publish/fetch wire contract is now implemented on the client paths as observed behavior (single `PUT`, no fence/token/prepare; `200` full-match idempotent; `400`/`401`/`403`/`409`/`500` truthful; payload-only `jcs-sha256-v1` per `SYNC-DATA-025`–`SYNC-DATA-032`/`SYNC-CC-020`), while stale-`409`-beyond-next-local-change, lease/ack, publisher election, and remote frame ops beyond the `topicMessage` + ordinary-`messageBlock` incremental `order_frame` (`SYNC-DATA-048`/`SYNC-CC-024`, see §3) stay code-less target (the conservative local-triggered auto-publish policy is now implemented in `syncAuto` + `publishBaselineIfEligible`, see §3).
- Replay applies operations idempotently with deterministic last-writer-wins resolution and tombstone propagation for deletions.
- Payloads carry only allowlisted shareable fields. Credentials, derived data, device-local paths, and UI state stay out of the sync channel.

### 2.1 Supported personal relay paths (this phase)

Two shapes share one relay protocol; both support plain HTTP and native
HTTPS transports and preserve the loopback `pnpm sync:relay` path, tests,
and strict TLS semantics. The relay never generates, manages, installs, or
rotates certificates: HTTPS is served only with operator-supplied
certificate/key files, using ordinary verification with no bypass or
auto-trust. Do not use systemd/cloud variants, reverse proxies, or fallback
technologies. There is no independent web admin UI: the existing Cherry Chat
client pairing UI remains the pairing-acceptance control plane.

- Command (loopback): `pnpm sync:relay -- --port <port> --db <path> --token <token>` (token fallback: `SYNC_RELAY_TOKEN` env; explicit `--token` wins). Help: `pnpm sync:relay -- --help`. The command runs `scripts/sync-relay/server.ts` with the repository's pinned Node/pnpm conventions.
- Command (LAN): `pnpm sync:relay -- --host <LAN-IP> --port <port> --db <path> --token <token> [--cert <cert.pem> --key <key.pem>]`. This is the supported direct host-run LAN shape alongside the Docker Compose deployment in Section 2.2 (Linux x64/arm64 home servers). Without `--cert`/`--key` the relay serves plain HTTP (unencrypted — the client shows a visible non-blocking warning for non-loopback `http://` endpoints); with a user-supplied `--cert`/`--key` pair it serves native HTTPS (Node stdlib `node:https`, no new dependency) and binds the user-selected non-loopback LAN address. There is no reverse-proxy, service-manager, or cloud variant.
- Prerequisites: macOS-arm64-first scope, pinned Node v24.11.1 and pnpm 10.27.0, repository checkout with dependencies installed.
- Loopback mode: binds `127.0.0.1` by default (`--host localhost` also allowed) and serves plain HTTP without cert/key. Existing loopback behavior, CLI defaults, and tests are preserved.
- LAN mode: a non-loopback `--host` must be an explicit numeric LAN IP and serves plain HTTP unless both `--cert` and `--key` are given (native HTTPS). Plain HTTP is unencrypted: use it only on networks you trust, or terminate HTTPS outside the relay. Cert/key files are read before the DB is opened; missing, empty, or mismatched material aborts startup without creating the DB; a partial pair (one without the other) is rejected. The readiness line advertises the actual scheme and bound host/port (`http://<LAN-IP>:<port>` or `https://<LAN-IP>:<port>`).
- Container bridge bind: `--host 0.0.0.0 --allow-unspecified-bind` is deployment-scoped for the Docker bridge image only (container-internal bind; Docker controls host exposure via `ports:`). It is never a user-facing advertised endpoint and must not be used for direct host runs.
- Certificate provisioning contract (user-owned, no automation): to serve HTTPS, generate or provision a PEM certificate and private key covering the LAN host/IP yourself (example: `openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem -days 365 -nodes -subj "/CN=<LAN-IP>" -addext "subjectAltName=IP:<LAN-IP>"`), then configure trust explicitly via `NODE_EXTRA_CA_CERTS`/OS trust as applicable to the runtime. Verification is never disabled in app code or tests. The relay and Docker image never perform this step for you.
- Two-profile setup/pairing flow over LAN: start one relay in LAN mode, set the same `http://<LAN-IP>:<port>` (or `https://<LAN-IP>:<port>` when you provided TLS) endpoint plus token on both app instances (for HTTPS, ensure both runtimes trust the certificate), Connect both (each registers its own device code), then complete the explicit device-code request -> accept flow and sync covered topic/message/message-block data. Registration and channel membership persist in Main config/SQLite and on the relay across app restarts.
- Client endpoint policy: both valid `http://` and `https://` endpoints are accepted for loopback and non-loopback hosts. A non-loopback `http://` endpoint shows a visible non-blocking warning in Sync Settings (unencrypted transport); saving and syncing remain possible.
- Persistent DB/token handling: `--db` selects the relay SQLite file and `--token` the Bearer token. Restart with the same `--db`/`--token` (plus `--cert`/`--key` for HTTPS) retains relay registration/channel membership/operations/per-channel cursors. A normal stop (`SIGTERM`/`SIGINT`) shuts down exactly once — stops accepting requests, closes active SSE streams, closes the HTTPS/HTTP server and SQLite connection cleanly — and never deletes the DB. User-owned relay data (DB/cert/key files) is never deleted by normal stop/close.
- Restart/recovery behavior: after a relay restart on the same DB/token (and cert/key for HTTPS), retained operations stay contiguous from per-channel cursor 0, per-channel sequences continue, paired channel membership needs no re-pairing, queued edits push and converge, and per-channel cursors advance. A short interruption surfaces a truthful sync error with pending work retained and cursors pinned until recovery.
- Explicit boundary: this path provides a runnable LAN relay contract and validation only. It claims no certificate issuance/installation automation, no certificate rotation, no backups, no capacity/SLA, no WAN/public-internet exposure, no WAL/OS-crash/power-loss durability, and no full production operations (deployment, upgrade, backup).

### 2.2 Docker Compose relay deployment (Linux x64/arm64 home servers)

Docker Compose is the supported containerized relay deployment shape for
this phase, alongside the direct host-run shapes in Section 2.1.
Target: Linux x64/arm64 home servers only. Docker Desktop/macOS/Windows
support is not claimed. The relay generates no certificates: by default the
deployment serves plain HTTP; HTTPS is only served when the operator mounts
their own certificate/key files (passthrough, never generated or managed).
Contract files: `docker-compose.yml` (root), root `.dockerignore` (keeps
secrets and unrelated artifacts out of the build context),
`deploy/sync-relay/Dockerfile` (multi-arch-capable Node 24.11.1 image with
pnpm 10.27.0, carrying only the relay server, the shared sync contract, and
a reproducible better-sqlite3 + tsx install from
`deploy/sync-relay/pnpm-lock.yaml` via `--frozen-lockfile` — never the
Electron app, never OpenSSL),
`deploy/sync-relay/docker-entrypoint.sh` (restrictive umask 077, then init,
then exec), and `deploy/sync-relay/relay-init.mjs` (testable without
Docker).

- Compose contract: standard bridge networking with `ports:`
  (`${RELAY_PORT:-3030}:3030`; the container listens on 3030), restart
  policy `unless-stopped`, and one user-owned persistent bind mount
  (`./relay-data:/data`) holding the SQLite DB/WAL sidecars, the mode-0600
  token file, and (only when `RELAY_PUBLIC_URL` is set) the public
  `relay-config.cherry` artifact. There is no `network_mode: host`, no
  `RELAY_LAN_IP`, and no wildcard advertised endpoint: the relay binds the
  container-internal `0.0.0.0` (deployment-scoped
  `--allow-unspecified-bind`) and Docker controls host exposure.
- First-start init (once; reused byte-identical on restart): sets a
  restrictive umask (077) so DB WAL/SHM sidecars default to owner-only,
  creates a cryptographically strong token (0600, atomic temp + rename),
  ensures the DB placeholder, and — only when `RELAY_PUBLIC_URL` is set —
  writes a small versioned public config carrying only operator-supplied
  endpoint metadata (public URL, relay name, versions; never any token,
  certificate, or key). Existing token/config artifacts are strictly
  validated before reuse (corrupt token, unknown/secret config keys, bad
  types, invalid issuedAt, or public-URL mismatch fail closed without a
  rewrite). Reused on restart; no automatic rotation, backup, WAN exposure,
  reverse proxy, service manager, or cloud variant in this phase. A legacy
  `RELAY_LAN_IP` variable fails fast with an explicit error. There is no
  independent web admin UI and this path claims no production readiness.
- Transport choices (both explicitly supported): direct LAN HTTP
  (`http://<server>:<port>` — unencrypted; the client shows a visible
  non-blocking warning and the operator accepts responsibility for the
  untrusted network) or externally provided HTTPS (terminate TLS outside
  the relay, or mount your own cert/key via `RELAY_TLS_CERT_FILE` /
  `RELAY_TLS_KEY_FILE` for native relay HTTPS with ordinary verification).
  Docker itself never generates or manages certificates.
- First-start logs show only safe values: container port and bind note,
  token-file path (with retrieval instructions), DB path, config path or
  the no-public-URL note, public URL or none, and TLS passthrough paths or
  the plain-HTTP note, plus first-start/reuse status. The full token is
  never printed and no certificate/private-key artifacts are generated.
- Exact user flow: server `docker compose up -d` -> retrieve the token
   from the protected file (`docker compose exec sync-relay cat
   /data/relay-token`) -> on each Cherry Chat client, enter the server
   endpoint (`http://<server>:<port>` or your externally provided
   `https://...`) plus the token, and Connect: each client registers its own
   device code, then pair devices with an explicit device-code request ->
   accept through the client pairing UI.
   When `RELAY_PUBLIC_URL` is set, `relay-data/relay-config.cherry` records
  the operator-supplied public URL for reference; otherwise users enter the
  endpoint manually.
- Public config schema (`deploy/sync-relay/relay-config.schema.json`,
  version 2): relay name, operator-supplied public URL (`http://` or
  `https://`), and schema/init version metadata; JSON content in
  `relay-config.cherry` (mode 0644, atomic writes). No token, no
  certificate, no key, no fingerprint.
- Entrypoint preserves relay semantics: it runs init, then `exec`s the
  unchanged `scripts/sync-relay/server.ts` CLI with the container-internal
  bridge bind (`--host 0.0.0.0 --allow-unspecified-bind --port/--db`, token
  via the `SYNC_RELAY_TOKEN` env fallback so the secret never appears in
  `ps`, plus `--cert/--key` only when the operator supplied the TLS
  passthrough pair), keeping the CLI startup order (token/TLS validation
  before DB/listen) and direct SIGTERM/SIGINT delivery for graceful
  shutdown.
- No client config-import UI exists in this phase. The intended client step
  is: enter the endpoint, enter the token, connect, and pair via the
  existing pairing UI. Client import work is a separate decision.
- Explicit non-goals: no Web admin UI, no cloud-specific deployment, no
  reverse-proxy setup, no certificate automation, no backup, no rotation,
  and no WAN claims.
- Manual Docker Hub publish with pull-only server deployment (mechanics
  only, execution unvalidated): `.github/workflows/docker-relay-publish.yml`
  is `workflow_dispatch`-only with an optional `ref` input defaulting to
  `jorkey/integration` (manual-run meaning at start: the HEAD of that
  branch; `fetch-depth: 0`). The `relay-checks` job checks out that ref
  once, resolves the full 40-hex commit SHA, and passes it to `publish`
  via a job output; `publish` checks out that exact fixed SHA (never the
  mutable ref again), validates the 40-hex shape, verifies checked-out
  HEAD matches the pinned SHA, and derives the tag by explicitly taking
  the first 12 hex characters with length/character validation. The
  pre-publish gate is intentionally minimal — the relay init contract
  suite (`scripts/sync-relay/__tests__/dockerRelayInit.test.ts`) plus
  `node --check`/`sh -n` static checks, none requiring a Docker daemon —
  and is not the full `pnpm build:check` aggregate gate. On success it
  builds once with Docker Buildx for `linux/amd64,linux/arm64` and pushes
  the same manifest under two tags to
  `docker.io/jorkeyliu/cherry-chat-sync-relay`: one commit-derived
  `sha-<12-hex-commit>` tag plus the mutable `dev` tag (Docker Hub login
  via `DOCKERHUB_USERNAME` / `DOCKERHUB_TOKEN` secrets). After login and
  before push it runs `docker buildx imagetools inspect` on the
  `sha-<12-hex>` reference only: tag exists fails closed, definitive
  not-found continues, and any inconclusive registry answer fails closed
  rather than risking an overwrite; the `dev` tag is explicitly mutable, is
  never immutability-checked, and moves on every publish. It never
  publishes `latest` and never deploys. Dev servers track `dev` pull-only
  by setting the full reference and skipping the local build:
  `SYNC_RELAY_IMAGE=docker.io/jorkeyliu/cherry-chat-sync-relay:dev
  docker compose pull`, then the same `SYNC_RELAY_IMAGE=... docker compose
  up -d --no-build` after every publish. `dev` is mutable and unsuitable as
  a strict production version; use the `sha-<12-hex>` tag to track/roll back
  to an exact commit with the same pull + `up -d --no-build` shape. When
  `SYNC_RELAY_IMAGE` is unset the Compose default keeps the local
  `docker compose up -d --build` path. The workflow refuses an
  already-existing sha tag but concurrent racing publishes of the same sha
  tag are still resolved by the registry, so the workflow alone does not
  claim absolute registry-level immutability. This changes no
  relay protocol or behavior and no application release/update flow; the
  updater/release freeze and identity boundaries are untouched.
- Validation boundary: the init contract is proven by focused tests without
  a Docker daemon (see Section 7). Docker image build/execution was NOT
  validated: the current host is macOS arm64 with no Docker binary/daemon,
  so Linux x64/arm64 image execution remains unproven, and the manual
  publish workflow itself has no successful run evidence yet. This path claims no
  production readiness.

## 3. What current evidence establishes

- Main-owned SQLite remains the chat authority with the operation log captured alongside the enclosing mutation for supported stable paths. The log is sync intent only, not a second authority.
- The relay path moves operations between two profiles and converges them on covered topic, message, and message-block shapes, including offline backlog with retry and fail-closed behavior on authentication failure. Automatic online convergence and cursor-based recovery after short disconnection are validated for those covered shapes; assertions cover sync status, durable cursor advance, outbox drain, and truthful errors.
- Delete/recovery semantics are validated for covered topic/message/message-block paths: online hard delete propagation, offline hard delete with automatic recovery on reconnect, late-child suppression via the parent tombstone, soft-delete topic -> restoreTopic round trip with content preserved, and concurrent delete/edit convergence to a single agreed result without asserting a fixed winner.
- Ordinary edit semantics are validated for covered message shapes: online content edit automatic convergence; paused-transport independent-field edits preserving both fields; paused same-field edits converging to the existing timestamp-then-operationId LWW winner with observable conflict-count evidence; and bounded outbox/app-restart recovery for stable message content edits — clean-close same-message 3 edits, clean-close same-topic 3 messages x1 edit each, clean-close mixed backlog (message A x2 plus B/C x1), controlled SIGTERM single-edit same-profile relaunch, and direct SIGKILL single-edit same-profile relaunch. All are bounded synthetic/disposable macOS Electron E2E cases with the same test-side relay alive across the app relaunch unless stated otherwise.
- Field-clock conflict records may increase even for disjoint edits over an existing baseline clock, while fields still merge. This is current baseline-field-clock recording behavior, not a new product decision.
- The authoritative sync path is strictly authenticated push/pull plus cursor; SSE is notification-only and never decides convergence. Auth precedence (401 before any interruption handling), 503 pause behavior with counter/cursor preservation, resume, and independent push/pull direction barriers are validated under a controlled in-memory network-interruption harness only. Direction-level interruption is additionally proven on the real two-profile path: pull interruption after the relay accepted operations with pull held then auto-convergence, and push interruption with pending retained then automatic retry after release. Batch-internal partial push and page-internal partial pull are explicitly not claimed: relay push is atomic and no deterministic in-request barrier exists.
- Relay-side identical operation replay is accepted idempotently without cursor/opcount growth on the bounded test-side relay. This proves idempotent-accept handling only; it proves no real lost-response client timing.
- Bounded file-backed reference-relay restart is validated for covered shapes: operation/cursor retention and sequence continuity across a controlled owned-process SIGTERM restart with a disposable database, including a pending stable message edit queued during the outage converging after restart through two disposable profiles and Main SQLite IPC. Convergence is decided by strictly authenticated push/pull plus cursor; SSE remains hint-only.
- Deterministic Main-only local logical baseline candidate capture exists as an internal/non-wire bootstrap input (`local_sync_baseline_candidate`, `local-sync-baseline-v1`, inventory `topic-message-stable-block-v1`): one SQLite-consistent read-only transaction over Main chat authority (now including `sync_membership_clock` in the same snapshot) emitting allowlisted current state for topics, stable messages, and stable supported message blocks, plus representable tombstones, entity/field version metadata, and `parentMembershipClock` — topic omits `parentMembershipClock`, `message`/`message_block` carry exact persisted `{timestamp, operationId}` or `null`; missing live child membership is explicit `unversionedMembershipCount` + `unversioned-membership` (prevents `complete`, no substitution/guessing via `entityClock`/`fieldClock`/`createdAt`/current time); malformed/duplicate/orphan and parent-mismatch fail closed — deleted child retained membership with tombstone is allowed, current transient/permanently excluded child metadata may be omitted under truthful existing exclusion diagnostics, true no-row/no-tombstone orphan is rejected; deterministic order, parent closure, truthful symbolic completeness (`complete` within this provisional subset only / `partial` / `unbound`), provisional local watermark observation (never a SYNC-DATA-007 no-gap watermark), pending-outbox count only, and SHA-256 manifest digest over canonical content that now deterministically covers membership. `complete` additionally requires every clocked payload field to carry a field clock and no extra clocks for absent fields (`unversioned-field` otherwise) plus zero `unversionedMembershipCount`. Excluded transient/unsupported rows are never partial shells. A bounded Main-only local validated merge engine (`src/main/services/sync/syncBaselineApply.ts`, `applyLocalSyncBaselineCandidate`) exists for fully versioned complete candidates: one synchronous SQLite transaction unioning chat rows plus entity/field clocks, tombstone keys, and now atomically `sync_membership_clock` (absence never deletes; only explicit tombstones delete; outbox/applied/cursor/channel/conflicts preserved; canonical full-payload shape with required/optional topic overflow semantics and exact field-clock match; differing fields without a local field clock fail closed even with an entity clock while equal values repair metadata; tombstones targeting live unversioned rows including cascade descendants fail closed; membership — only exact `{parentId, timestamp, operationId}` tuple is idempotent, same-parent different clock and different parent are both contradictions and rollback, no max/min/LWW overwrite; deletes use the authoritative FK cascade plus empty-segment cleanup without sync capture; idempotent). It is not wired to relay/bootstrap, cannot resolve unversioned same-ID conflicts, and does not establish convergence or a no-gap watermark. Its canonicalizer/digest (`local-sync-baseline-v1`) is internal/non-wire and is not the public `jcs-sha256-v1` contract (`SYNC-DATA-032`); wire use must go through the explicit wire projection (`projectLocalBaselineToWirePayload`, see §3) and must not upload the local candidate as-is. This is now frame-aware internal `local-sync-baseline-v1`/`topic-message-stable-block-order-v1`/`parent-order-frame-v1` with `011` unbounded parent IDs (see next bullets) — `sync_parent_order_frame` local frames persist via additive `010`/`011` and are captured/merged by frame-aware candidate/apply with normalized effective order, `frameClock` LWW, fixed-point and dense materialization; the pure local payload-only wire projection (`projectLocalBaselineToWirePayload`, see §3 projection bullet) now maps complete candidates to the locked `payload` (including `payload.orderFrames`) with shared-validator self-check — the local candidate itself must still never be uploaded as-is. This proves local capture plus local merge plus local payload projection only; the local path itself claims no publisher barrier, no receiver bootstrap/apply, no reconciliation/repair, and no convergence (the relay resource envelope/persistence surface is owned by §3, not by this local path).
- Dedicated Main SQLite parent-membership clock persistence (additive `009_sync_membership_clock` — `sync_membership_clock` with `child_entity_type`/`child_entity_id` (`message`/`message_block`), immutable `parent_id`, creation `{timestamp, operationId}`): supported local creation paths that carry a trustworthy creation sync operation persist the membership atomically in the enclosing aggregate transaction; accepted remote true creates do likewise; ordinary edits preserve membership, deletion retains historical membership, same-parent accepted reappearance after delete preserves the original membership tuple, different-parent retained metadata/reparent fails closed and rolls back. Pre-existing rows, closure snapshots, transient→stable promotions, and currently unsupported/skipped compound/branch/clone/paste/reset paths remain without membership and therefore truthful unversioned for wire completeness (`entityClock`/`fieldClock`/`createdAt`/current time never substitutes). Capture and apply now handle this truthfully with frame-aware normalization (see next bullets) — candidate reads membership + frames in the same snapshot with `unversionedMembershipCount` + `unversioned-membership` and `missing/incomplete-order-frame` completeness gating and deterministic digest coverage including frames; apply atomically persists/verifies membership with exact-tuple idempotency and contradiction rollback.
- Additive Main SQLite parent-order-frame persistence (additive `010_sync_parent_order_frame` — `sync_parent_order_frame` with `kind`/`parent_id` (`topicMessage`/`messageBlock` only), `(kind,parentId)` key, `frame_version='parent-order-frame-v1'`, `ordered_child_ids_json` strict JSON array, dedicated `{timestamp, operationId}` frameClock): no backfill — pre-existing parents remain frameless; live parents have exactly one winning frame including empty `[]`, hard/tombstoned parents no frames; soft-deleted topic stays live inventory (`deletedAt`) with frame preserved and restore preserves frame while supported message changes on it still maintain frame; hard topic/message cascades (`hardDeleteTopic`/`purgeExpiredTopics`/`emptyTrashTopics`/`resetAssistantTopics`/`deleteMessage`/`deleteBlocks`) remove topic plus descendant `messageBlock` frames atomically; supported transaction-owned message/block membership/order mutations atomically refresh exact final stable-supported child order with frameClock strictly greater than existing frameClock and all included child `parentMembershipClock`s (`max(existingFrameClock, all included membershipClocks)+1`, `MAX_SAFE_INTEGER` exhaustion fail-closed); ordinary content edits without inclusion change do not advance; inclusion transitions (`stable→transient`/`supported→unsupported`) refresh only when all included children have complete real memberships otherwise invalidate frameless — successful `topicMessage` and ordinary-`messageBlock` refreshes under complete trusted membership each mint+enqueue exactly one matching `order_frame` (`SYNC-DATA-048`/F1); missing membership or excluded status invalidates frameless with 0 op (truthful partial), malformed `extra`/`ordered_child_ids_json`/clocks fail closed with enclosing aggregate transaction rollback; unsupported structural paths (`branchMessagesToTopic`, `cloneMessagesToTopic`, `insertMessagesAfterAnchor`, `pasteMessagesToTopic`, `selectAnswerMessage`, `resetMessagesForResend`, `deleteMessagesWithSegments`) invalidate affected parent frames atomically without minting any outbox or frame operations, while `reorderMessages` with complete stored membership now mints/persists the winning `topicMessage` frame plus one matching `order_frame` op in the same transaction (`SYNC-DATA-048`; missing membership still truthfully invalidates); stable→transient transitions on any path invalidate the local `topicMessage` frame with 0 frame op (transient exclusion is not a cross-device structure operation); remote incremental `SyncOperation` now carries `order_frame` for the minting ordinary `topic`/`topicMessage` and `message`/`messageBlock` paths (`SYNC-DATA-048`/`SYNC-CC-024` dual-pair ordinary scope) and stays frame-less/deferred only for compound/unsupported structure paths and other unauthorized domains; internal `local_sync_baseline_candidate` is now frame-aware and captures/derives normalized effective frames in the same read snapshot (see next bullets).
- Frame-aware internal `local_sync_baseline_candidate` (`local-sync-baseline-v1`/`topic-message-stable-block-order-v1`/`parent-order-frame-v1`, `src/main/services/sync/syncBaseline.ts` + `syncFrameEvaluation.ts`): one SQLite-consistent read-only transaction over Main chat authority capturing `sync_membership_clock` + `sync_parent_order_frame` together; for each live `topic`/`message` parent it emits normalized effective `orderedChildIds` (filtered winning list + deterministic `membershipClock>frameClock` suffix sorted by `membershipClock` then `childId` UTF-8 lex) with original `frameClock` unchanged (source DB retains raw winning frame); includes empty `[]` frames for live empty parents and preserves frames for soft-deleted live topics; legacy per-row `sortOrder` and its field clocks are excluded/ignored, payload never carries `sortOrder`; missing/incomplete/unversioned remain exact partial diagnostics (`missing-order-frame`/`incomplete-order-frame`/`unversioned-membership`) never synthesized; deterministic order, parent closure, suppressed-tombstone exclusion, wrong-kind/orphan/mismatch fail-closed, and SHA-256 manifest digest over canonical content covering frames/membership deterministically.
- Frame-aware bounded local validated merge (`src/main/services/sync/syncBaselineApply.ts`, `applyLocalSyncBaselineCandidate` + `syncFrameEvaluation.ts`): one synchronous SQLite transaction unioning chat rows plus entity/field/membership clocks plus `sync_parent_order_frame` winning frames with full validation (schema/inventory/orderFrameVersion/digest/manifest exact keys/bound+complete/frame full-set including empty, `sortOrder` excluded). For each relevant parent it re-evaluates existing local raw winner to current semantic effective sequence against merged live state, then merges incoming normalized frame by `frameClock` LWW — higher wins and persists incoming normalized snapshot for that clock, lower loses, equal clock compares semantic effective sequences (byte-equal alone insufficient) and fails closed on divergence, optionally normalizing stored content while retaining clock; preserves versioned local-only children per cutoff (`<=frameClock` must be covered) and suffix (`>frameClock` sorted) rules, blocks on unversioned local children, and materializes dense `sortOrder 0..n-1` only as local projection; only winning tombstone (legacy `(T,null)` strong barrier `(T,null) > (T,nonNull)` applied uniformly) suppresses, UTF-8 byte-lex tie ordering, and full rollback preserves protected local sync state (`sync_outbox`/`sync_applied`/`sync_state`/channel never mutated). Colon-containing IDs supported in internal keys with strict local Unicode-scalar / operationId shape.
- Additive migration `011_sync_parent_order_frame_parent_id_unbounded` (`sync_parent_order_frame` rebuild, no backfill): removes erroneous `CHECK (length(parent_id) <= 256)` from ordinary parent IDs while preserving rows and all other constraints (`kind`/`frame_version`/`json_valid`/`timestamp`/`operationId 1..256`/no-colon/`PRIMARY KEY(kind,parent_id)`); ordinary `id`/`topicId`/`messageId`/`parentId`/`orderedChildIds` are non-empty valid Unicode scalar with no upper bound, `operationIds` remain `1..256`/no-colon; `ParentMembershipClock` `parentId` mandatory/exact; table rebuild is transactional with FK enforcement off (runner-owned) and copy `SELECT *` then `DROP` old. Normalized representation boundary: candidate emits effective sequence with unchanged clock; apply persists selected normalized snapshot and re-evaluates against merged state.
- Additive migration `012_sync_frame_high_water` (`sync_frame_high_water`, `SYNC-DATA-048` local implementation invariant): per-`(kind, parent_id)` PK with safe-nonnegative `max_timestamp`; backfilled from current winning-frame timestamps (existing rows preserved byte-for-byte); allocation uses max(high-water, existing frame, included membership clocks)+1 and advances the mark in the same transaction (rollback-safe); invalidation deletes winning rows but never lowers the mark; every accepted winning-frame persist (local issuance via `persistParentFrameInTx`, remote incremental apply, shared baseline merge core) advances it; `MAX_SAFE` allocation fails closed. Only max timestamps stored (no operationId); never on wire; never affects candidate completeness/authority. Test isolation clears it alongside winning frames (`clearAllForTests`); profile isolation follows the chat DB; channel switches never clear it (consistent with surviving frames).

- `sync-baseline-wire-v1` 纯协议校验与 `jcs-sha256-v1` 实现已作为纯函数协议模块落地（`packages/shared/sync/baselineWire.ts`，`sync-baseline-wire-v1` / `SYNC-DATA-041`–`SYNC-DATA-045` / `SYNC-CC-021` F-01..F-05，`packages/shared/sync/index.ts` barrel 导出，`canonicalize@2.1.0` 在 root 与 `deploy/sync-relay` 均为部署依赖；模块本体保持 transport-free 纯协议（无 relay/network 调用），但已被 relay baseline resource、client publish/fetch 与 wire projection 消费；除该 baseline PUT/GET 路径外没有其他远程协议上线）：固定 lowerCamelCase wire types/constants（`WIRE_VERSION='sync-baseline-wire-v1'` / `PAYLOAD_SCHEMA='chat-core-baseline-v1'` / `INVENTORY_VERSION='topic-message-stable-block-order-v1'` / `ORDER_FRAME_VERSION='parent-order-frame-v1'` / `SCOPE` / `DIGEST_SCHEME='jcs-sha256-v1'` / `COMPLETENESS_COMPLETE='complete'`；`MAX_SAFE_INT`/`OPERATION_ID_MAX_LENGTH=256`/`TRANSIENT_STATUSES`/`UNSUPPORTED_BLOCK_TYPES`）；strict raw JSON parse 与 decoded duplicate-key 拒绝（`parseStrictJson`/`parseEnvelopeJson`/`parsePayloadJson` 先 `assertNoDuplicateKeys` 扫描 decoded key 再 `JSON.parse`，重复 key fail-closed，语法/尾随 token/未闭合由 `JSON.parse` 判定，`scanForIllegalScalars` 迭代栈避免 deep RangeError，`assertNoDuplicateKeys` 不导出）；exact-key/version/I-JSON/Unicode/safe-int/clock/status/block-type 校验（`assertExactKeys` 缺失/未知均 fail-closed，仅 `payload` 对象、`manifest`/`entity`/`tombstone`/`orderFrame`/`clock` 固定键，版本常量严格相等，`id`/`parentId` 非空 valid Unicode scalar 拒绝 lone surrogate，`string|null`/`boolean|null`、时间字段 verbatim I-JSON `string|null` 不解析不归一化，所有键固定存在，`operationId` 非空 ≤256 且不含 `:`，`timestamp`/`watermark`/`counts` safe non-negative int，`channelId` 非空 Unicode scalar，`status` 拒绝 `streaming/pending/processing/searching`，`type` 大小写/空白无关拒绝 `tool/file/image/video/citation`）；UTF-8 byte lex 数组序与去重（`topics`/`messages`/`messageBlocks` 按 `id` UTF-8 严格递增，`tombstones` 按 `entityType` rank→`entityId` UTF-8，`orderFrames` 按 `kind` rank→`parentId` UTF-8，`orderedChildIds` 去重，`compareUtf8ByteLex` 以 `TextEncoder` UTF-8 bytes 比较）；parent/entity/tombstone/order-frame closure（`message.topicId` 必 live `topic`，`messageBlock.messageId` 必 live `message`，live↔tombstone 互斥，frame `frameVersion`/`kind`/`parentId` 覆盖校验，每 live `topic` exactly-one `topicMessage` frame、每 live `message` exactly-one `messageBlock` frame 含空 `[]`，无 tombstoned/excluded/unknown parent 的 frame）；`parentMembershipClock` cutoff 后缀（`orderedChildIds` 长度精确等于 live children 且含全部 live children，`membershipClock > frameClock` 必须为连续后缀且按 `membershipClock`→`childId` UTF-8 确定性排序、`<= frameClock` 不得后置，否则 `400`/fail-closed）；manifest 唯一重算（`validateManifestRecompute` 从已验证 `payload` 重算 `liveCounts`/`tombstoneCounts`/`frameCounts` 与 `completeness:'complete'`，声明不匹配即 `400`/fail-closed 即便 digest 自洽）；legacy tombstone barrier 比较（`compareDeletionClocks`/`compareClocks`/`compareMembershipToFrameClock`，`(T,null) > (T,nonNull)` 抑制全部同 `T` live 操作，两 `null` 等价，新 `operationId` 必须非空不可伪造且为 legacy 兼容序，`deletionClock.operationId` 仅 tombstone 允许 `null`）；仅 `payload` JCS canonicalization 与可注入 SHA-256 digest verification（`canonicalizePayload` 先严格 `validatePayload` 再 `canonicalize@2.1.0` 对 `payload` 对象、`computeSyncDigest`/`verifySyncDigest`/`verifyEnvelopeDigest` 仅对 canonical `payload` UTF-8 bytes 注入 `hashHex` → lowercase 64hex，outer `digest` 键明确、`digestScheme='jcs-sha256-v1'`、`wireVersion`/`channelId`/`watermark` 均不入 digest，`verifyEnvelopeDigest` 先 `validateEnvelope` 再重算 compare，`canonicalizePayload` 非法 Unicode/surrogate fail-closed 杜绝 `canonicalize@2.1.0` lone-surrogate gap）；shared barrel 已导出（`packages/shared/sync/index.ts` `export * from './baselineWire'`）；`canonicalize@2.1.0` root 与 `deploy/sync-relay` 部署依赖。Relay baseline resource 已调用其 strict validator/digest（见 §3）；publisher barrier（`SyncClient.publishBaseline` + `SyncService.publishBaseline`）与 receiver bootstrap/apply（`SyncClient.fetchBaseline` + wire merge + `sync()` cursor-0 触发）均已接入并由 §3/§7 覆盖 — 模块本身仍保持 transport-free，可独立复验。
- Pure local transport-free baseline wire `payload` projection (`src/main/services/sync/syncBaselineWireProjection.ts`, `projectLocalBaselineToWirePayload` + `computeWirePayloadDigest`/`verifyWirePayloadDigest`, Main-only, no transport): maps a `complete` frame-aware `local-sync-baseline-v1` candidate to the strictly locked `sync-baseline-wire-v1` `payload` object only — never an envelope. Strips every local internal diagnostic (`pendingOutboxCount`, `observationBinding`, `observedLocalChannelKey`/`observedLocalCursor`, `unversioned`/`excluded`/`orphan`/`aggregate` counters, `reasons`/`completenessReasons`, local `kind`/`schemaVersion` envelope and local manifest digest); maps topics/messages/messageBlocks in full-state with fixed keys (`message_block` → `messageBlock`, `parentMembershipClock.parentId` verified then stripped to `{timestamp, operationId}`), tombstones (`message_block` → `messageBlock`, legacy `null operationId` preserved as strong barrier), `parent-order-frame-v1` frames (effective order incl. empty `[]` preserved under original `frameClock`), and a recomputed wire manifest (`payloadSchema`/`inventoryVersion`/`orderFrameVersion`/`scope` + `liveCounts`/`tombstoneCounts`/`frameCounts`/`completeness:'complete'`); sorts entities/tombstones/frames with the shared `compareUtf8ByteLex` contract (tombstone type rank, frame kind rank) while `orderedChildIds` preserve business order; self-validates every output through the existing shared strict `validatePayload` plus payload-only `jcs-sha256-v1` `computeSyncDigest`/`verifySyncDigest` (Node `crypto` SHA-256), never reimplementing protocol rules. Only `complete` + bound + outbox-empty + fully clocked candidates project; `partial`/`unbound`, version/kind mismatch, missing entity/field/membership clocks, and membership parent mismatch fail closed. Proven by `src/main/services/sync/__tests__/syncBaselineWireProjection.test.ts` (14 focused tests: determinism, diagnostics stripping, UTF-8 byte-lex incl. `U+E000` vs `U+10000`, frame-aware incl. empty frames, tombstone mapping incl. legacy `null`, manifest, digest self-verification, and illegal/incomplete rejection); this projection module itself has no transport: no publisher barrier, no receiver wire bootstrap/apply, no remote incremental frame op contract, no IPC/UI change.
- Relay baseline resource (`PUT /sync/baseline` publish/idempotent-replace + `GET /sync/baseline` fetch current, `SYNC-CC-022`/`SYNC-DATA-046`, `scripts/sync-relay/server.ts` reference relay only): per-channel single current-effective `sync-baseline-wire-v1` envelope persisted in SQLite (`sync_channel_baselines`, schema `cc-2` purely additive over `cc-1` — devices/channels/memberships/requests/operations untouched, restart durable); request/success bodies are directly the locked `SyncEnvelope` JSON; strict validation reuses the shared `baselineWire` parser/validator with raw-text duplicate-key rejection (no copied rules) plus Node-crypto payload-only `jcs-sha256-v1` recompute (byte-identity only); watermarks gated by `N <= head` (empty accepts first relay-confirmed N; `N < current`/`same-N divergent`/`stale` → `409 baseline-conflict`; full-match same-N → idempotent `200`; above-head → `400 watermark-above-head`); auth follows the existing plane (`401 unauthorized`, `403` credential/`pairing-required`/`channel-mismatch`, GET `404 baseline-not-found`, `500 store-unavailable`, all exact `{error}`); each publish decides inside one synchronous per-channel transaction (serialized, no split-brain); full op-log retention is untouched so N+1 replay survives publishes; no size threshold/`413` on this resource. Proven by `scripts/sync-relay/__tests__/baselineResource.test.ts` (15 focused tests: empty first publish + GET, idempotent exact, canonical-equivalent same-N 200, divergent/lower 409 with post-conflict idempotency, higher replace, above-head 400, wrong-channel 403 + GET isolation, auth matrix, bad envelope/digest 400 incl. duplicate keys, multibyte UTF-8 cross-chunk decode, concurrent single-winner + identical-concurrent, N+1 retention, file restart, cc-1 forward migration, unknown-marker fail-closed with zero side effects). No renderer/preload/IPC/UI change; the PUT publisher/barrier path is implemented separately as the verification-only explicit entry (`SYNC-DATA-025`/`026`/`027`/`032`, `SYNC-CC-019`/`020`/`021`/`022`, see below); remote frame ops and size/compression/lease/GC remain unimplemented. Receiver bootstrap/apply is implemented separately (`SYNC-CC-023`/`SYNC-DATA-047`, see next bullet).
- Device connection and pairing implement the [connection/channel ADR](./sync-connection-channel.md) target model (explicit Connect registers the device with a stable public device code + durable secret and automatic re-attach; pairing is a device-code request plus explicit target accept into a hidden per-channel group; push/pull/SSE require paired membership with per-channel contiguous sequencing). Pre-channel invite/founder/global-trust state is reset without migration as applied history (local chats and outbox intent preserved); founder/trust is not a current concept. The shared SYNC-CC conformance core (`tests/e2e/utils/sync-relay-conformance.ts`) runs the same observable cases against both the in-memory TestRelay and the production `createRelayServer` SQLite path. Reference relay remains a test/reference implementation, not production-ready.
- The above is limited implementation regression evidence on the current exact worktree for the covered shapes and the reference/test-side relay only. It establishes no larger/longer backlog or capacity behavior, no WAL/OS-crash/power-loss durability, no production relay lifecycle (deployment, upgrade, backup) readiness, and no compound/structured content, attachment, E2EE, or full product readiness.

- Receiver-first baseline bootstrap closed loop (`SYNC-CC-023`/`SYNC-DATA-047`, implemented 2026-09-11): `SyncClient.fetchBaseline` (`GET /sync/baseline` with existing device-header auth, raw-text strict `parseEnvelopeJson`, `404` strict `{error:'baseline-not-found'}` single-key body as explicit no-baseline (other 404 bodies throw via safe mapping), other non-2xx via existing safe relay `{error}` mapping, abort/timeout managed); `syncBaselineApply.ts` exposes the single validated merge core `mergeValidatedBaselineInTx` for caller-owned transactions (existing `applyLocalSyncBaselineCandidate` API/behavior/tests preserved) plus wire adapter `syncBaselineWireApply.ts` (shared `validateEnvelope`/`verifyEnvelopeDigest`, `messageBlock`→`message_block`, `parentMembershipClock` parentId補, frames, no wire `sortOrder`, no fabricated local diagnostics, no copied LWW rules); `SyncService.sync()` triggers strictly when attached + locally channel-bound + cursor==0 before any ordinary push/pull (cursor>0 never GETs; channel-switch reset 0 may trigger later), `404` continues with existing push/pull, `200` runs one SQLite transaction (merge + `sync_state.cursor=N` with pre-commit cursor-0/channel-match asserts, `sync_outbox`/`sync_applied`/pre-pair rows preserved, outbox never cleared/never required empty; channel/config change fails closed), then pulls from N (`N+1` onward) with post-commit pull failure retaining N; `N==0` is legal (stays 0, repeat GET idempotent, pull seq1 — efficiency tradeoff, no bootstrapped flag). No renderer/preload/IPC/UI change; no SSE hint/compaction/size; remote frame ops only the `SYNC-DATA-048` dual-pair `order_frame` (`topic`+`topicMessage` and `message`+`messageBlock` ordinary scope).

- Publisher barrier publish closed loop (`SYNC-DATA-025`/`026`/`027`/`032`, `SYNC-CC-019`/`020`/`021`/`022`, implemented 2026-09-11, verification-only explicit entry): `SyncClient.publishBaseline` (single client-declared `PUT /sync/baseline` with the locked `sync-baseline-wire-v1` envelope as the direct JSON body — no fence/token/prepare; pre-transport strict `validateEnvelope` so an invalid envelope never PUTs; `200` body strictly parsed via shared `parseEnvelopeJson`; non-2xx `400`/`401`/`403`/`409`/`500` retain the relay `{error}` via the existing safe mapping with no silent overwrite and no automatic fallback; abort/timeout managed like push/pull); `syncBaselinePublish.ts` transport-free envelope builder (`assertBarrierSnapshotProof` + `buildPublishEnvelope`: N proven only by the barrier same-snapshot `observedLocalCursor`/`observedLocalChannelKey`, payload-only `jcs-sha256-v1` digest, locked outer key order, self-validated via shared `validateEnvelope`/`verifyEnvelopeDigest`); `SyncService.publishBaseline()` holds the exclusive barrier (`statusSyncing` + `publishBarrierHeld`, always released in `finally`) with real local-mutation quiescence — not detection-only: all 30 public aggregate mutation entries fail before any SQLite transaction opens (the 14 supported tx-owned methods via the `syncCtx()` entry gate; the other 16 mutating methods via explicit entry guards; reads never gated), so chat rows and outbox intent cannot commit under the barrier; the post-commit hook fallback refuses capture with no outbox write and a durable capture-failure record; remote pull/apply internal writes never enter the aggregate and stay unblocked. No cross-network SQLite write transaction is held; no queueing, lease/ack, or new persisted state. It then runs the strict order attached + channel-bound → outbox drained/empty → pull to an empty page with service-boundary contiguity re-check → same-snapshot capture (logical baseline + clocks + tombstones + binding + cursor N + outbox=0) → locked envelope → pre-PUT channel/cursor re-verify → single PUT → `200` full-match (channel/N/digest) success with `lastSyncAt` refresh, any other outcome a truthful error with cursor/outbox/chat untouched. Any unmet precondition (unquiesced concurrent cycle, non-empty outbox, non-empty/gapped pull, non-`complete`/unbound candidate, missing binding/clocks, post-snapshot cursor/channel move) fails closed with no PUT. Shutdown/stale-config aborts write no durable status and never PUT; channel changes record a truthful `lastError` but never mutate cursor/outbox/chat. Ordinary `sync()` never auto-publishes (receiver/push/pull semantics unchanged); the caller drains via `sync()` first, then publishes explicitly. The sender barrier (`SYNC-DATA-026`) scope is this entry only — the publisher is not claimed trusted/online beyond it, and remote parent-frame ops beyond `SYNC-DATA-048`, size/compression/capacity, lease/ack, retention/compaction/GC, E2EE, attachments/segments/branches/topic ordering/reparent stay explicitly excluded. Proven by `src/main/services/sync/__tests__/syncBaselinePublish.test.ts` (18 focused tests: PUT serialization/single-call/no-fence, 409/400 truthful, invalid-envelope never-PUT, abort; barrier order, success with same-snapshot N/binding/digest invariants, outbox-non-empty/pull-non-empty/unbound/partial/cursor-moved fail-closed with no PUT, 409/400 release with truthful state, shutdown/stale-config release with no PUT, exclusivity/retry; real quiescence — aggregate TX append/ensure fail with no row and no outbox across the pull gate and the snapshot→PUT window, unsupported reorder fails with the winning frame intact, hook fallback refuses capture with no outbox and a durable record, remote apply succeeds under the barrier, mutations recover after release).

- Conservative baseline auto-publish policy (implemented 2026-09-11, local-triggered only): `SyncService.publishBaselineIfEligible()` (typed `published`/`skipped`/`needs-sync`/`deferred` result, `instanceof` + numeric relay `cause.status` only, never exception strings; strict `publishBaseline()` barrier stays the single PUT executor with unchanged fail-closed semantics) plus `SyncAutoService` in-memory-only `localPublishIntent`/`localPublishSeq` (no persistence, no new timer, no `runSync` recursion, no publish-induced auto-sync loop). Only an ordinary `SyncService.sync()` success inside `syncAuto` drain that contains an unconsumed local chat-mutation/enqueue trigger attempts at most one PUT; remote SSE/hint, connection/channel change, reconnect, 30s reconcile, and manual `window.api.sync.sync()` (direct `SyncService.sync()`, bypassing `syncAuto`) never PUT. Only `complete` + bound + watermark N>0 attempts; `partial`/`unbound`/N==0 are non-error `skipped` with no PUT and no `lastError` overwrite. `409 baseline-conflict` defers this round with no immediate re-capture/retry (waits for the next local-change-driven successful sync); other PUT failures also never turn the successful op-log sync into a failure or a tight retry — already truthfully recorded via the barrier path (`lastError` + scoped log), no new UI/IPC contract. Full retention continues; no lease/ack, GC/compaction, frequency config, publisher election, permanent fingerprint, remote frame ops beyond `SYNC-DATA-048`, or UI setting. Merge is conservative: a drain cycle containing an unconsumed local trigger may publish once and then consumes that round's intent; `needs-sync` (outbox-not-drained/barrier-blocked/busy) retains the intent for the next successful cycle; sync failure retains the intent for the existing bounded retry. Barrier-held local mutations keep failing closed before commit with the error truthfully returned to the caller (`{ok:false}` envelope, no faked enqueue); the caller retries after release — no silent loss (if a future path cannot guarantee this, work stops with a reported blocker). Proven by `src/main/services/sync/__tests__/syncAutoPublish.test.ts` 19 focused tests (see §7). Still limited validation, not production-ready.

- Incremental `topicMessage` + `messageBlock` order_frame closed loop (implemented 2026-09-11; `messageBlock` second-pair extension implemented 2026-09-12 under user authorization with no new lock number, same `op:'order_frame'`/five-key wire, limited validation, not production-ready): shared `packages/shared/sync` types plus `validateSyncOperationStrict` carry the dedicated `order_frame` branch (strictly closed five-key `parent-order-frame-v1` payload with exactly two legal pairs — `entityType:'topic'`+`kind:'topicMessage'` and `entityType:'message'`+`kind:'messageBlock'`, each `entityId===payload.parentId`, envelope `id`/`timestamp` mirroring `frameClock`, `deviceId` envelope-only; unknown/version/kind/cross-pair/duplicate/mirror mismatches, upsert-allowlist entry, and delete-with-frame all fail closed; old clients fail closed on the new op). Issuance mints the winning frame plus exactly one matching `order_frame` reusing that clock in the same SQLite transaction for empty-topic establish, stable `appendMessage`, `deleteMessage`, `deleteMessages`, membership-complete `reorderMessages`, and transient→stable promotion inclusion (`updateMessage`, `appendMessage` promotion, `updateMessageAndBlocks`) with the stable entity op / trustworthy creation membership preceding its frame; promotion rescan supplements entity upsert/field evidence only, never membership (pre-existing/promotion rows stay unversioned per 009); missing membership truthfully invalidates with 0 op while the user mutation succeeds and the candidate stays truthful partial; pure content edits and brand-new transient appends mint no frame op; compound paths keep zero frame ops; outbox push sorts frames after member entity ops. Errata (narrow): stable→transient is not a cross-device structure operation — transient status never rides the wire, so all stable→transient paths invalidate the local frame with 0 op (user mutation succeeds, candidate truthful partial); pure exclusion never enters the relay. Remote apply never lets a frame exclude a receiver-alive/stable member (fail-closed coverage gate with rollback and no cursor advance); `SyncOrphanError` stays reserved for genuinely resolvable arrival gaps (unknown parent, listed member not yet arrived); missing local membership, wrong parent, and equal-clock divergence stay fail-closed. Post-invalidation re-mint may reuse a timestamp with a fresh id (allocation is membership-derived once the high-water row is gone); receivers holding the pre-invalidation winner treat it as equal-clock idempotent, so order/content/clock-timestamp converge while the stored winner row id may legitimately differ until a future mint supersedes both. Relay push/pull persist and replay the op byte-identically with no schema migration under the same strict validator (`400` illegal, `409` divergent id, identical idempotent with no cursor growth). Remote apply runs before generic upsert/delete in one transaction (idempotent `sync_applied` with equal-clock semantic compare and conflict-tolerant insert; unknown parent/member as retryable orphan via the existing buffer with no poison-ack; tombstoned parent consumed; incomplete/wrong-parent/missing-membership/equal-divergence fail closed with rollback and no cursor advance; winner persists plus deterministic dense `0..n-1` materialization; never creates/deletes/reparents, never resurrects, never overwrites content). Proven by `src/main/services/sync/__tests__/syncTopicMessageOrderFrame.test.ts` (15 focused tests: validator closure, atomic issuance with clock mirror and same-tx rollback, reorder complete/incomplete, unsupported zero-op, apply newer/older/equal-idempotent/equal-divergent/deleted/unknown/wrong-parent/suffix/incomplete/out-of-order, real-relay push/pull `400`/`409`/idempotent, dual-profile real-relay append/reorder/delete convergence without baseline) plus updated `syncParentOrderFrame` reorder, `syncBaselineEndToEnd`/`syncAutoPublishEndToEnd` N+1 frame-advance assertions (see §7). ordinary `messageBlock` inclusion/order paths now issue the second legal pair (`appendMessage` stable create/inclusion, `updateBlocks`/`updateSingleBlock`/`updateMessageAndBlocks` true-new + promotion inclusion, `bulkAddBlocks` stable creates, `deleteBlocks`/block deletes, parent transient→stable complete frame; exclusion/unsupported invalidate with 0 op, pure edits 0 op, no block reorder API); all compound paths stay deferred; no complete-structure-convergence claim (no full structure without a complete membership/frame set). Still limited validation, not production-ready.

## 4. Current gap to target

- Data convergence per the approved [Sync Data Convergence ADR](./sync-data-convergence.md) (`SYNC-DATA-*`, including `SYNC-DATA-019`–`SYNC-DATA-045` / `SYNC-CC-021`) 仍未实现到收敛：dedicated `sync_membership_clock` (009) 真实创建 `{timestamp, operationId}` 持久化已落地于支持路径（见 §3），dedicated `sync_parent_order_frame` (010) per-parent `parent-order-frame-v1` winning-frame source truthfully persisted locally with no backfill（见 §3 — persistent `(kind,parentId)` frame with ordered child IDs + dedicated frameClock, empty `[]` for live empty parents, no frame for hard/tombstoned parents, soft-deleted topic live inventory preserved, hard cascades removed, inclusion/unsupported invalidation, fail-closed, no outbox/frame mint），additive rebuild `011_sync_parent_order_frame_parent_id_unbounded` 已移除普通 parent ID 上的 256 上限（operationIds 仍 1..256/no-colon，rows preserved），且 deterministic frame-aware local baseline candidate（`local_sync_baseline_candidate` at `local-sync-baseline-v1` / `topic-message-stable-block-order-v1` / `parent-order-frame-v1`）现已在同一 SQLite 快照内读取 `sync_membership_clock` + `sync_parent_order_frame`（topic 省略 `parentMembershipClock`，`message`/`message_block` 携带精确持久化时钟或 `null`；normalized effective `orderedChildIds` under original `frameClock` including empty frames; missing/incomplete/unversioned 为显式 `missing/incomplete-order-frame` + `unversioned-membership` 且阻止 `complete`，不合成；`sortOrder` 已不再承载/合并，legacy `sortOrder` clocks 忽略，digest 确定性覆盖 frames/membership）且 bounded frame-aware local merge（`applyLocalSyncBaselineCandidate`）现已原子持久化/校验 membership + winning frames（`frameClock` LWW + equal-clock semantic effective comparison + fixed-point re-evaluation + versioned local-only cutoff/suffix 保留/ unversioned 阻断 + dense `sortOrder 0..n-1` 物化，strong barrier + UTF-8 tie，full rollback）— 此五者已为内部本地候选/应用/持久化/线载荷投影闭环（纯本地 `payload` 投影已落地，见 §3：完整候选映射为锁定 `payload`，内部诊断已剥离，不输出 envelope），receiver bootstrap 已落地（`SYNC-CC-023`/`SYNC-DATA-047`，见 §3），publisher barrier 已作为显式仅限验证入口落地（`SyncService.publishBaseline` 单次 client-declared `PUT`，无 auto-publish，见 §3）且 publisher 仍不宣称可信/自动上线 — 剩余未实现包括：relay envelope binding 的客户端侧 barrier/snapshot 已落地 publish 入口（含 outer `{wireVersion, channelId, watermark, digestScheme, digest}` + payload-only `jcs-sha256-v1` outer `digest` 的 JCS publish，relay 侧 envelope 校验/绑定/持久化/发布/获取已实现 `SYNC-CC-022`/`SYNC-DATA-046`）；receiver wire bootstrap 已实现（见 §3）；auto-publish 策略、lease/ack、stale-`409`-beyond-relay 仍为 code-less target；远程增量 parent-frame operation contract 已覆盖 `topic`/`topicMessage` 与 `message`/`messageBlock` ordinary 路径的 `order_frame`（`SYNC-DATA-048`/`SYNC-CC-024` dual-pair ordinary scope，见 §3），frame-less/deferred 仅剩 compound/unsupported 结构路径与其他未授权域（其远程 `SyncOperation` 保持不变/frame-less；普通 `fieldClocks` 仍含旧 `sortOrder` 语义但已被本地 baseline 路径忽略；缺 membership/excluded 状态仍 invalidate/partial，无手动 block reorder、无完整结构收敛）；size/compression/lease/GC 仍 deferred。

- Coverage beyond the validated topic, message, and message-block shapes remains unproven, including compound/complex operations, ordering under replay (now governed target-only by parent-scoped order frames per `SYNC-DATA-033`–`SYNC-DATA-040` plus wire inventory v1 `topic-message-stable-block-order-v1`/`parent-order-frame-v1` at `sync-baseline-wire-v1`/`chat-core-baseline-v1` per `SYNC-DATA-041`–`SYNC-DATA-045` / `SYNC-CC-021` F-01..F-05 closed plus fixed-field inventory with **locked lowerCamelCase JSON spelling per §10A.1** (F-02/F-03/F-05): `wireVersion` only outer envelope / `payloadSchema`/`inventoryVersion`/`orderFrameVersion` only inside `payload.manifest` (F-01); outer `{wireVersion, channelId, watermark, digestScheme, digest, payload}` and `payload` root/manifest/entity/tombstone/order-frame/clock keys fixed (order-frame keys locked in v1; path/HTTP verb/TS/DB still deferred — updated `SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021`); type/value domain fixed per F-03 (`id`/parent non-empty valid Unicode scalar; string fields `string|null`; boolean `boolean|null`; time fields verbatim I-JSON `string|null` not parsed, not LWW; all keys fixed existence); parent/frame full-set (F-04, every live included parent exactly one frame including empty `[]`, tombstoned/excluded parent no frame, `orderedChildIds` exactly inventory live children); tombstone legacy `null` strong barrier `(T,null) > (T,nonNull)` suppressing all equal-T live ops (F-05, two `null` equivalent, new `operationId` non-empty non-forge, legacy compatibility order); order frames are versioned logical payload in `payload.orderFrames` and enter `jcs-sha256-v1` via **only** `payload` with outer `digest` key `digest` (F-05) with manifest uniquely recomputed from validated `payload` plus closure/membership/frame coverage (`400`/fail-closed even if digest self-consistent, F-05); normative `parentMembershipClock` (wire key `parentMembershipClock` locked, column mapping deferred) with membership-clock-gated completeness and deterministic append per `SYNC-DATA-035`/`SYNC-DATA-043`; per-parent persistent winning frame with fixed-point re-evaluation and atomic transaction per `SYNC-DATA-036` plus per-version strictly closed with fixed logical fields / transient-status & `tool/file/image/video/citation` gate / clock shapes / exactly-one field-clock + required membership clock / tombstone strong barrier / exactly-one winning frame including empty / fixed `completeness:'complete'` manifest recompute / UTF-8 byte lexicographic arrays / duplicate fail-closed per `SYNC-DATA-042`–`SYNC-DATA-045` — pure `baselineWire` validator already enforces wire v1 field inventory compliance (exactly-one field clocks / required `parentMembershipClock` / no `sortOrder` / fixed keys `messageBlock`/`parentMembershipClock`/`orderedChildIds` etc. / strong barrier / recompute — pure protocol only), but local candidate is now frame-aware with same-snapshot normalized effective frames (including empty `[]` and soft-deleted live topics, no `sortOrder` carry/merge, legacy clocks ignored, exact partial diagnostics, digest covers frames/membership) and frame-aware apply with `frameClock` LWW + equal-clock semantic effective comparison + fixed-point re-evaluation + cutoff/suffix preservation + dense `0..n-1` materialization and full rollback; local `sortOrder` remains local-only per `SYNC-DATA-038`/`SYNC-DATA-042`, baseline `payload` frames are emitted as wire `payload.orderFrames` via the pure local projection (`projectLocalBaselineToWirePayload`, see §3) from the candidate's normalized effective sequences with original `frameClock` per `SYNC-DATA-037`/`SYNC-DATA-044` (the candidate itself stays internal), unsupported structural paths invalidate local frames without minting operations), and structured content, attachments, and incomplete snapshots, which stay excluded from sync payloads plus the wire v1 excluded product domains (`segments`, `branches/tree`, `attachments/file bytes`, `tool/citation` rich blocks per `SYNC-DATA-045`). Per-row `message.sortOrder`/`message_block.sortOrder` are excluded from wire inventory per `SYNC-DATA-038`/`SYNC-DATA-042` and per `SYNC-DATA-045` excluded-local domains (`pendingOutboxCount`/`observed` cursor/channel/diagnostics) never enter the digest; dense `sortOrder 0..n-1` remains local projection authority without rewriting SQLite governance; topic ordering is not introduced (only `message`/`block` locked); wire `payload` JSON spelling is now locked per §10A.1/`SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021` (F-02/F-05; order-frame keys locked in v1; `parentMembershipClock` wire key `parentMembershipClock` locked, column mapping deferred) and `current local candidate` stays internal/non-wire per `SYNC-DATA-045` (still pre-lock `message_block`/`sortOrder`/`observationBinding` spelling, not wire-compliant; membership now exposed locally with null for unversioned).
- Scale beyond covered history/outbox sizes remains unproven, including larger/longer backlog and capacity/write-amplification behavior. No capacity threshold or SLA is claimed.
- Lifecycle durability beyond the bounded restart and bounded single-edit app-restart cases remains unproven: multi-edit SIGTERM/SIGKILL backlog combinations are unproven; WAL/OS-crash/power-loss durability is explicitly unproven (direct SIGKILL relaunch proves bounded same-profile pending-edit recovery only, not storage durability under crash or power loss); relay production lifecycle (deployment, upgrade, backup) remains unproven; interruption evidence beyond direction-level push/pull barriers plus the bounded restart is unproven — batch-internal partial push and page-internal partial pull stay unclaimed.
- Push ordering beyond the covered delete/recovery and ordinary-edit paths (parents before children, deferred orphan handling, fail-closed acknowledgements and malformed payloads) and any fixed winner for concurrent delete/edit beyond the current LWW remain ungoverned; user-visible `message`/`block` order is now governed target-only by parent-scoped frames per `SYNC-DATA-033`–`SYNC-DATA-036` plus wire inventory v1 `SYNC-DATA-041`–`SYNC-DATA-045` (fixed fields with F-03 type/domain, stable gate, clocks `{timestamp, operationId}`, tombstone strong barrier `(T,null) > (T,nonNull)` suppressing all equal-T live ops per F-05, exactly-one winning frame including empty per F-04, membership-gated completeness with `membershipClock <= frameClock` incomplete → reject/repair and `membershipClock > frameClock` deterministic append by `membershipClock` then `childId`, per-parent persistent winning frame with fixed-point re-evaluation, atomic transaction, outer `digest` key `digest` with JCS input only `payload` per F-05, locked lowerCamelCase JSON spelling per §10A.1 (F-02; order-frame keys locked in v1), UTF-8 byte lexicographic determinism over locked `payload`, per-version strictly closed, manifest `payload.manifest` `completeness:'complete'` only with recompute `400`/fail-closed per F-05) — local runtime frame handling is now frame-aware for the internal local baseline path: pure `baselineWire` validator already enforces wire v1 invariants (per-field exactly-one clocks / required `parentMembershipClock` / no `sortOrder` / `payload.orderFrames` with fixed keys `kind`/`parentId`/`orderedChildIds`/`frameClock` / `payload`-only JCS with outer `digest` / strong barrier / manifest recompute — pure protocol only), while local persistent winning frames now exist for supported transaction-owned mutations via `010_sync_parent_order_frame` (see §3), candidate captures normalized effective frames in same snapshot including empty/soft-deleted cases, and apply merges winning frames with `frameClock` LWW + equal-clock semantic effective comparison + fixed-point re-evaluation + cutoff/suffix local-only preservation and dense `sortOrder 0..n-1` materialization (see §3); cross-device relay/receiver (envelope binding, persistence/publish/fetch/barrier, receiver bootstrap) are landed (see §3) and the remote incremental frame operation contract is landed for `topic`/`topicMessage` and `message`/`messageBlock` ordinary paths (`SYNC-DATA-048`/`SYNC-CC-024` dual-pair ordinary scope); the pure local payload-only projection is landed (see §3) and remote `SyncOperation` stays frame-less/deferred only for compound/unsupported structure paths and other unauthorized domains with legacy `sortOrder` clocks ignored only on the local baseline path (no manual block reorder, no complete-structure convergence; missing membership/excluded status still invalidates to truthful partial).

- Conflict recovery UX, compound/structured content, attachments, and E2EE remain deferred: same-field resolution stays at the existing timestamp-then-operationId LWW with bounded conflict-count record only; there is no row-level conflict visibility and no dedicated recovery experience; structured content, attachments, and incomplete snapshots stay excluded from sync payloads; E2EE stays unproven; full product readiness stays unclaimed.

## 5. Next decision and step

The service connection + registration + channel namespace/pairing foundation governed by the [Sync Connection, Registration, and Hidden Multi-Channel Pairing ADR](./sync-connection-channel.md) (`SYNC-CC-*`) is implemented on the current exact worktree with limited validation completed (see §7): shared conformance dual-executed (in-memory + production `createRelayServer` SQLite), 5 sync Playwright specs 31/31 including file-backed production restart E2E, and `pnpm build:check` exit 0. The `sync-baseline-wire-v1` pure protocol validator (`packages/shared/sync/baselineWire.ts`, `SYNC-DATA-041`–`SYNC-DATA-045` / `SYNC-CC-021` F-01..F-05, `canonicalize@2.1.0`) is now implemented and proven with 53 focused tests (see §3/§7) as a transport-free protocol module; since 2026-09-11 the relay consumes it for the baseline resource (`PUT`/`GET /sync/baseline`, `SYNC-CC-022`/`SYNC-DATA-046`) and the publisher barrier is now implemented as the verification-only explicit entry per `SYNC-DATA-026` (receiver bootstrap per `SYNC-CC-023`/`SYNC-DATA-047`; see §3). Initial snapshot/existing-data convergence stays a separate pre-existing sync-data problem and is not part of that foundation step.

- Entry: an explicitly activated decision with claim, minimum sufficient method, and stopping condition.
- Exit: documented foundation behavior per the ADR conformance requirements with accepted trade-offs and residual risks. Exit is met as limited implementation regression evidence only; it authorizes no production readiness claim.
- No production rollout follows from this step alone; production authorization remains a separate governed decision. No implementation authorization follows from this document update. The `SYNC-DATA-019`–`SYNC-DATA-045` (including F-01..F-05 plus wire inventory v1 `sync-baseline-wire-v1` **only outer `wireVersion`** / `chat-core-baseline-v1` / `topic-message-stable-block-order-v1` / `parent-order-frame-v1` at `SYNC-DATA-041`–`SYNC-DATA-045` with fixed full-state logical fields plus F-03 type/value domain (`SYNC-DATA-042`), stable gate (`SYNC-DATA-043`), clocks/tombstone/membership with locked lowerCamelCase keys (`entityClock`/`fieldClocks`/`parentMembershipClock`/`deletionClock`/`frameClock`; tombstone strong barrier `(T,null) > (T,nonNull)` per F-05) (`SYNC-DATA-043`), exactly-one winning frame including empty + parent/frame full-set per F-04 / fixed manifest `payload.manifest` with recompute + outer `digest` key clarified + `payload`-only JCS input (F-05) / UTF-8 byte lexicographic determinism over locked `payload` / per-version strictly closed (`SYNC-DATA-044`–`SYNC-DATA-045`, not complete-product sync), parent-scoped order-frame wire logical semantics with order frames as versioned logical payload in `payload.orderFrames` entering `jcs-sha256-v1` via only `payload` per `SYNC-DATA-027`/`SYNC-DATA-032`/`SYNC-DATA-044` with **inventory v1 byte-level JSON spelling now locked per §10A.1** (updated `SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021` F-02/F-05; order-frame keys locked in v1), normative `parentMembershipClock` with membership-clock-gated completeness and deterministic append per `SYNC-DATA-035`/`SYNC-DATA-043`, per-parent persistent winning frame with fixed-point re-evaluation and atomic transaction boundary per `SYNC-DATA-036`, per-row `sortOrder` wire exclusion) / `SYNC-CC-017`–`SYNC-CC-021` baseline persistence/offline-bootstrap/watermark-boundary plus publish-gate/in-flight-protection plus single-stage publish/fetch wire-contract with outer `{wireVersion, channelId, watermark, digestScheme, digest, payload}` envelope (channelId auth-derived, F-01/F-02) plus public digest `jcs-sha256-v1` **over only `payload` with outer `digest` key** plus parent-scoped order-frame target likewise authorizes no relay integration beyond the pure validator. Updated `SYNC-DATA-040`/`SYNC-DATA-045`/`SYNC-CC-021` now lock inventory v1 baseline JSON spelling (outer envelope/`payload` root/manifest/entity/tombstone/order-frame/clock per §10A.1; order-frame keys locked in v1) and leave future op names/endpoint/shared TS types beyond wire DTO/DB tables mapping beyond wire DTO as deferred; `parentMembershipClock` wire JSON key is locked (`parentMembershipClock`); Main SQLite now has dedicated additive `009_sync_membership_clock` (`sync_membership_clock` for `message`/`message_block` with truthful creation `{timestamp, operationId}` for supported paths, no backfill, see §3) while `local_sync_baseline_candidate` (`local-sync-baseline-v1`/`topic-message-stable-block-order-v1`/`parent-order-frame-v1`) remains internal/non-wire but is now frame-aware and truthfully carries `parentMembershipClock` + normalized effective frames (topic omits, `message`/`message_block` exact persisted clock or `null`, missing → `unversionedMembershipCount` + `unversioned-membership`, missing/incomplete frames → `missing/incomplete-order-frame`, no `sortOrder`, digest covers frames/membership) and is atomically merged via frame-aware local validated apply (see §3); the pure local payload-only projection `projectLocalBaselineToWirePayload` (strip diagnostics, map exactly, JCS `payload`-only digest/verify, validate via `baselineWire` without relay) is now implemented and focused-tested (see §3/§7) — the local candidate as-is must still never be uploaded; relay envelope binding/publish/fetch/merge remain target-only with no implementation authorization. Additive `010_sync_parent_order_frame` now truthfully persists per-parent `parent-order-frame-v1` winning frames locally with no backfill (see §3 — empty `[]` for live empty parents, no frame for hard/tombstoned parents) and `011` removes parent-ID 256 cap; `local_sync_baseline_candidate`/`apply` are now frame-aware with normalized frames and dense materialization plus the landed pure local payload-only projection, but relay (envelope binding/persistence/publish/fetch/barrier/receiver) remain absent; topic ordering is not introduced (only `message`/`block` locked, future semantic scope restricted to `topic→message` and `message→block`).
- Next natural implementation boundary (separately activated, **not authorized here**, document only, do not implement): **publish policy and in-flight signaling over the landed publish/receive loop (relay resource + receiver bootstrap + verification-only publisher barrier all landed)** — the loop is now closed for the explicit verification-only path (`PUT`/`GET /sync/baseline` with envelope binding, per-channel single-current `cc-2` persistence, serialized gating, and full-retention N+1 protection per `SYNC-CC-022`/`SYNC-DATA-046`; receiver bootstrap per `SYNC-CC-023`/`SYNC-DATA-047`; publisher barrier per `SYNC-DATA-026` with same-snapshot N proof, see §3), so the next boundary is what stays code-less target only: lease/ack plus stale-`409`-beyond-next-local-change, publisher election, and the remote incremental parent-frame operation contract — that remains separately activated — plus size/compression/GC. The conservative local-triggered auto-publish policy is now implemented (see §3) and is no longer code-less. User requested pause after commit, but document the technical next boundary normally; no implementation authorization is claimed.

## 6. Temporary working judgments

These judgments guide the next step only. They are not product authority and change when evidence requires it.

- Transactional outbox for supported stable mutations: intent is enqueued inside the same aggregate transaction so a failed mutation leaves no orphaned intent. Wire `payload` shape for inventory v1 is locked (§10A.1: outer `{wireVersion, channelId, watermark, digestScheme, digest, payload}`; `payload` root `{payloadSchema, inventoryVersion, orderFrameVersion, scope, topics, messages, messageBlocks, tombstones, orderFrames, manifest}`; clock keys `{timestamp, operationId}`; `parentMembershipClock` wire key locked).
- Authoritative path is strictly authenticated push/pull plus cursor; notification is never data authority: any push hint only wakes the device, and the authenticated pull and reconciliation path decides what converges.
- Field-level handling for covered shapes: creates merge by identity; updates carry only intentional changed fields; independent fields merge; same-field conflicts resolve by the existing timestamp-then-operationId LWW with a bounded observable conflict record while dedicated restore/conflict recovery experience stays deferred. Conflict records may increase even for disjoint edits over an existing baseline clock while fields still merge; this is current recording behavior, not a new winner or UI semantic.
- Deletion of covered shapes wins over late descendants: a late child arriving after the parent tombstone is suppressed and must not resurrect the parent; ordering-only changes before `SYNC-DATA-033` are not propagated as sync operations (after `SYNC-DATA-033`, `message`/`block` structural order changes must be carried by parent-scoped order frames per `SYNC-DATA-033`/`SYNC-DATA-037` with LWW and validity filtering plus normative `parent-membership clock`-gated completeness and deterministic retention per `SYNC-DATA-035` (`membershipClock <= frameClock` not listed is incomplete → reject/repair, not silent append; `membershipClock > frameClock` append by `membershipClock` then `childId`; missing/incomparable fail-closed; duplicate first retained; `parentMembershipClock` wire JSON key `parentMembershipClock` locked per §10A.1/`SYNC-DATA-040`; Main SQLite now additively persists it for supported paths via `009_sync_membership_clock` truthfully without backfill — `entityClock`/`fieldClock`/`createdAt`/current time never substitutes, see §3) and per-parent persistent winning frame with fixed-point re-evaluation plus atomic dense materialization and atomic transaction boundary per `SYNC-DATA-036` (arrival-order independent fixed point; one visible commit covering entity/tombstone/`parent-membership`/winning-frame/all-affected-parents dense `0..n-1`/applied/cursor; `reparent` both sides atomic; otherwise pending/invisible full rollback); per-row `sortOrder` remains wire-excluded per `SYNC-DATA-038` and topic ordering is not introduced — only `message`/`block` locked; order frames are versioned logical payload in `payload.orderFrames` and enter `jcs-sha256-v1` via only `payload` with outer `digest` key `digest` per `SYNC-DATA-027`/`SYNC-DATA-032`/`SYNC-DATA-044` with inventory v1 byte-level JSON spelling locked per §10A.1/`SYNC-DATA-040` (F-02/F-05); locally, per-parent winning frames now persist truthfully for supported mutations via `010_sync_parent_order_frame` with atomic invalidation on unsupported paths (see §3), and candidate/apply are now frame-aware with normalized effective frames + `frameClock` LWW/equal-clock semantic + fixed-point re-evaluation and dense materialization in a cross-parent atomic transaction (see §3); pure `baselineWire` validator already enforces those wire invariants (pure protocol only). Capture failures are reported truthfully and never as silent convergence. Current runtime has no remote frame issuance for `reorder`/`insert-at`/`branch`/`clone`/`paste`/`select-answer` (local frames cover supported transaction-owned mutations only), so no complete-convergence claim extends over unsupported reordered state; `parentMembershipClock` is now persisted for supported creations/accepted remote true creates via 009 (ordinary edits preserve, deletion retains, reappearance same-parent preserves original tuple, different-parent fails closed) and now truthfully exposed in the frame-aware local baseline candidate (topic omits, `message`/`message_block` exact or `null`, missing → `unversionedMembershipCount` + `unversioned-membership`, missing/incomplete frames → `missing/incomplete-order-frame`, no `sortOrder`, digest covers frames/membership) and atomically merged via frame-aware local validated apply (exact membership tuple idempotent, `frameClock` LWW + equal-clock semantic effective, fixed-point, cutoff/suffix, dense, strong barrier, full rollback); wire `payload` spelling is locked per §10A.1, the pure validator is wire-compliant, and the pure local payload-only projection is landed (see §3); the relay baseline resource itself is landed (see §3) while publisher barrier and receiver bootstrap/apply remain unimplemented — runtime/app integration for those stays absent (see §5).
- Restore means soft-delete topic -> restoreTopic round trip with content preserved; hard delete is irreversible and is not a restore feature.
- Payloads carry only allowlisted shareable fields with **locked type/value domain per F-03** (`id`/`topicId`/`messageId` non-empty valid Unicode scalar with no 256 upper bound; `operationId` `1..256`/no-colon; string fields `string|null`; boolean `boolean|null`; time fields verbatim I-JSON `string|null` not parsed, not LWW; all keys fixed existence per `SYNC-DATA-042`); structured content, attachments, incomplete snapshots, credentials, derived data, device-local paths, UI state stay excluded. For ordering, wire truth is parent-scoped order frames per `SYNC-DATA-033`–`SYNC-DATA-037` (order frames are versioned logical payload in `payload.orderFrames` and enter `jcs-sha256-v1` via **only** `payload` with outer `digest` key `digest` per `SYNC-DATA-027`/`SYNC-DATA-032`/`SYNC-DATA-044` F-01/F-05) with per-row `message`/`block` `sortOrder` excluded per `SYNC-DATA-038` while local DB dense `sortOrder 0..n-1` remains projection/query authority without rewriting SQLite governance (persistent winning frame plus fixed-point re-evaluation and atomic transaction per `SYNC-DATA-036`; `parentMembershipClock` wire JSON key `parentMembershipClock` locked per §10A.1/`SYNC-DATA-040` F-02; Main SQLite now additively persists it for supported paths via `009_sync_membership_clock` — see §3 — plus `010_sync_parent_order_frame` locally persists per-parent `parent-order-frame-v1` winning frames with dedicated frameClock (see §3) with atomic invalidation on unsupported paths; local baseline candidate now truthfully carries membership (topic omits, `message`/`message_block` exact or `null`, missing → `unversionedMembershipCount` + `unversioned-membership`, digest covers membership) with atomic apply (exact tuple idempotent, contradictions rollback, no max/min/LWW) but frame-aware candidate/apply now carry normalized effective frames with original `frameClock` (see §3) — pure `baselineWire` validator is wire-compliant for those invariants (pure protocol), and the pure local payload-only projection is landed (see §3); the relay baseline resource itself is landed (see §3) while publisher barrier and receiver bootstrap/apply remain unimplemented — runtime/app integration for those stays absent). Topic ordering not introduced (only `message`/`block` locked). Per-version wire artifacts including frames and logical `parentMembershipClock` are strictly closed with **fixed lowerCamelCase key spelling per §10A.1** (outer `wireVersion`/`channelId`/`watermark`/`digestScheme`/`digest`/`payload`, `payload` root, manifest, entity/tombstone/order-frame/clock — order-frame keys locked in v1): unknown fields/version fail closed per `SYNC-DATA-039`/`SYNC-DATA-045` (`membershipClock <= frameClock` completeness failure is `400`/blocked/repair-required per `SYNC-DATA-035`); future operation names and endpoint/TS types beyond wire DTO/DB mapping beyond wire DTO remain deferred per updated `SYNC-DATA-040` (F-02/F-05).
- Relay interruption evidence is the controlled in-memory pause/resume harness plus the bounded file-backed SIGTERM restart plus bounded direction-level push/pull interruption on the real two-profile path only: disposable database, controlled owned process with ownership/cleanup boundaries, strictly authenticated push/pull plus cursor; the restart and interruption harnesses are explicit test infrastructure, not production lifecycle. No batch-internal partial-push/page-internal partial-pull, WAL/OS-crash/power-loss, capacity/SLA, or production deployment claim; concurrent delete/edit converges to one stable result with no fixed winner asserted beyond the current LWW. Relay-side identical replay accepted idempotently without cursor/opcount growth proves idempotent-accept handling only, not real lost-response client timing.
- Pending-edit recovery is bounded outbox/app-restart recovery for stable message content edits only: clean-close same-message 3 edits, same-topic 3 messages x1, mixed A x2 + B/C x1, controlled SIGTERM single-edit relaunch, and direct SIGKILL single-edit relaunch; multi-edit SIGTERM/SIGKILL backlog combinations stay unproven. Direct SIGKILL proves bounded same-profile recovery only and establishes no WAL/OS-crash/power-loss durability.

## 7. Evidence pointers

- Implementation: `src/main/services/sync/` (operation-log capture, apply, client, connection/registration/channel pairing; `syncBaseline.ts` deterministic Main-only local baseline candidate capture plus `syncBaselineApply.ts` bounded Main-only local validated merge plus `syncTombstoneCodec.ts` shared strict tombstone/channel-key codecs), `packages/shared/sync/` (`baselineWire.ts` `sync-baseline-wire-v1` pure protocol validator + `index.ts` barrel export, plus payload shape, filtering, device-code/pairing validation, and endpoint validation accepting valid `http://`/`https://` endpoints for loopback and non-loopback hosts plus the non-loopback-HTTP warning predicate consumed by Sync Settings; `canonicalize@2.1.0` root `package.json` + `deploy/sync-relay/package.json` deploy dependency with `pnpm-lock.yaml`), `scripts/sync-relay/server.ts` (reference relay, non-production; user entrypoint via `pnpm sync:relay` with `--port/--db/--token/--host/--cert/--key/--allow-unspecified-bind/--help`, loopback and LAN plain HTTP by default, native HTTPS with user-supplied cert/key, container-internal `0.0.0.0` bridge bind only with the deployment flag, exactly-once SIGTERM/SIGINT graceful shutdown), additive sync metadata migrations `005_sync_metadata` + `006_sync_field_merge` + `008_sync_channel_reset` (`008` drops the superseded `007` trust-mirror table; the `007` migration key is retained as applied-history only; pre-channel cursor state is cleared; chats/outbox/field clocks preserved) + `009_sync_membership_clock` (dedicated `sync_membership_clock` for `message`/`message_block` — `child_entity_type`/`child_entity_id`/`parent_id`/`timestamp`/`operationId`; truthful creation persistence for supported local + accepted remote true creates, atomic in aggregate tx; ordinary edits preserve, deletion retains, same-parent reappearance preserves original tuple, different-parent fails closed/rollback; no backfill/guessing, see §3; now frame-aware: `local_sync_baseline_candidate` captures `sync_membership_clock` + `sync_parent_order_frame` in same snapshot with normalized effective order (filtered winning list + deterministic `membershipClock>frameClock` suffix) including empty `[]` and soft-deleted live topics, no `sortOrder` carry/merge (legacy clocks ignored), exact partial diagnostics, digest covers frames/membership, and `applyLocalSyncBaselineCandidate` atomically merges entities/tombstones/membership/winning frames with `frameClock` LWW + equal-clock semantic effective comparison, fixed-point, cutoff/suffix preservation, dense `0..n-1` materialization, strong barrier, full rollback, plus per-parent high-water advance on every accepted winning-frame persist (`012_sync_frame_high_water`); additive `011_sync_parent_order_frame_parent_id_unbounded` rebuilds `sync_parent_order_frame` removing 256 cap on ordinary IDs (operationIds stay 1..256/no-colon) preserving rows; remains internal `local-sync-baseline-v1`/`topic-message-stable-block-order-v1`/`parent-order-frame-v1` local persistence/candidate/apply plus the pure local wire `payload` projection only (`src/main/services/sync/syncBaselineWireProjection.ts`: `complete` candidate → locked `sync-baseline-wire-v1` `payload`, diagnostics stripped, shared `baselineWire` self-validation, payload-only `jcs-sha256-v1` digest/verify; no envelope/relay/network); no publisher barrier/receiver/remote frame ops beyond `SYNC-DATA-048`).
- Pairing behavior (SYNC-CC foundation, current worktree): `tests/e2e/specs/sync/sync-pairing.spec.ts` (real two-profile Connect/register -> device-code request -> accept -> paired sync convergence; registration/membership across app restart; Disconnect -> Connect without reconfiguration; Unpair preserving service and local chats; third-device join; paired-requester refusal and late-accept no-merge; two independent channels with isolation and independent cursors); `tests/e2e/utils/sync-relay-pairing.test.ts` (registration fail-closed, request lifecycle, atomic channel rules, dissolve, unpaired data-plane refusal, cross-channel isolation); `src/main/services/sync/__tests__/syncChannelProtocol.test.ts` (live reference-relay protocol: registration/auth, lifecycle, atomicity, dissolve, per-channel sequencing); `src/main/services/sync/__tests__/syncPairingTrust.test.ts` (explicit Connect/Disconnect, pairing-state mapping, client-side paired-request refusal, channel reconcile); `packages/shared/sync/__tests__/pairing.test.ts` (device-code/credential validation).
- Integrated behavior: `tests/e2e/specs/sync/sync-two-profiles.spec.ts` (two-profile sync scope, including delete/recovery and ordinary-edit/concurrent-edit convergence; bounded outbox/app-restart backlog — clean-close same-message 3 edits, clean-close same-topic 3 messages x1 edit each, clean-close mixed backlog, controlled SIGTERM single-edit same-profile relaunch, direct SIGKILL single-edit same-profile relaunch; direction-level interruption and idempotent replay — pull interruption after push holds reader then auto-converges, push interruption holds relay then auto-retries after release, identical operation replay is idempotent at the relay); `tests/e2e/specs/sync/sync-relay-restart.spec.ts` (bounded file-backed relay SIGTERM restart: operation/cursor retention and post-restart pending-edit convergence).
- Unit behavior: operation-log, apply, and relay suites alongside the paths above, plus `src/main/services/sync/__tests__/syncBaseline.test.ts` (focused tests covering `unversioned-field` and now `unversionedMembershipCount` + `unversioned-membership`: strict malformed/duplicate/orphan/parent-mismatch membership validation — deleted child with tombstone allowed, transient/permanently excluded may be omitted under truthful exclusion diagnostics, true orphan rejected; topic omits `parentMembershipClock`, `message`/`message_block` exact or `null`, no substitution, digest deterministically covers membership; deterministic candidate: repeat/insertion-order canonical equivalence, digest sensitivity, allowlist non-leakage, transient/unsupported exclusion with bounded reasons, parent closure, tombstone coexistence and fail-closed parsing, field-clock allowlisting, watermark bound/unbound and fail-closed metadata, pending-outbox count without payload exposure, full read-only state preservation) plus `src/main/services/sync/__tests__/syncBaselineApply.test.ts` (focused tests for bounded local validated merge including membership: two in-memory Main DBs — independent union, independent-field merge, same-field winner and operationId tie-break, weak-incoming loss, unversioned-collision rollback, absence preservation, winning/losing tombstones with parent containment and stale-descendant suppression and live+tombstone version resolution, reparent rollback, malformed/partial/unbound/tampered/version/duplicate rejection without mutation, outbox/applied/cursor/channel/conflict preservation with no outbox creation, idempotence, mid-transaction rollback via reparent, plus F1 canonical required/unexpected/type-shape and optional-overflow exact-clock rules with full inserted-field verification, F2 unversioned live/cascade tombstone rejection, F3 missing-field-clock strict repair semantics, F4 authoritative topic/message/block delete coverage for file_refs/segments/memberships with delete idempotence, plus membership exact-tuple idempotency vs same-parent diff clock / different parent contradictions + rollback and no max/min/LWW), plus `packages/shared/sync/__tests__/baselineWire.test.ts` (53 tests: gold vector + canonical `jcs-sha256-v1` digest `a67d76b7…` over only `payload` with outer `digest`, insertion-order insensitivity, strict raw JSON duplicate-key rejection including `\u0061` decoded equivalence, exact-key/unknown/missing/sortOrder/version/transient-status/unsupported-block-type/lone-surrogate/safe-int/operationId colon/length/exact `fieldClocks`/`parentMembershipClock` fail-closed, UTF-8 byte lex order including `U+E000` vs `U+10000` determinism and duplicate/tombstone/frame/orderedChildIds duplicates, membershipClock suffix contiguous + deterministically sorted, manifest recompute `400` even if digest self-consistent, empty-frame/full-set closure, legacy `(T,null)>(T,nonNull)` barrier, `canonicalize@2.1.0` `payload`-only + injectable `hashHex` `Uint8Array` + lowercase 64hex, deep JSON iterative stack, RFC8785 vectors) plus `src/main/services/chatDb/__tests__/migration009.test.ts` (additive DDL + parent-index + no-backfill + CHECK `message`/`message_block` only + re-apply preservation) plus `src/main/services/sync/__tests__/syncMembershipClock.test.ts` (supported local creation atomically persists `parentMembershipClock`, ordinary edits preserve, deletion retains, same-parent reappearance after delete preserves original tuple, different-parent fails closed/rollback, pre-existing/closure/promotion/unsupported paths stay truthful unversioned without guessing, `entityClock`/`fieldClock`/`createdAt`/now never substitutes, idempotent and conflict rollback) plus affected migration/tombstone coverage (`migration002`/`migration003`/`migration004`/`chatDb`/`ftsProjection` tombstone/segment cleanup) plus `tests/e2e/utils/sync-relay-pause.test.ts` (in-memory pause/resume and direction-barrier determinism), `tests/e2e/utils/sync-relay-process.ts` (explicit file-backed relay lifecycle test harness with ownership/cleanup boundaries; disposable database, controlled owned process), and `scripts/sync-relay/__tests__/relayUserEntrypoint.test.ts` (user-entrypoint CLI contract: `sync:relay` mapping, arg/env precedence, loopback/host/port validation, `--help`, graceful SIGTERM retaining the DB with restart continuity), plus `src/main/services/sync/__tests__/syncBaselineWireProjection.test.ts` (14 focused tests for the pure local wire `payload` projection: locked shape with no envelope, full diagnostics stripping, UTF-8 byte-lex ordering incl. `U+E000` vs `U+10000`, frame-aware content incl. empty frames, tombstone mapping incl. legacy `null` barrier, manifest recompute, payload-only `jcs-sha256-v1` digest self-verification, illegal/incomplete fail-closed rejection).
- User-entrypoint persistence: `tests/e2e/specs/sync/sync-user-entrypoint.spec.ts` (two real profiles through the user entrypoint file with explicit `--db/--token`: independent config, registration/channel membership, covered sync, relay stop with truthful failure and pinned per-channel cursors, same DB/token restart with retained registration/channel membership/operations/per-channel cursors and post-restart convergence) via `tests/e2e/utils/sync-relay-user-entrypoint.ts` (same entrypoint file and CLI args as `pnpm sync:relay`; Electron-as-Node launcher only for the ABI 145 lane; disposable owned root only).
- LAN path: `scripts/sync-relay/__tests__/relayLanHttps.test.ts` (non-loopback plain HTTP as an explicit supported transport with health coverage, user-supplied cert/key HTTPS, fail-before-DB on missing/empty/mismatched/partial material, container-internal unspecified bind gated on the deployment flag, native HTTPS health/readiness with disposable cert/key and explicit CA trust, loopback HTTP regression) and `tests/e2e/specs/sync/sync-lan-https.spec.ts` (two real profiles over dynamically discovered non-loopback HTTPS with explicit CA trust: registration/channel membership, covered sync, relay stop with truthful failure and pinned per-channel cursors, same DB/cert/key/token restart with retained registration/channel membership/operations/per-channel cursors and post-restart convergence; explicit skip when no suitable interface is available).
- Docker Compose deployment contract: `scripts/sync-relay/__tests__/dockerRelayInit.test.ts` (focused suite, no Docker daemon: first init creates token/DB with safe modes and secret-free logs and no certificate artifacts; `RELAY_PUBLIC_URL` writes a metadata-only public config; second init reuses byte-identical artifacts; corrupt token/config fails closed; legacy `RELAY_LAN_IP` fails fast; public config matches `deploy/sync-relay/relay-config.schema.json` v2; Dockerfile/Compose/entrypoint structural checks for bridge networking, ports, and the container-internal bind) over `deploy/sync-relay/relay-init.mjs` (first-start init/reuse/fail-closed logic for token/DB/optional public config/optional TLS passthrough), `deploy/sync-relay/docker-entrypoint.sh` (Linux check, init, `exec` of the unchanged relay CLI with `--host 0.0.0.0 --allow-unspecified-bind --port/--db` and token via `SYNC_RELAY_TOKEN`, plus `--cert/--key` only for the operator-supplied passthrough pair), `deploy/sync-relay/Dockerfile` (multi-arch Node 24.11.1 + pnpm 10.27.0, narrow relay payload, no OpenSSL), and root `docker-compose.yml` (standard bridge networking with `ports:`, no `RELAY_LAN_IP`/host network, one `./relay-data:/data` mount). Docker image build/execution not validated on the macOS host (no Docker binary/daemon).
- Current validation (2026-09-11 final exact code state, limited implementation regression only, not production readiness): final `pnpm build:check` exit 0 in 732s covering lint/typecheck/i18n/format/openapi plus full Vitest — 12,544 passed + 90 skipped, 0 failed (aggregate gate scope includes renderer 371/5246; the four renderer stabilization tests in that gate are independent delivery-blocking fixes, not sync functionality); receiver bootstrap focused 25/25 on the canonical pnpm Node lane (Node v24.11.1/pnpm 10.27.0) — `syncBaselineFetch` 11/11, `syncBaselineWireApply` 5/5, `syncBootstrap` 9/9 — plus `baselineResource` 15/15 and shared `baselineWire` 53 tests; focused shared `packages/shared/sync/__tests__/baselineWire.test.ts` 53 tests; `src/main/services/sync/__tests__/syncBaseline.test.ts` / `syncBaselineApply.test.ts` / `syncBaselineFrames.test.ts` (frame-aware/strict/colon) + `syncBaselineColonStrictLegacy.test.ts` + `src/main/services/chatDb/__tests__/migration009.test.ts` + `migration010.test.ts` + `migration011.test.ts` + `src/main/services/sync/__tests__/syncMembershipClock.test.ts` + `syncParentOrderFrame.test.ts` + affected migration/tombstone tests pass; 5 sync Playwright specs 31/31 (existing evidence from prior runs, not re-run in this unit) (`sync-pairing`, `sync-two-profiles`, `sync-relay-restart`, `sync-user-entrypoint`, `sync-lan-https`); shared SYNC-CC conformance dual-executed against the in-memory TestRelay and the production `createRelayServer` SQLite path; file-backed production restart E2E passed. CI adds the focused production file-backed relay restart E2E (`sync-relay-restart-e2e` job: `pnpm test:e2e tests/e2e/specs/sync/sync-relay-restart.spec.ts`). `baselineWire.ts` stays a pure protocol module (no transport of its own) and is now consumed by the relay baseline resource for strict envelope validation plus payload-only digest recompute/verify (see §3); membership read/capture and atomic apply plus additive `010_sync_parent_order_frame` local winning-frame persistence now landed (see §3) but candidate/apply now frame-aware with same-snapshot normalized frames + LWW/equal-clock effective merge + dense materialization; pure local wire `payload` projection now landed (see §3) with 14 focused tests passing; the relay baseline resource is landed (see §3: envelope binding, per-channel single-current `cc-2` persistence, serialized gating, restart durability) plus the receiver bootstrap (see §3) plus the verification-only publisher barrier (see §3), while remote frame ops beyond `SYNC-DATA-048`, auto-publish policy, and lease/ack remain code-less target (see §4/§5), projection consumers beyond the relay resource plus the receiver wire adapter remain local `payload`-only. Still not production-ready: snapshot/backfill (no backfill/guessing is intentional truthful unversioned), capacity, WAL/power-loss, E2EE, attachments, and other §4 gaps unchanged. All evidence remains bounded synthetic/disposable macOS Electron E2E/test-side relay scope and must not be extrapolated to production readiness. Reference/test-side relay remains not production-ready.
- Relay baseline resource (2026-09-11 sixth unit, `SYNC-CC-022`/`SYNC-DATA-046`): `scripts/sync-relay/server.ts` `PUT`/`GET /sync/baseline` with `sync_channel_baselines` (`cc-2` additive, fail-closed on unknown markers, Buffer-concat exact body decode) plus `scripts/sync-relay/__tests__/baselineResource.test.ts` 15 focused tests passing on the canonical pnpm Node lane (Node v24.11.1/pnpm 10.27.0) alongside `pairingAtomicity` (schema marker asserted as literal `cc-2`) and shared `baselineWire` 53 tests; final `pnpm build:check` exit 0 in 732s (lint/typecheck/i18n/format/openapi passed; full Vitest 12,544 passed + 90 skipped, 0 failed) plus receiver bootstrap focused 25/25 (`syncBaselineFetch` 11/11, `syncBaselineWireApply` 5/5, `syncBootstrap` 9/9); final `pnpm build:check` exit 0 in 732s (lint/typecheck/i18n/format/openapi passed; full Vitest 12,544 passed + 90 skipped, 0 failed); no new E2E runs, prior E2E evidence retained (see below). Remote frame ops beyond `SYNC-DATA-048`, auto-publish policy, lease/ack, and size/compression/GC remain code-less target (see §4/§5); receiver bootstrap/apply is now implemented (`SYNC-CC-023`/`SYNC-DATA-047`).
- Publisher barrier publish (2026-09-11, `SYNC-DATA-025`/`026`/`027`/`032`, `SYNC-CC-019`/`020`/`021`/`022`, verification-only): `src/main/services/sync/syncBaselinePublish.ts` (transport-free same-snapshot proof + locked envelope builder) + `SyncClient.publishBaseline` (single client-declared `PUT`) + `SyncService.publishBaseline()` (exclusive barrier with strict quiescence → outbox-drain → empty-pull → same-snapshot → single-PUT → release order) plus `src/main/services/sync/__tests__/syncBaselinePublish.test.ts` 18 focused tests passing on the canonical pnpm Node lane (Node v24.11.1/pnpm 10.27.0, `main-native` project, including real-quiescence cases: mid-flight aggregate TX refusal with no row/outbox, hook-fallback refusal, unblocked remote apply, post-release recovery); neighboring focused suites re-run green in this delivery without code change — `syncBootstrap` + `syncBaselineWireProjection` 23/23 (`main-native`), `syncBaselineFetch` 11/11 (`main`), plus quiescence-adjacent `chatDb aggregate` + `syncAuditFixes` + third/seventh-audit + fail-closed-regression suites green; no full `pnpm build:check` in this delivery (owned by Validation). Ordinary `sync()` never auto-publishes; no renderer/preload/IPC/UI change.
- Baseline publish→persist→bootstrap→N+1贯通回归（`SYNC-CC-022`/`SYNC-DATA-046` + `SYNC-CC-023`/`SYNC-DATA-047` + `SYNC-DATA-025`/`026`，main-native integration，非 Playwright E2E）：`src/main/services/sync/__tests__/syncBaselineEndToEnd.test.ts` 1/1 在 canonical pnpm Node lane 通过（Node v24.11.1/pnpm 10.27.0，`main-native` project）。`SyncService.publishBaseline()` 无生产 IPC/preload/UI 入口且不新增此类入口，故不使用标准 Electron E2E fixture；本 suite 保持最接近真实边界的组合 — 两个真实 SQLite chat DB（`runMigrations`）+ 单例生产 `SyncService`/`ChatDbAggregateService`（按序绑定）+ 真实 reference relay（`createRelayServer` 文件 DB，loopback 临时端口/唯一 token，ownership-scoped cleanup），pairing/push/pull/baseline 全经真实 `fetch`，无 `SyncClient`/HTTP/DB mock。覆盖：同一 paired channel 身份；A 真实 supported 数据集（两有序 message/block + 空 topic + 无 block message tombstone，全字段时钟/membership/frames，经 `updateTopicMetadata` 补齐 topic 可选字段）先经正常 op-log 同步使 relay head=N、A outbox drain；A 显式 `publishBaseline()` 单次真实 `PUT` 成功，真实 `GET` 验证 envelope（watermark/digest/channel），同文件 relay 重启后 `GET` 仍返回同一 digest（单次持久化证据，不重复 `baselineResource` 全部用例）；B（cursor=0，含预置本地 topic/message/block + outbox）经生产 `sync()` 真实 `GET` baseline 单事务 merge 后 cursor=N，再拉取 N+1（A 在 publish 后追加的 message/block 经 op-log），代表性 topic/message/message-block、tombstone 缺席、membership/order-frame（含空 `[]`）与 `sort_order` 有效排序在 B 正确，B 原有行/outbox 按既有语义保留后推送排空；失败边界沿用已有真实覆盖而不复制 — cursor=0 无 baseline `404` 继续 op-log 由 `syncBootstrap.test.ts`（cursor0+404）覆盖。远程增量 frame op 仍为 code-less target：N+1 子行经 op-log 行/`sort_order` 收敛，winning frame 保持 publish 快照（suite 内已按此断言）。提交 `0839345be5` 的 `pnpm build:check` 为 exit 0、727s、12562 passed/90 skipped（仅对该 exact state）；本次仅运行 focused integration 与邻近 focused suites（`syncBaselinePublish` 18/18 + `syncBootstrap` 9/9 + `syncBaselineWireProjection` 14/14、`syncBaselineFetch` 11/11、`baselineResource` 15/15、shared `baselineWire` 53/53），未运行最终 aggregate gate（交 Validation）。仍为 limited validation, not production-ready；§4/§5 缺口与两个 ADR target-only 条文不变。
- Conservative auto-publish (2026-09-11): `src/main/services/sync/__tests__/syncAutoPublish.test.ts` 22 focused tests passing on the canonical pnpm Node lane (Node v24.11.1/pnpm 10.27.0, `main-native` project)
- Auto local-path贯通回归（main-native integration，非 Playwright E2E）：`src/main/services/sync/__tests__/syncAutoPublishEndToEnd.test.ts` 1/1 在 canonical pnpm Node lane 通过（Node v24.11.1/pnpm 10.27.0，`main-native` project，真实 timers + bounded polling，不用 fake timers）。两个真实 SQLite chat DB（`runMigrations`）+ 单例生产 `SyncService`/`ChatDbAggregateService`（按序绑定）+ 真实 reference relay（`createRelayServer` 文件 DB，loopback 临时端口/唯一 token，ownership-scoped cleanup），pairing/push/pull/baseline 全经真实 `fetch`，无 `SyncClient`/HTTP/DB mock。`SyncAutoService` 使用默认 live `runSync`/`tryPublishBaseline`（生产 `sync()`/`publishBaselineIfEligible()`），仅经现有 `SyncAutoDeps.createSubscriber` 注入 noop subscriber（不测 SSE，不新增 test-only API）。覆盖：A 先配对并准备 supported complete 基线形状；auto 启动后初始 drain 无 local intent 时真实 `GET` 为 404 且零 `PUT /sync/baseline`（启动即同步不发布）；随后真实 aggregate `appendMessage` 经生产 `onEnqueue` 置位 local intent，去抖/drain 后生产 `sync()` 成功再经 `publishBaselineIfEligible` 单次真实 `PUT`（delegating `fetch` 计数观测、无行为变更、不改生产，`PUT` 数==1，静置后仍为 1 并 `GET` digest 稳定），outbox drain、cursor/head truthful、`GET` envelope（watermark/channel/digest）正确；停止 A auto 后再经显式手动 `sync()` 推送 N+1（计数仍为 1），B（cursor=0，含预置本地行/outbox）经生产 `sync()` 真实 `GET` 单事务 merge 至 N 再拉取 N+1，代表性 topic/message/block、tombstone 缺席、membership/order-frame（含空 `[]`、publish 快照帧 + N+1 经 op-log 行/`sort_order`）与有效排序在 B 正确，B 既有数据/outbox 按既有语义保留后排空。remote/reconcile/manual 不 `PUT` 不在此重复，已有 `syncAutoPublish.test.ts` state machine 足够。仍为 limited validation, not production-ready；不声称 Electron/preload/UI/多进程/SSE/restart/时延 SLA，不改两个 ADR，不改变 next decision。
- Conservative auto-publish state machine (existing): `syncAuto` intent state machine 10/10 (local→single PUT + consume; remote/reconcile/reconnect-only never PUT; merged local+remote single PUT; sync-failure retain + next-success publish; deferred-conflict consume without tight retry; skipped consume; in-publish new trigger retained via epoch; stop/start no leak) plus `publishBaselineIfEligible` production eligibility 11/11 (eligible single PUT; unbound/N0/partial non-error skip with no PUT and no `lastError`; outbox-not-drained/busy `needs-sync` with no PUT; 409 `conflict` + 400 `publish-failed` deferred with single PUT, cursor/outbox/chat truthful, no throw to the op-log sync; manual `sync()` never PUTs; finding-01 preemption `needs-sync`/busy with no PUT and no `lastError` overwrite; busy error typed via `instanceof`) plus finding-01 busy-retain 1/1 (needs-sync retains intent, next schedule publishes once, no tight retry). Neighboring focused suites re-run green without behavior change — `syncBaselinePublish` + `syncBootstrap` + `syncRealtime` + `syncAutoConfigFailure` + `syncBaselineEndToEnd` 52/52 (`main-native`). No full `pnpm build:check` in this delivery (per task scope). Verification boundary: no Playwright E2E for the auto path; `publishBaseline()` explicit entry semantics/tests unchanged; still limited validation, not production-ready. Still not implemented (code-less target, ADRs unchanged): publisher election, lease/ack, stale-`409`-beyond-next-local-change, remote frame ops beyond `SYNC-DATA-048` (compound/unsupported paths), GC/compaction, frequency config, permanent fingerprint, UI setting.
- Incremental `topicMessage` order_frame (2026-09-11 seventh unit, `SYNC-DATA-048`/`SYNC-CC-024`, plus transient errata closure): `src/main/services/sync/__tests__/syncTopicMessageOrderFrame.test.ts` 23/23 on the canonical pnpm Node lane (Node v24.11.1/pnpm 10.27.0) — validator closure, atomic issuance with clock mirror and same-tx rollback, reorder complete/incomplete, unsupported zero-op, apply newer/older/equal-idempotent/equal-divergent/deleted/unknown/wrong-parent/suffix/incomplete-fail-closed/out-of-order-orphan, real-relay push/pull `400`/`409`/idempotent, dual-profile append/reorder/delete convergence without baseline, plus stable→transient local-invalidate with 0 op on all three paths (`updateMessage`, `appendMessage` overwrite, `updateMessageAndBlocks`) with truthful partial candidate, transient→stable promotion inclusion with exact single op, no-change zero-op, missing-membership invalidate, mint-path exhaustion rollback, and dual-profile proof that pure exclusion never enters the relay while promotion inclusion converges remotely with full winning-row identity — plus updated `syncParentOrderFrame` reorder/transition, `syncBaselineEndToEnd`/`syncAutoPublishEndToEnd` N+1 frame-advance assertions; neighboring focused suites re-run green (`syncIntegration`, `syncService`, `syncFieldMerge`, `syncMembershipClock`, `syncAutoPublish`, `syncBaseline`, `syncBaselineApply`, `syncBaselineFrames`, `syncBaselineWireProjection`, `syncBaselineWireApply`). Typecheck focused on the touched surface; `pnpm format` clean; `git diff --check` clean. No full `pnpm build:check` in this delivery (per task scope; aggregate left to Validation). Still limited validation, not production-ready.
- Incremental `messageBlock` order_frame second-pair extension (2026-09-12 ninth unit, `SYNC-DATA-048`/`SYNC-CC-024`, user-authorized implementation, same wire contract, no new lock; limited validation, not production-ready): `src/main/services/sync/__tests__/syncMessageBlockOrderFrame.test.ts` 15/15 on the canonical pnpm Node lane (Node v24.11.1/pnpm 10.27.0) — dual-pair validator closure + cross fail-closed, ordinary issuance with mirrored clock/atomicity (append/bulk/update/delete + trustworthy-membership parent promotion complete frame; pre-existing promotion stays 0-op partial), exclusion/unsupported/compound/pure-edit 0-op + partial, malformed rollback, apply newer/older/equal/equal-divergent/deleted/unknown/wrong-parent/suffix/incomplete/dense/orphan, real-relay `400`/`409`/idempotent/verbatim, dual-profile stable create/delete incremental convergence with winning-frame order and explicit no-manual-reorder scope, plus F1/F2 single per-parent frame audit (pre-existing promotion entity-only 0-op + trustworthy-promotion single mint, combined stable true-create/inclusion single mint, rollback residue, exclusion priority, batch per-parent cap) — plus updated `syncParentOrderFrame` exclusion-invalidate, `syncTopicMessageOrderFrame` empty-block-frame outbox, and re-run green `syncTopicMessageOrderFrame`/`syncParentOrderFrame`/`syncFrameHighWater`/shared `payloadFilter` (65/65), `syncMembershipClock`/`syncFailClosedRegression`/migration010/011/012/relay `baselineResource`/shared `baselineWire` (110/110), `syncBaseline`/`syncBaselineApply`/`syncBaselineFrames`/`syncBaselineWireProjection`/`syncIntegration` (110/110). Typecheck clean; `pnpm format` + `git diff --check` clean. No full `pnpm build:check` in this delivery (per task scope; aggregate left to Validation). Still limited validation, not production-ready.

- Frame timestamp high-water (2026-09-12 eighth unit, `SYNC-DATA-048` local implementation invariant, no wire change): additive migration `012_sync_frame_high_water` with `src/main/services/chatDb/__tests__/migration012.test.ts` 6/6 (registration/DDL/backfill, empty DB, pre-012 seed, idempotent never-lower re-run, CHECK rejections, violation rollback, MAX_SAFE) plus `src/main/services/sync/__tests__/syncFrameHighWater.test.ts` 6/6 (invalidate→remint strictly greater for any operationId byte order; remote incoming winner advances the mark with older-state no-lower and local remint above; baseline bootstrap/merge then invalidate/remint strictly greater; same-tx atomicity with post-advance failure rollback and failed-allocation no-advance; MAX_SAFE mint fail-closed with zero residue; dual-profile relay where a crafted minimal-id new winner still wins remotely by timestamp). All frame-landing paths covered by the single unified helper (`persistParentFrameInTx` for local issuance + remote incremental apply, shared `mergeValidatedBaselineInTx` core for local baseline apply + receiver wire apply); invalidation paths never touch the mark; test isolation and profile/channel ownership follow existing semantics. Still limited validation, not production-ready.
- Git owns run history; this document carries no per-run history.

## 8. Fallback activation condition

PowerSync, cr-sqlite with relay, Turso Database Sync, and Automerge-family approaches are conditional fallbacks only.

A fallback is considered only when both hold in the same concrete scenario: a reproducible blocker on the primary path against the goal in Section 1, and evidence of a clear advantage of that fallback in that same scenario. Activation requires a new explicit decision. No parallel comparison runs before that decision.
